# Project Log

Use this file to preserve context between coding sessions. Keep entries short and update the top section before committing.

## Current status

Implemented the first basic vertical slice scaffold plus initial multi-client lifecycle support, improved transcript readability, file/slash-command autocomplete, transcript scroll protection with a floating "Jump to latest" affordance, the first session identity/sidebar usability pass, a compact-successful-tool transcript pass, and the first canonical session title/summary metadata pass. The latest transcript noise, running-control, message-action, session-sidebar, controller handoff, reconnect/restart, scroll-protection, session-metadata, and inspector-removal changes have now been dogfooded/partially harness-validated; current direction remains workflow usability over broad polish, with follow-up work focused on incremental refinements from real-use feedback:

- Bun workspaces monorepo with `apps/server`, `apps/web`, and `packages/protocol`.
- React web theme preference now applies at app startup through a shared theme helper instead of depending on the Settings route, so `piWebThemePreference` persists across reloads for the whole site and follows OS changes when set to System.
- Real chat-only sessions now use the same file-backed pi session manager as workspace sessions while keeping tools disabled; Bakery also keeps a tiny durable submitted-prompt receipt in SQLite until pi emits the official user message, then reconciles it, so reloads/backend restarts can show submitted prompts instead of title-only empty transcript ghosts.
- React web now restores the old composer model/thinking surface: session runtime settings flow from snapshots and `settings_update` messages, the composer shows a model/thinking popover plus context usage, and the Show thinking toggle persists locally.
- React composer was reshaped toward an AI chat input pattern using smaller local pieces for notices, attachment tray, textarea, toolbar, mode badge, model/thinking menu, and context usage while preserving slash/file autocomplete, image attachments, bash modes, follow-up, abort, and runtime settings behavior.
- UI harness validation selection now recognizes common React rewrite surfaces and harness family files, mapping composer/autocomplete/question/transcript/session shell/connection/sidebar edits to focused fake-agent scenarios before broad-suite escalation; related React stable selector hooks were restored for question, plan-card, session-list, and file-autocomplete coverage.
- React composer mode is now selectable from a popout menu with Prompt/Steer, Bash, and Bash without context options; typed `!`/`!!` still override for compatibility, but users no longer need to remember the shorthand to choose a mode.
- React composer supports `Cmd+.`/`Ctrl+.` to cycle Prompt/Steer, Ask, Plan, Bash, and Bash without context without editing the draft text; the Send/Guide button now follows the active mode accent color. Accepting a Plan Card now forces the accepted-plan draft back to Prompt mode so it continues the approved work instead of launching another `/plan` loop.
- Fixed the React `/new` composer draft leak where a previously accepted plan could prefill the newly created session with `Proceed with the recommended plan.`: plan/running-queue prefill events are session-scoped, consumed after application, and the composer remounts per session while the slash-command harness asserts `/new` lands on an empty prompt.
- Fixed the current React image intake failure where native paperclip selection and image paste could silently do nothing before any thumbnail/notice appeared: the paperclip now uses a persistent React-owned file input reset after every change, paste handling reports unsupported image-like clipboard payloads, and the iteration selector now routes image-intake files to image-focused harness scenarios.
- Fixed React live transcript tool-call-only assistant rows so SDK tool call announcements are consumed as tool metadata instead of rendering stray `↳ read ...` lines with extra vertical space; mixed assistant text also keeps tool-call labels out of the visible/copy body.
- Continued the stacked mobile React cleanup: portrait mobile prompt text now avoids browser focus zoom, composer growth follows the CSS mobile cap, the toolbar is denser with attachment/send controls pushed to the right edge, context/workspace/status chrome collapses on phones, the paperclip file input is clipped to its own tap target, the compact 360px header keeps hamburger/title/Details usable, the mobile drawer prioritizes session switching, mobile dialogs/popovers stay onscreen, transcript rows guard rich content from overflow, normal Question Cards show all options inline above the composer, too-many-option questions use an explicit Show all options chooser instead of a confusing always-clipped card scrollbar, and custom freeform answers now stay in the normal composer instead of a secondary in-card input.
- Fixed mobile Safari viewport/safe-area behavior for fresh sessions: the React shell now uses dynamic viewport height instead of `100vh`, the page/root are overflow-contained to reduce browser chrome rubber-band scrolling from composer gestures, the header opts into top safe-area padding via `viewport-fit=cover`, and mobile composer padding accounts for the bottom safe area.
- Added first-class Ask composer mode over the WebSocket protocol; Ask sends an `ask` client message that the backend currently maps to a guided prompt discouraging file edits, shell commands, and tool calls unless explicitly requested, leaving room for stricter per-turn enforcement later.
- Fixed draft-session workspace selection so choosing a workspace promotes the draft to a workspace session, reconnects the current session stream, classifies any session with a workspace path under that workspace, moved the draft workspace selector from the composer footer into the top session header, keeps the selector visible/tappable on mobile draft sessions, and keeps model/context controls visible before the first prompt using the backend's available-model catalog.
- Removed the React composer's bottom transcript fade overlay after dogfooding showed it dimmed the last visible transcript line above the input.
- Fixed real pi session startup so `PI_WEB_DEFAULT_MODEL` is applied to brand-new SDK sessions instead of letting the SDK fall back to its own default model; restored existing session files and pending explicit web model selections remain preserved.
- Added a Settings > Models default-session-model preference for brand-new sessions. The SQLite app setting takes precedence over `PI_WEB_DEFAULT_MODEL`, while explicit draft composer model selections still override at promotion time and existing/restored sessions keep their saved model.
- Fixed React image paste regressions: image-only sends now use the default inspection prompt instead of sending protocol-invalid empty text, image clipboard extraction handles both `files` and `items`, document-level image paste attaches when focus is outside other editors, and the focused image harness now covers pasted image-only sends plus the restored paperclip selectors.
- Restored React composer screenshot artifact upload for normal image intake: paste/drop/paperclip still attach prompt image bytes, and supported PNG/JPEG/GIF/WebP files now also upload through the session artifact API and append `Screenshot artifact: .bakery/artifacts/...` draft lines for transcript previews.
- Fixed server-backed attachments so uploaded `.bakery/attachments/...` image references are rehydrated from artifact storage and passed to the pi SDK as prompt image bytes; agents no longer need direct filesystem access to the UI-only attachment path.
- Updated Bakery's pi SDK dependencies from the old `@mariozechner` 0.70.2 line to the renamed `@earendil-works/pi-ai`/`@earendil-works/pi-coding-agent` 0.74.1 packages so container `bun install` resolves the same pi runtime as the local installation.
- Fixed React live transcript hydration so session metadata updates no longer reset the visible transcript from a stale snapshot message array; this preserves the just-sent first user prompt while the assistant response streams instead of requiring a session switch/reopen to see it.
- React transcript integration now preserves the agreed main-branch transcript/tool semantics for the first pass: active tool snapshots hydrate after reconnect, bash delta output streams visibly, persisted slash-command receipts decode as polished rows, SDK tool results keep `toolCallId` row identity, duplicate tool/developer-bash receipts stay quiet, assistant streaming uses the main-style placeholder, plan cards keep Reject/Dismiss, artifact image paths use main-style normalization, transcript follow state persists with distinct visible-row unread counts, and subagent/tool/system rows regained stable hooks/actions for focused harness coverage.
- Added the backend workspace approval foundation plus sidebar Add Workspace UI: configured workspace roots now act as Browse Roots, persisted workspaces act as Approved Workspaces for runtime permission including descendants, `/api/workspaces/browse` lists Browse Roots plus approved workspaces and safe directory children, `DELETE /api/workspaces?path=...` revokes approval without deleting sessions, runtime/file/search/session operations check the combined permission scope while keeping managed git worktree sessions tied to an approved source workspace, and the React sidebar dialog now feels more like a local folder selector with root-bounded breadcrumbs, select-vs-browse row behavior, a quiet manual path fallback, approved-workspace revoke actions, and `~` display for a configured home Browse Root.
- Fixed React live transcript updates by unwrapping normalized WebSocket agent events before they reach the transcript reducer; direct `/sessions/:id` loads now also retry session selection after the session list arrives so the WebSocket opens reliably.
- React deep session reloads now have an explicit bootstrap phase: `/sessions/:id` prefers the route session id during initial fetch and shows quiet shadcn skeletons instead of flashing disconnected/no-session states until the first WebSocket snapshot or a real failure.
- React sidebar recent sessions now also respect app bootstrap by showing quiet non-clickable row skeletons instead of briefly flashing the real empty state before sessions load.
- React header now has a Session Details dialog with session metadata, workspace/isolation fields, metadata generation/apply, and Preview Stack status/start/stop/open/copy controls for isolated worktree sessions.
- React transcript now restores the quiet action surface and `/plan` presentation: rows expose hover/focus copy actions, `/plan` output renders as a compact Plan Card with fullscreen details and Accept-plan composer prefill, and paused transcript following shows a floating Jump to latest button with unread update counts. Opening an existing session now initially lands at the latest transcript content even if a previous session had paused follow-latest, while user scroll-away protection still applies after the initial resume scroll. User and assistant message rows now reveal subtle local timestamps only on row hover/focus beside the copy/fork actions; restored real pi snapshots use the durable `SessionManager` branch timeline with persisted entry timestamps instead of `session.state.messages` as the transcript source, and numeric SDK message timestamps normalize for older/reloaded snapshots.
- React WebSocket server error envelopes now render as visible transcript error rows instead of being silently ignored, making stale-backend protocol mismatches like pre-Ask `bad_message` failures debuggable.
- Fixed React transcript follow-latest behavior during streaming assistant updates by scrolling on item updates and content resize while the reader remains at the bottom. Follow-latest now also stays anchored through lazy-loaded screenshot/artifact thumbnail layout shifts, avoiding the Jump-to-latest flash/oscillation seen in image-heavy sessions.
- Replaced the composer model/thinking native selects with compact shadcn-style Base UI Select controls so the settings popover matches the rest of the composer UI.
- React sidebar shell now uses the actual generated shadcn `sidebar-07` primitive family, with Bakery-specific workspace/session/actions/settings content ported into the generated sidebar layout, icon collapse rail, and mobile Sheet drawer.
- React sidebar/browser chrome now use the new chef-hat Bakery logo asset, with the top-left sidebar header simplified into a static Bakery identity instead of a workspace dropdown/path, the duplicate footer identity removed, Settings icons aligned on the plain cog, and a reusable themed brand tile so the black mark stays legible in both light and dark mode.
- React app settings now open as local dialog state from the sidebar/search action instead of using a `/settings` modal route; the route helper only owns real content routes, keeping settings aligned with its dialog behavior.
- React Session Details now has a stronger attention hierarchy: title/summary and explicit metadata generation lead the dialog, workspace/worktree fields get primary weight, date/version fields sit in compact supporting tiles, and session-tree/preview-stack internals are quieter secondary sections.
- React sidebar session pinning now follows the redesign-navbar pattern more closely: pin/unpin is a visible inline row action instead of an overflow-menu item, pinned rows keep the unpin affordance visible, and pinned sessions show a small workspace cue while floating above workspace groups.
- React sidebar session rows now suppress low-signal `idle` and `isolated` title badges; running/error/aborting statuses still surface, while isolation remains available through the session metadata line and details dialog. The selected session row now uses live connection status over cached session status, so a stale `running` snapshot no longer leaves the spinner visible after the active run is idle.
- React sidebar session rows now use a one-line layout with compact recency (`now`, `2m`, `1h`, `3d`) in the overflow-action slot by default; hovering/focusing swaps that same slot to the ellipsis menu to reduce visual clutter.
- React sidebar recency now means work recency instead of access recency: session cards, recency groups, pinned ordering, workspace ordering, and Details use transcript activity when present and fall back to `createdAt` for empty sessions, so reopening an old session no longer makes it say `now` or jump upward solely from `lastOpenedAt`.
- Continued the sidebar/search/transcript dogfood polish: sidebar actions now follow a quieter Codex-like hierarchy, workspace rows use folder/open-folder icons with the active workspace tint on the folder, running sessions use a small blue spinner instead of a loud `RUNNING` pill, mobile keeps the Search row, command search has a wider dialog with an explicit close button, compact recency switches to days after 24h, queued follow-up pills truncate instead of bleeding out of the card, workspace collapse/expand animates through Base UI height transitions, and transcript/attachment images open in a fitted lightbox preview.
- React session headers keep isolation visible where it matters by showing a subtle `Worktree` chip on isolated session pages and moving the subtitle copy back to repo/branch metadata instead of repeating `isolated`.
- React sidebar creation is back to a single clean `New session` button after dogfooding the adjacent isolated-session dropdown; isolated session creation remains available through the command palette action.
- Fixed React sidebar pinning so the row-level pin action is direct again and pin/unpin updates session state optimistically; pinned sessions immediately move into/out of the Pinned section while the server patch reconciles.
- Hardened React sidebar pinning against WebSocket/session-list races: incoming snapshot/metadata session payloads now merge through a shared helper that preserves pending pin choices, preventing a just-pinned row from snapping back into its workspace group.
- React sidebar New session/New isolated session actions now route directly to the newly created session after creation instead of leaving the browser on the previous page.
- React session lists now expose a trash action with confirmation; deleting a session calls the existing backend DELETE route, removes it from local state, and routes to the next session or `/sessions` when the open session is deleted.
- React controller handoff is exposed in the React UI: WebSocket controller snapshots/updates feed the session page and composer, viewer tabs show a Take control banner/button, and composer sends are disabled until control is claimed instead of surfacing repeated `not_controller` server errors.
- Fixed the React duplicate-tab WebSocket disconnect loop by keeping server-issued client ids in tab-local hook memory instead of shared `localStorage`; separate tabs now receive separate ids, while reconnect-controller harness coverage no longer clears the old shared key workaround.
- Containerized development image includes common agent/dev CLI tools, including `ripgrep` (`rg`), `fd` (via Debian `fd-find` with an `fd` symlink), and GitHub CLI (`gh`), plus Playwright Chromium/headless-shell Linux runtime libraries for in-container UI harnesses, and points pi at a container-local agent-dir overlay so host-mounted macOS/Windows managed binaries do not shadow Linux tools. Container docs now clarify the Git/GitHub auth posture: local Bun piggybacks on host backend-process auth, while Docker Compose keeps safe defaults and documents trusted-dev SSH-agent/`gh` auth opt-in recipes for remote Git/GitHub operations.
- Fixed container-backed real pi session startup when the pi SDK resolves npm package resources: the dev image now includes `npm` plus a native build toolchain, points global npm installs at writable `/workspace/.bakery-data/npm-global`, filters the known Bun-incompatible `npm:@howaboua/pi-codex-conversion` host package from the container settings overlay by default, and the server runner repairs sparse non-login `PATH` values before creating SDK sessions so `npm root -g` / package install checks do not fail when launched outside an interactive shell. The container overlay now also normalizes legacy pinned Git `pi-subagents`/`pi-intercom` installs to npm packages and links Bakery's workspace Pi runtime packages into overlay/global module roots so subagent child `pi` processes can resolve extension peer dependencies.
- Fixed the SSH auth Compose override for Docker Desktop/OrbStack-style dev containers: the override now defaults to `/run/host-services/ssh-auth.sock`, still accepts host/container socket overrides, mounts host `known_hosts` read-only, validates `ssh-add` plus SSH Git remote reads, and the dev-server manager now ignores stale host/container pid files before restarting the backend.
- Shared Zod/TypeScript protocol definitions, including controller state, session lifecycle config, and runtime model/thinking settings.
- Fastify backend with localhost/token auth, browser-origin gating for HTTP/WebSocket access, workspace allowlist checks, config/workspace/model endpoints, persistent managed workspace entries, Git clone/GitHub repository workspace creation endpoints, session CRUD, SQLite metadata, and WebSocket session endpoint.
- In-process pi SDK session runner wired for prompt, steer, follow-up, abort, model/thinking changes, snapshots, and normalized event streaming.
- WebSocket session hubs now support reconnect snapshots, multiple clients, first-client controller assignment, take-control, per-client sequence envelopes, disconnected idle disposal, and transient active-tool snapshot hydration so running Subagent Cards survive browser refresh/reconnect until their final tool result arrives.
- Minimal Vite web component UI for API/token settings, workspace/session list, session creation/opening, WebSocket connection, readable transcript rendering, a consolidated model/thinking picker, prompt/steer/follow-up input, abort controls, controller/viewer status, and TUI-like transcript follow-latest auto-scroll.
- Composer/footer polish now softens the hard transcript/composer boundary with a reserved-space gradient wash, keeps the centered composer width, includes the pending `ask_question` panel in the same composer-area visual language, and uses a compact desktop question layout with subtle desktop shortcut numbers while preserving touch-friendly mobile controls. The lower transcript/composer fade has been tightened after real-use feedback so the composer glow no longer washes far up into transcript content. Fresh empty sessions now use the Empty Session Landing: a centered capped auto-growing composer, stable baking/cooking quote, and compact `/plan`/Screenshot/`@file`/`!bash` chips that disappear once the composer needs more space.
- Assistant transcript rendering now handles Markdown via `marked`, hides thinking by default with a `Show thinking` toggle, renders readable thinking traces dim/italic when enabled, and formats inline tool calls compactly instead of dumping raw provider JSON/encrypted thinking payloads.
- Tool execution cards now use more TUI-like compact titles, green/blue/red status backgrounds, and cleaner result extraction from text/image/diff/stdout/stderr result payloads.
- Tool activity rendering has been simplified again: nested live/completed tool stacks were removed in favor of flat tool receipt rows, reducing mobile inconsistency and deleting the fragile group patch/restore path. The latest receipt design uses a structured action-first header (`bash · command`, `read · path`), suppresses routine success summaries/status text, only shows noteworthy collapsed durations, and keeps failed rows aligned with successful rows except for the red expand chevron. A new URL/localStorage-gated `toolUi=bash-card` experiment can render only Bash tools with a terminal-style card while preserving the default flat receipts for all operators.
- Web dev server now disables Vite browser HMR/reload by default so in-browser agent edits do not refresh/kill the UI session; set `PI_WEB_VITE_HMR=true` to opt back in. Server `dev` now runs without Bun watch by default so edits to backend/shared packages do not restart and kill in-process pi sessions; use `bun run dev:server:watch` to opt into backend watch mode. The default local bootstrap is now `bun run dev`, which ensures a detached backend is up and then runs Vite in the foreground; use `bun run dev:server:restart` to restart only the backend during the dev loop. The web UI also remembers and reopens the last selected session after a page reload. The React Vite dev server now allows Tailnet `.ts.net` hostnames by default, configurable with `PI_WEB_ALLOWED_HOSTS` or Compose's `PI_WEB_VITE_ALLOWED_HOSTS`, keeps HMR/reload disabled unless `PI_WEB_VITE_HMR=true`, ignores local `.bakery`/`test-results` churn, exposes the `dev:lan` script expected by Docker Compose, and avoids reconnecting an already-open selected session from route/session-list churn so Tailscale browser access is not blocked by Vite host validation or local log-write reload loops.
- Added the first repo-local Bakery Launcher prototype: `bun run bakery` starts the existing backend plus Vite UI as foreground child processes, prints the local UI/API URLs and selected workspace root, defaults the workspace to the invocation directory when `PI_WEB_WORKSPACE_ROOT` is unset, and shuts both children down on Ctrl+C. This proves the intended future `bunx bakery` notebook-style UX without adding npm `bin` metadata, publishing changes, or single-port static serving yet.
- Added the first clean-path route boundary: opening/creating/selecting a session pushes `/sessions/:id`, direct loads of `/sessions/:id` open that session, Back/Forward switches session routes, and Back to `/` clears the selected session while keeping the current app shell. Route parsing/building lives in a small tested web helper instead of introducing a full router dependency.
- File search/complete endpoints now call the ignore-aware workspace scanner, validate query params with shared Zod schemas, cap result limits, skip default heavy/binary paths, and keep nested `.gitignore` rules scoped to their subtree.
- Prompt input now has `@file` autocomplete backed by the file search/complete endpoints, with keyboard navigation, click selection, directory continuation, and prompt draft preservation across live transcript rerenders; fixed dropdown closing during rerenders while the prompt remains focused.
- Added shared command metadata protocol, `GET /api/sessions/:id/commands`, and prompt-box slash-command autocomplete for built-ins, extension commands, prompt templates, and skills; terminal/UI-only built-ins are marked unsupported in metadata.
- WebSocket prompt handling now intercepts built-in slash commands before they reach the LLM as normal prompts. Implemented web results for `/reload`, `/changelog`, `/session`, `/compact`, `/name`, and `/copy`; unsupported built-ins render a structured web error instead of being sent to the agent.
- Prompt drafts that start with `!` or `!!` now run through pi's native bash execution path over WebSocket: `!` records output in session context, `!!` marks the bash execution excluded from LLM context, snapshot hydration renders persisted `bashExecution` rows as terminal-style tool cards, and the first web slice blocks direct bash while an agent turn is already running. Developer-run bash rows now stay expanded by default, avoid completed-tool grouping, use distinct local-bash styling so they do not look like ordinary agent tool calls, and coalesce SDK `bashExecution` echoes into the optimistic local bash row so failed commands do not render as a second card. Live web bash updates now stream append-only deltas with byte/chunk metrics instead of rebroadcasting cumulative output on every chunk; clients append deltas, retain legacy cumulative-output support, and show an explicit missing-prefix placeholder if a reconnect catches a stream mid-command before the final result arrives.
- Fixed the server startup regression from the `/changelog` implementation by replacing Bun-incompatible `createRequire(...).resolve("@earendil-works/pi-coding-agent")` usage with `import.meta.resolve` for locating the pi package changelog.
- Fixed command and file autocomplete keyboard navigation so the dropdown scrolls to keep the selected row visible after arrow-key selection changes; later fixed the extracted composer keydown handler to read live autocomplete state after async result patches, restoring Arrow/Tab/Enter selection for slash commands and `@file` matches.
- Fixed the follow-up regression from that change: initial render no longer calls `querySelector("")`, so the app refreshes workspaces/sessions instead of staying empty.
- Continued the behavior-preserving `main.ts` deslop chain: autocomplete orchestration, plan-action handling, running-queue controls, and settings/session-detail rendering now live in focused web modules instead of the app coordinator.
- Added a right-side Details/Preview inspector panel: transcript messages/tool cards are selectable, selection and collapse state persist locally, Details shows compact metadata/content plus collapsible raw event data with copy controls, and Preview renders Markdown/code plus sandboxed HTML/SVG snippets.
- Improved streaming UI responsiveness by throttling WebSocket-driven renders, reducing transcript follow-scroll layout work, and caching rendered transcript segment HTML.
- Added basic session tree/fork/navigation support: shared tree/fork/navigate protocol schemas, `GET /api/sessions/:id/tree`, `POST /api/sessions/:id/fork`, and `POST /api/sessions/:id/tree/navigate`. The visible right-inspector Tree tab and wide `/tree` drawer were later removed with the rest of the inspector sidebar; transcript tree data now powers Event Fork affordances from user, assistant, and tool rows, with user-message forks reopening the selected prompt as an editable draft and non-user forks continuing through the selected event. Latest follow-up fixed stale transcript action rows after async tree refreshes so newly tree-backed assistant responses gain the visible fork icon without requiring another transcript event; then tightened assistant/user tree matching for titles truncated on word boundaries or numeric provider timestamps. `tree-fork-navigation` now asserts both user and assistant fork affordances.
- Reduced streaming render lockups by throttling live renders further, patching only dirty transcript items during active runs instead of replacing the whole app shell, and hiding running assistant text behind an Assistant Streaming Placeholder until the completed message can render as Markdown.
- Added an agent-operable Playwright UI harness: `PI_WEB_FAKE_AGENT=1` enables a deterministic fake session runner, `bun run test:web-perf` starts backend/web against temp dirs, drives the real browser UI, measures prompt/control responsiveness during synthetic streaming, and writes screenshots/traces/metrics under ignored `test-results/`; `bun run ui:manual` opens a headed fake-agent browser session for exploratory manual validation until Ctrl+C.
- The left workspace/session sidebar is now collapsible, with state persisted in local storage and compatible grid sizing when the right inspector is also collapsed.
- Assistant Markdown, assistant/user image content parts, and tool-result image content now render safe inline images (`http(s)`, `file`, app-relative, and base64 `data:image/png|jpeg|gif|webp`); tool cards containing rendered images stay open by default, and the fake-agent runner can emit a sample inline PNG when prompts mention images/screenshots for visual validation.
- Prompt composer now supports image attachments for new prompts via paste, drag/drop, or file picker, with thumbnails/removal before send; attached images are sent over the WebSocket prompt message into `AgentSession.prompt(..., { images })` and render back in the transcript. After send, attachment thumbnails clear immediately, and image-only sends use a default inspection prompt.
- Expanded `bun run test:web-perf` into an all-scenarios fake-agent suite covering streaming responsiveness, inspector Details/Preview, slash commands, tree/fork/navigation, reconnect/controller handoff, and narrow tool-heavy streams. Harness metrics now include Chromium long-task samples plus app-exported render/patch timing via `window.__piWebPerf`; fake-agent sessions now persist enough JSONL tree data to exercise tree navigation and fork flows.
- Refactored transcript rows into dedicated `<pi-transcript-row>` custom elements. Live patches now update existing row instances, and actively streaming assistant/user text updates the existing `<pre>` `textContent` without replacing the row HTML. `window.__piWebPerf` now also records row-update timings.
- Added web-perf threshold checks to the Playwright harness for long tasks plus render/patch/row-update max timings. Thresholds are intentionally loose and configurable with `PI_WEB_PERF_MAX_*`; set `PI_WEB_PERF_THRESHOLDS=off` to disable locally.
- Fixed transcript selection scroll jumps: clicking an earlier message/image/tool while auto-scroll is enabled now preserves the current transcript scroll position instead of jumping to the bottom.
- Made the fake-agent runner more realistic and deterministic: uneven chunk sizes/cadence, no-delay event bursts, repeated partial Markdown/code fences, and delayed/bursty tool updates interleaved during assistant streaming.
- Added focused Playwright harness coverage for `@file` autocomplete/search+directory continuation, prompt image attachment add/remove/send/render flows, and model/thinking selector updates. This caught and fixed duplicate trailing slashes for directory autocomplete insertion/rendering.
- Added reconnect/restart UX basics: a visible connection-state banner (`connected`/`connecting`/`reconnecting`/`disconnected`/`retry failed`), automatic WebSocket reconnect with backoff, clearer disconnected/send-failed copy, per-session prompt draft persistence in `localStorage`, and warnings that image attachments are prompt-only and not restored after refresh. Added fake-agent harness coverage for reload/reconnect draft preservation and an actual backend process restart (`backend-restart`) that verifies reconnect, draft preservation, and post-restart send usability.
- Simplified multi-tab control handoff: any connected viewer can take control immediately at any time without requesting approval from the previous controller. Removed the pending takeover timeout/config path, approval/denial protocol messages, and stale Control requested/Approve/Deny UI copy; reconnect/controller harness coverage now asserts immediate steal-and-send behavior.
- Transcript scrolling now protects scrolled-up reading during streaming: leaving the bottom pauses follow-latest without yanking the transcript, tracks unread transcript item updates, and shows a floating "Jump to latest" button that resumes follow-latest. WebSocket connection close/retry notices stay in the banner instead of adding transcript rows.
- Dogfooding usability findings addressed/validated in real use: running activity/tool output is quieter with successful tools collapsed/grouped into background activity; connection lifecycle events stay out of the transcript; composer/buttons are more compact and TUI-like; steer/follow-up affordances and queued chips are visible in the compact composer; inspector remains a low-frequency/debug UI; the left sidebar can auto-collapse during active sessions; message actions live in overflow menus.
- Session identity/sidebar usability pass: header now shows an editable session title with repo/path metadata below it; session lists hide raw UUIDs by default, sort by last activity, show last prompt snippets, relative activity time, and status chips; older-than-one-week sessions are hidden behind a persisted "show older" toggle. `/api/sessions` now enriches metadata from pi JSONL/session state with last activity, last user prompt, and active status.
- Successful tool executions now automatically fold back into a smaller, muted activity row when they transition from running to done, with a compact result preview in the header; running, failed, newly selected, and image-bearing tool rows stay expanded/prominent. Re-clicking an expanded tool header now collapses it instead of immediately reselecting/reopening it; transcript row selection binding is restored after full renders and ignores collapsible headers. The Preview inspector now renders assistant/user content segments so inline image parts preview correctly. The Playwright harness now pre-seeds its API base in localStorage so local dev servers on the default port do not race isolated fake-agent runs.
- Prompt composer/running controls are now more compact and TUI-like: shorter textarea, explicit idle vs running mode hint, Enter/Alt+Enter shortcut labels on buttons, running-state footer emphasis, queued steer/follow-up chips in the composer, and `queue_update` events no longer add transcript noise.
- Transcript rows now have quiet per-message overflow menus for Copy, Details, Preview, and valid user-message Fork actions. The actions select/open the inspector without adding always-visible clutter, and the inspector-preview harness now exercises menu-driven Preview/Details flows.
- The session sidebar now communicates whether it is pinned open or will auto-collapse, and opening an existing recent session auto-collapses the sidebar unless the operator has pinned it open.
- Desktop left-sidebar distraction spike: unpinned sessions now open from the header hamburger as a temporary overlay with backdrop/Escape/toggle close behavior, while pinned mode remains a docked left column. New/forked sessions and automatic reload restore now collapse the temporary overlay by default so chat controls are not blocked. The recent-session list is grouped by recency (`Today`, `Yesterday`, `Earlier this week`, `Older`) with group collapse state persisted, and main-area notices remain visible when the sidebar overlay is closed. Harness helpers now explicitly open/close the overlay when scenarios need session cards/settings vs. chat controls. Fixed a follow-up where hovering the sidebar backdrop inherited the global `button:hover` background and visually blanked the chat area.
- Mobile sidebar follow-through: the hamburger drawer now reuses the same recency-grouped session list, keeps Today/Yesterday expanded and older groups collapsed by default, omits the desktop-only Pin affordance, renders a backdrop while open, and keeps session creation/groups ahead of lower-priority settings in the drawer. `mobile-layout` now asserts these drawer details. Mobile initial render also ignores persisted desktop-open/pinned sidebar state so the transcript shell paints first with the temporary drawer closed, while mobile drawer interactions preserve the desktop pin preference.
- Expanded tool-output ergonomics for long-running bash-style commands: tool result text now renders as terminal-like preformatted output while preserving Markdown image payloads, expanded tool bodies are capped to an internal scroll viewport instead of dominating the full transcript, running tool output pins to the bottom while streaming, and the fake-agent narrow tool harness now emits/asserts long terminal output.
- Added safe local image artifact rendering for workspace-relative paths in transcript text/tool output: `GET /api/sessions/:id/files/raw?path=...` serves image previews after session/workspace realpath checks, transcript rendering adds thumbnail cards for detected image paths, and the fake-agent harness now covers an `image-artifact-paths` scenario.
- Tuned compact successful-tool rows to fade further into background activity: repeated completed tools pack more tightly, done badges are quieter, and compact summaries skip boilerplate like `running fake tool...`, stdout labels, and successful exit-code lines while noting long output line counts.
- Adjacent completed non-image tool rows now render as a tighter visual activity group; live and snapshot hydration drop assistant messages that only repeat tool-call metadata, carry their labels onto corresponding generic tool-result rows, and merge obvious duplicate adjacent tool-result rows when they repeat the same successful command output.
- Removed cross-message suppression for local image artifact thumbnails after dogfooding showed repeated screenshot paths could be hidden even when the current message clearly referenced them; each message can now render its own artifact previews.
- Added `repeated-image-artifact-paths` and `artifact-path-formats` Playwright regression scenarios to cover repeated local screenshot paths across assistant messages plus inline-code, fenced-code, and long nested `test-results/.../*.png` artifact path shapes; both are included in the full fake-agent scenario set.
- Added a TUI-like context usage indicator inline with the web composer mode label showing estimated used tokens vs. the active model context window, including percentage and a small progress bar. The indicator is sourced from pi `AgentSession.getContextUsage()`, updates after completed turns, and the fake-agent harness asserts it via a `context-usage` scenario.
- `/new` now works as a web slash command: typing exactly `/new` creates and opens a fresh empty session in the current session's workspace, keeps the previous session running if needed, blocks when prompt images are attached, leaves invalid `/new ...` drafts intact with usage copy, and appears as supported in slash-command autocomplete.
- The UI harness now captures backend/Vite child process output to `server.log`/`web.log` artifacts instead of streaming it to stdout by default, keeping `bun run test:web-perf` concise for agent context; use `PI_WEB_HARNESS_CHILD_LOGS=1` or `--verbose` to stream logs while debugging. Scenario metadata now feeds a typed runner registry so the dispatcher stays thin, and harness runs also write compact `timeline.json` artifacts with route, app-state, and session-WebSocket envelope summaries for lifecycle debugging.
- `/new` now keeps keyboard flow intact by focusing the prompt textbox in the newly opened session once its WebSocket snapshot/controller state is ready; slash-command harness coverage asserts this focus behavior.
- Queued running-control chips now support canceling individual steering/follow-up messages, editing a queued item back into the composer, and sending screenshots with queued steer/follow-up messages. The queue now renders as a visually distinct “Queued for this run” strip over the bottom of the transcript, above the running-input footer, with image-count badges for queued attachments and compact overflow behavior that shows the first three items plus a `+N more`/`Show less` toggle. The backend exposes typed queued-image payloads plus `cancel_queued_message`; real sessions remove items by clearing/requeueing remaining SDK queues while preserving web-queued images, fake-agent sessions mirror the behavior, and the harness covers mixed steer/follow-up/image edit/cancel/overflow flows.
- Added canonical web session title/summary metadata plumbing: persisted title/summary provenance, migration/source inference for existing titles, manual title protection, derived fork metadata, `/name` canonical web-title handling (`/name`, `/name <title>`, `/name --clear`) with pi-name mirroring only for manual titles, WebSocket `session_metadata_update`, header title edit preservation, generic-first-prompt handling that leaves “what's next?” sessions as provisional “New session,” summary chrome collapsed by default, sidebar summary previews, app metadata model setting storage, and a user-triggered ✨ title+summary suggestion flow. Automatic background generation is intentionally deferred/disabled to avoid surprise token spend; generation currently uses a deterministic heuristic/fake path rather than a real model-backed SDK call.
- Added agent commit-hygiene guidance in `AGENTS.md`: prefer concise Conventional Commit messages and one coherent vertical-slice commit when possible.
- Fixed session metadata suggestion apply failures from the browser by allowing CORS preflight for `PATCH` (and other non-GET methods) on the API server.
- Added dedicated fake-agent session metadata harness coverage for generic first prompts, header title edits, `/name`, `/name --clear`, explicit ✨ title-only suggestions, suggestion apply, summary expand/collapse, and sidebar summary previews. Also fixed metadata-update merging so enriched `lastUserPrompt` survives WebSocket metadata broadcasts, adjusted header stacking so summary toggles remain clickable, preserved prompt drafts synchronously on reload, and restored the full fake-agent harness pass including transcript scroll stability.
- Added a `"What's next?" dev loop` section to `AGENTS.md` so future planning prompts re-read `PROJECT_LOG.md`, propose the top slices, recommend one vertical slice, and state scope/validation before implementation.
- Wired real model-backed explicit session metadata generation for non-fake-agent sessions: ✨ now selects the configured metadata model (falling back to current/default/available authenticated model), asks for strict JSON title+summary suggestions outside the transcript/session tree, keeps fake-agent deterministic with the title-only heuristic, and increments generation metadata only for non-deferred suggestions. Removed the metadata-only temperature option after real Codex/OpenAI-compatible providers rejected it, and route metadata generation errors through a cleaner 502 response instead of a raw Fastify 500. Tightened the ✨ button so it sits next to the title text and renders smaller in the header. Polished the suggestion panel with clearer title/summary labels, less alarming model/reason copy, compact action buttons, a Regenerate action, and friendlier generation error text.
- Refined transcript/tool-output ergonomics: expanded tool output viewports use responsive clamp sizing, running tools get a little more vertical room while staying internally scrollable, terminal output wraps less aggressively, and inline/local image previews have responsive thumbnail and expanded sizing with smaller tool-card thumbnails.
- Tightened compact successful-tool rows again: summaries now strip ANSI/control noise and boilerplate, use first+last meaningful lines for long output, focused harness coverage asserts noisy labels stay out of collapsed summaries, and completed tools no longer auto-expand just because their text mentions local image artifact paths.
- Added a higher-level completed-tool activity group: adjacent successful non-image tools now collapse into one `Ran N tools` card that expands to reveal the individual compact tool rows, reducing the vertical “barcode” effect during tool-heavy agent turns. The fake-agent runner and Playwright harness now cover multi-tool grouping. Follow-up visual hierarchy tuning moved tool activity groups and their nested compact rows toward neutral/slate tones so they are easier to distinguish from green assistant conversation cards, then added more transcript/card spacing so action boundaries are clearer.
- Added a first attention-hierarchy pass for active runs and user-input-needed states: active run detail now appears inline in the composer mode row next to `Running input` instead of as a large transcript overlay, completed tool history remains grouped/subordinate, historical selection outlines are quieter, and a separate attention-needed banner is reserved for actionable states like controller handoff, viewer mode, disconnect/retry failure. Fixed the remaining successful-tool expansion bug by keeping completed done tool rows collapsed even when selected; only running/error rows auto-open.
- Added the first Workbench theme foundation: dark/light CSS tokens, a persisted System/Workbench Dark/Workbench Light selector in the sidebar, system color-scheme syncing, broad replacement of the existing hard-coded palette through token aliases, and fake-agent theme harness screenshots for both modes.
- Removed the unexplained green conversation/tool backgrounds from the primary transcript hierarchy: assistant messages are neutral, user messages are blue/info-tinted, tool activity is slate/neutral with success green reserved for small status text, and both themes now use more modern accent/depth tokens for layered cards and transcript surfaces.
- Suppressed empty assistant transcript placeholders so a running turn with only active tool cards/status does not show a large blank assistant rectangle before text arrives.
- Polished local image artifact cards/captions for light mode: theme-aware card/caption surfaces, better contrast, softer radius/shadow, and split path captions with the filename emphasized while preserving full-path title/tooltips.
- Fixed the light-mode running composer footer so the bottom bar uses theme-aware light surfaces/blue border/shadow instead of the previous dark running gradient.
- Fixed the queued follow-up/steer strip in light mode by moving its dark amber gradient, pill backgrounds, borders, and labels onto theme-aware queue tokens.
- Clarified completed tool cards that appear immediately after a running tool: they now render as dashed, muted “also ran” background activity instead of looking like unexplained output attached to the active command.
- Added a small theme audit pass for remaining dark overlays: autocomplete popovers, inline tool/image chips, session summary/suggestion panels, and app settings now use theme-aware surface/border/shadow tokens instead of hard-coded dark translucent backgrounds.
- Added a dedicated `theme-gallery` fake-agent harness scenario that captures Workbench Dark/Light screenshots with user/assistant messages, local image artifacts, grouped completed tools, an active running tool, queued follow-up strip, and running composer footer visible together for faster theme regression review.
- Added first-class web Q&A plumbing: a bakery-owned `ask_question` SDK custom tool for real sessions, shared pending-question/answer WebSocket protocol, transient per-session pending question state that survives browser reconnects while the backend stays alive, controller-only `answer_question` handling, a sticky composer-area “Answer needed” panel with options/custom answer/cancel and display-only recommendations, fake-agent question simulation, and `question-answer` Playwright coverage for option answer, cancel, and reload/reconnect preservation.
- Added the first bundled workflow launcher for dogfooding `ask_question`: `/grill-me` appears in slash autocomplete as a skill-style command, accepts an optional focus, expands to a codebase-interview prompt that tells the model to inspect project context and ask one recommended question at a time via `ask_question`, and is covered through the slash-command fake-agent harness path.
- Dogfooded `/grill-me` in a real session: the model used `ask_question` correctly. Follow-up UX improvements now make recommended answer options visually explicit, focus the question panel when a new question arrives, and support arrow-key/Home/End navigation across options before pressing Enter.
- Answered `ask_question` interactions now leave durable compact transcript context: completed/cancelled question tool results derive a quiet Q&A summary row showing the question and chosen/custom/cancelled answer, while the original tool card remains available for details/debug. Snapshot hydration also derives the same summaries so answered questions stay scannable after reload.
- Fixed a sporadic blank assistant transcript card seen during real dogfooding: non-renderable assistant events are now removed from transcript state and live DOM patches remove any existing row instead of updating it into an empty `Pi` card.
- Added explicit recommended-option plumbing for `ask_question`: the tool schema and shared pending-question protocol now accept `recommendedOptionIndex`, fake-agent questions populate it, and the web UI uses it for option highlighting/focus with the previous recommendation-text heuristic as fallback. The Q&A harness now captures pending-question screenshots before answering so artifacts actually show the recommended-option highlight and arrow-key navigation state, and running `ask_question` tool cards are collapsed by default while the sticky question panel is active instead of showing duplicated/raw question args. When an explicit recommended option is present, the sticky question panel now shows the recommendation only on the option badge/card and suppresses the redundant top recommendation line. Keyboard handling now supports number-key option submission and `c` to focus the custom answer even if focus takeover missed the panel, with visible keycap hints on option cards, the custom-answer row, submit button, and footer hint text. Question options now render vertically so Up/Down navigation matches the layout and option cards have more room for richer content.
- Fixed completed read/image tool cards expanding by default just because they contain image results or local artifact paths; visual tool results now auto-open only while running/error, keeping completed image reads as compact activity unless selected/expanded.
- Continued keyboard-first Q&A polish: pending question panels now advertise and support `Esc` cancellation, focus the panel when controller controls are disabled, guard answer/cancel sends to controllers with an active connection, return focus to the prompt textbox after option/custom/cancel answers, and explain that keyboard answer shortcuts are disabled in viewer/disconnected states. The Q&A harness now covers `Esc` cancel, prompt focus restoration after answers, focus takeover after reload, Tab movement through options, viewer shortcut no-ops, and Workbench Dark/Light screenshots for controller and viewer-disabled pending-question states.
- Modernized the pending Q&A composer panel to match the newer neutral composer/top-bar design language: options now render as clearer decision cards, recommended choices use calmer accent treatment, the custom answer row is secondary, desktop keeps keyboard shortcut hints, and mobile/narrow layouts replace those hints with touch-first guidance. Initial real-session dogfooding reported the updated question UI looks good.
- Fixed live transcript patch ordering for tool rows: patch updates now find rows nested inside completed-tool groups, newly patched rows insert according to transcript order instead of always appending, and manual expand/collapse preserves group positioning so expanding a lower tool row no longer appears to “fix” a weird order.
- Fixed pending Q&A Enter handling: focused/recommended option cards now answer on `Enter`/Space via explicit keyboard handling instead of relying on browser button activation, with focused harness coverage. Also made `/new` prompt focus restoration deterministic after the full-suite slash-command scenario exposed a race.
- Added a typed bundled workflow-skill registry and moved `/grill-me` metadata/prompt generation into it, so real and fake runners share the same launcher plumbing while preserving current slash-command behavior.
- Fixed completed markdown-card text selection being cleared by row click handling: transcript rows now preserve browser text selection after drag/highlight gestures instead of selecting the row and triggering a full render, with focused Playwright coverage.
- Renamed the bundled planning workflow launcher from `/grill-me` to `/plan`, tightened its generated prompt around the repository “what's next?” loop, one-question-at-a-time `ask_question` behavior, selectable recommendations, and final validation-plan handoff, and added prompt assertions to `bun run check`.
- Tightened `/plan` again to follow the canonical `grill-me` skill more closely: it now frames planning as a relentless design/codebase interview, walks decision-tree branches one-by-one, explores discoverable codebase/project-note answers instead of asking the operator, requires a recommended answer for each question, and keeps the final handoff focused on the smallest implementable slice.
- Compact workflow launch prompts in the transcript: `/plan` still sends the full planning instructions to the agent, but user-visible transcript rows now show a short “Launched /plan workflow” summary with focus instead of the full generated prompt block; raw details remain available in the inspector JSON.
- Extended workflow launch compaction to session/sidebar snippets and session tree labels, and added slash-command harness assertions that `/plan` stays compact outside the main transcript too.
- Added a local iteration telemetry JSON generator (`bun run report:iteration`) that aggregates recent git commit churn, `PROJECT_LOG.md` verification command mentions, and `test-results/ui-harness/**/metrics.json` artifacts into `test-results/iteration/iteration-report.json` for choosing speed-of-iteration improvements from evidence.
- Extended the iteration telemetry script with a validation selector (`bun run report:iteration --recommend <changed files...>`) that maps changed files to focused check/harness commands plus optional full-suite escalation guidance.
- Added an agent action insights mode (`bun run report:iteration --agent-actions`) that turns current telemetry into agent-behavior optimization guidance: validation selection, high-churn edit surfaces, recurring harness loops, and an explicit missing per-tool telemetry gap.
- Added agent iteration telemetry guidance to `AGENTS.md` and root check assertions so future agents are directed to use `bun run report:iteration --agent-actions --recommend` for iteration/agent-behavior planning and `bun run report:iteration --recommend <changed files>` before validation selection.
- Made validation selection more handoff-ready: `bun run report:iteration --recommend ...` now prints a copyable `## Validation decision` block with changed files, commands, optional escalation, and explicit full-suite run/skip guidance; `AGENTS.md` asks future agents to include that decision for non-trivial validation choices.
- Added `bun run report:iteration --session-context`, which reads local pi JSONL session logs, reports per-tool call counts/result payload sizes, largest tool-result contributors, and latest model usage without printing tool contents; use `--session <path>` to inspect a specific log.
- Refined session-context reporting with offline tool-input summaries: top read paths, top bash commands, repeated tool inputs, largest-result input labels, and actionable recommendations for repeated/large context contributors.
- Extended session-context reporting into a fuller current-session workflow telemetry view: validation command summaries with rerun/failure counts and rough durations, edit/write attempt success/failure counts by file, largest tool-result durations, and recommendations for validation reruns, edit retry loops, and mobile-layout artifact handoffs. Added `bun run report:iteration --session-history` to backfill all available local JSONL session logs into aggregate tool/validation/edit/context trends without printing raw tool contents. Added the project-local `.pi/skills/iteration-observability` skill so pi agents can explicitly load the telemetry workflow and turn the data into process recommendations.
- Added bounded recent-history iteration telemetry: `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session` now scans only the latest N local JSONL logs while skipping the current/latest session, and the iteration-observability skill documents this recent-history workflow. The same report now prints focused harness artifact pointers for repeated failed scenarios so agents can inspect `failure.txt`, logs, and screenshots before rerunning.
- Added a first-class latest harness artifact lookup: `bun run report:iteration --latest-artifact [scenario]` short-circuits generic report generation and prints the newest artifact path plus failure/log/screenshot inspection commands, while `--recommend` now shows copyable latest-artifact commands for selected focused scenarios. Use the rollout measurement protocol from the latest verification note to compare normalized repeated failed focused-harness reruns after enough new sessions.
- Added compact `--brief` output for `bun run report:iteration --session-context` / `--session-history` and first-class subagent-loop telemetry that summarizes parent subagent calls/results, output discipline (`output:false`, file-only, explicit paths), default parallel-output collision risk, child session path mentions, and chain artifact mentions without printing raw child content.
- Added `bun run project:notes`, a compact project handoff command that summarizes the design goal, recent status, latest verification, and next priorities while capping long `PROJECT_LOG.md` history; `AGENTS.md` now points agents to it before broad log reads.
- Started the architecture-review/iteration-speed refactor by extracting transcript rendering/model helpers and the `<pi-transcript-row>` custom element from `apps/web/src/main.ts` into `apps/web/src/transcript.ts`, with shared HTML/path/perf utilities in `apps/web/src/utils.ts`; this is intentionally move-only to reduce future main-file edit blast radius without changing behavior.
- Continued the iteration-speed refactor by extracting autocomplete token/render helpers and state types into `apps/web/src/autocomplete.ts`, adding focused Bun tests in `apps/web/src/autocomplete.test.ts`, and keeping the app-shell API/timer/focus wiring in `apps/web/src/main.ts`.
- Continued the architecture-review/iteration-speed pass by extracting pure session-tree flattening/path/render/fork-match helpers into `apps/web/src/session-tree.ts` with focused Bun tests in `apps/web/src/session-tree.test.ts`, while leaving DOM focus, API navigation/forking, and drawer state in `apps/web/src/main.ts`.
- Removed redundant connection banner copy, then made the connection row contextual: plain connected sessions no longer spend a full-width row, while reconnect/disconnect/retry and draft/image-refresh warnings still surface there.
- Added a focused `connection-disconnected` UI harness scenario that stops the fake backend and captures the contextual reconnect/disconnect banner for screenshot review.
- Fixed workspace-relative Markdown image links in assistant transcript text so they are rewritten through the safe session raw-file endpoint instead of rendering as broken browser-relative images; bare local image paths still render artifact cards.
- Reduced transcript image layout jitter by reserving stable image/card geometry for Markdown, rendered inline, and artifact images; artifact thumbnails keep a fixed thumbnail height until explicitly expanded.
- Fixed the new/empty-session composer layout regression seen during dogfooding: the prompt textarea can no longer be vertically resized enough to squeeze the empty transcript, and the send/follow-up controls stay bottom-aligned instead of stretching to the textarea height.
- Fixed the real empty-session layout regression behind that symptom: optional connection/attention banners now occupy explicit grid rows so the transcript remains the flexible row and the footer stays compact; hidden idle controls are truly hidden, prompt width no longer overlaps controls, and a focused `empty-session-layout` harness scenario guards the layout.
- Added LAN dev entry points: `dev:server:lan` binds the API on `0.0.0.0`, `dev:web:lan` binds Vite on `0.0.0.0`, custom hostnames can be allowed with `PI_WEB_VITE_ALLOWED_HOSTS`, the browser default API base now follows the page hostname, and `docs/local-network.md` documents token-protected LAN setup. The single-command `bun run dev:lan` now restarts the managed backend in LAN mode and starts the LAN Vite frontend.
- Added first remote-shaped screenshot artifact support: transcript image detection now recognizes absolute workspace paths and `file://` Markdown image URLs, the browser rewrites only paths under the selected session workspace to the existing safe raw-file endpoint, and a new `remote-image-artifact-paths` fake-agent harness scenario covers absolute + `file://` references without direct browser `file://` loads. The UI harness also clears inherited `PI_WEB_AUTH_TOKEN` so local auth env does not break isolated fake-agent runs.
- Added the first explicit remote screenshot artifact upload bridge: shared protocol schemas, `POST /api/sessions/:id/artifacts` for base64 image uploads, `GET /api/sessions/:id/artifacts/raw?path=...` for stable per-session previews from `PI_WEB_ARTIFACT_DIR`, browser fallback from remote absolute/`file://` paths to the artifact endpoint, and a `remote-image-artifact-upload` fake-agent harness scenario that pre-uploads a remote-only screenshot path and verifies transcript previews use `/artifacts/raw`.
- Added a practical remote-side upload helper, `bun run artifact:upload -- --session <id> --api <url> --path <remote-path> <image-file>`, plus `docs/remote-artifacts.md`; the harness now uses that CLI in `remote-image-artifact-upload` so the operator path is covered, not just the HTTP endpoint.
- Spiked/fixed the screenshot jitter/404 loop: failed transcript images now notify the app, failed URLs are suppressed for a short TTL and clear the rendered-segment cache, local/`file://` Markdown images no longer fall back to direct browser `file://`/relative loads when the safe resolver rejects them, and a `missing-remote-image-artifact` harness scenario guards against repeated failed-image rerenders.
- Composer image intake now has one automatic behavior instead of a Prompt/Artifact mode toggle: drag, paste, or paperclip always attaches supported images to the outgoing prompt so the agent receives image bytes, and also uploads session-scoped transcript artifact previews when a session is open, inserting `.bakery/artifacts/...` paths that render through `/artifacts/raw`. The `image-artifact-drop-upload` harness covers both the user-message image payload and artifact preview while `image-attachments` guards the paperclip path. Follow-up hardening made the drop zone accept file drags even when the browser/OS does not expose `image/*` during `dragover`, snapshots the live `FileList` before the file input is cleared, infers image MIME types from common image extensions when needed, renders prompt thumbnails before artifact upload work, surfaces visible attachment/upload failure notices instead of silently doing nothing, and switched the harness to Playwright's real file chooser path with an artificial picker delay plus a forced app rerender while the chooser is open. The paperclip uses a transient per-click file input outside the rerendered app template, so unrelated rerenders cannot replace the input that owns the native picker selection; the temporary Image debug readout was removed after real-browser diagnostics identified and fixed the `crypto.randomUUID` compatibility issue. The Fastify body limit now matches the intended artifact upload path, avoiding noisy `413 FST_ERR_CTP_BODY_TOO_LARGE` notices for normal screenshot-size base64 JSON uploads.
- Added a focused long tool/image-heavy transcript performance spike: the fake-agent runner can emit 96 alternating assistant/tool rows with repeated screenshot paths, `tool-image-heavy-transcript` measures post-growth responsiveness plus render/patch/row-update p95 timings, and collapsed transcript rows now defer expensive body HTML/image DOM until expanded. The focused scenario produced 96 tool rows with 39 visible artifact images, no long tasks, max render 19ms, and post-growth interaction latency under 75ms on the local harness.
- Tested the optimization against the real `Fix Screenshot Attachment Flow` session (`8a356ee5-d7c6-44b9-93d6-505b2634acd2`): 142 transcript rows, 108 tool rows, 102 collapsed rows, 95,636 transcript chars, open/render about 621ms, no long tasks, prompt fill 12ms, but full-app controls still showed cost (`toggleRightPanel` 671ms, `showThinking` 351ms, render p95 314ms). This points to the next full performance spike: decouple chat/transcript rendering from the rest of the app shell so inspector/session chrome/settings changes do not rehydrate or traverse transcript rows.
- Started that shell/transcript decoupling spike by splitting transcript structure dirtiness from full-app renders: completed-tool grouping/removal now rebuilds only the transcript section shell and hydrates row elements instead of replacing the whole app shell, while true snapshot/question/session chrome changes still use full render. The focused `tool-image-heavy-transcript` harness stayed responsive (latest max post-growth action latency 71ms, no long tasks, patch max 10ms). Follow-up fixed the focused `slash-commands` regression by keeping system slash-command result rows open by default instead of collapsing their bodies.
- Added the first mobile layout pass from dogfooding feedback: narrow screens now use a hamburger-triggered session drawer with sessions/new-session before lower-frequency settings, omit the right inspector rail entirely, and hide mobile Details/Preview message actions while keeping desktop inspector behavior and `/tree` drawer support intact. The mobile harness now asserts no side rails, session-first drawer order, inspector detachment, and the compact mobile context-usage pill in the lower composer bar.
- Reworked explicit title/summary suggestion UX into a shared editable model for desktop and mobile: ✨ suggestions now populate editable title/summary fields with per-field ✓ apply and × discard controls; desktop keeps the inline metadata panel while mobile shows the same controls in a trigger-adjacent top popover so generation progress/errors/actions remain visible even though saved summary chrome is hidden on narrow screens. Harness coverage now edits/applies the suggested title on desktop and asserts the mobile popover is visible near the ✨ button.
- Tightened the regular dev-loop validation guidance from iteration telemetry: `bun run report:iteration --recommend ...` now prints a focused-first strategy, stop-on-first-failure copy, and explicit full-suite `RUN`/`ESCALATE ONLY`/`SKIP by default` guidance; `AGENTS.md` and the iteration-observability skill now treat full `bun run test:web-perf` as an escalation instead of the default for every UI-adjacent change.
- Added `docs/codebase-efficiency-audit.md`, a codebase organization and iteration-efficiency spike that captures telemetry-backed hotspots (`apps/web/src/main.ts`, `scripts/ui-harness.ts`, `apps/server/src/index.ts`, validation reruns, and context-heavy logs), operating principles, and ranked multi-session follow-up slices for future refactors.
- Continued the iteration-speed/main-file refactor by extracting session-sidebar recency grouping, persisted group-collapse helpers, and session-card rendering from `apps/web/src/main.ts` into `apps/web/src/session-sidebar.ts`, reducing the main component surface without changing sidebar behavior.
- Fixed a mobile transcript jump/flicker where browser viewport/layout resize scroll events could disable follow-latest without a user scroll gesture, leaving the top of the current chat card clipped with the Jump-to-latest button visible. Transcript auto-follow now only turns off after wheel/touch user intent, and visual viewport resizes keep following the bottom.
- Added compact structured action summaries to `bun run report:iteration --session-context` and `--session-history`: tool call/result totals, validation runs/failures/reruns, edit attempts/failures, unique read/bash input counts, largest result/session sizes, and approximate event-span timing now appear before the detailed metadata-only sections. The iteration-observability skill now points agents at the new summary first while keeping raw prompts/tool outputs out of handoffs.
- Continued the main-file extraction dogfood slice by moving transcript follow/latest scroll state and Jump-to-latest patching into `apps/web/src/transcript-follow.ts`, keeping `main.ts` as the DOM/event coordinator while preserving the existing scroll behavior.
- Continued the small main-file extraction pass by moving running queue types, queue update/add/remove helpers, count predicates, and queued-chip rendering into `apps/web/src/running-queue.ts`; `main.ts` now keeps only WebSocket/composer coordination for queued steers/follow-ups.
- Added `bun run project:notes --handoff`, a read-only line-numbered handoff helper that points agents at the relevant `PROJECT_LOG.md` anchors, latest verification line, recent status bullets, top priorities, and end-of-session checklist without broad log reads.
- Fixed queued follow-up consistency during active runs: follow-up chips that leave the backend/pi queue now remain as dashed `Sending…` pending-transcript chips until the matching user transcript row appears, then the queue DOM is patched away immediately without requiring a full app render. The queued-follow-up fake-agent harness now simulates queue removal before transcript confirmation while also asserting the composer clears immediately, pending UI remains during the gap, and edit/cancel/overflow flows still work. Follow-ups removed the redundant `Session menu hidden/open` notice banner when toggling the hamburger/sidebar; the control itself communicates the state, and made the queued-message section an inline non-overlay panel that defaults collapsed on mobile so it no longer covers chat content.
- Tuned iteration telemetry for the latest 5-session review: `bun run report:iteration --session-context` and `--session-history` now print a `Rerun opportunity` block with repeated validation counts, repeated failed focused-harness counts, and a conservative avoidable-rerun target. Focused failed `ui-harness:*` loops now get stronger guidance to inspect the latest artifact directory/logs, patch one cause, and rerun only that scenario; the iteration-observability skill mirrors that stop/inspect/patch/rerun discipline.
- Fixed sidebar recency ordering when a session is reopened after its last transcript activity: WebSocket/session opens now touch `lastOpenedAt`, the sidebar sorts by the newer of `lastOpenedAt` and `lastActivityAt`, and cards display the same newest recency value so recently accessed sessions jump above older active sessions.
- Fixed duplicate pasted/attached screenshot previews in user transcript rows: the prompt image data URL still renders as the primary inline preview, while the generated `Screenshot artifact: .bakery/artifacts/...` helper path no longer adds a second artifact-card preview for the same prompt attachment.
- Made tool output expansion explicit and deterministic: tool/system rows now use a dedicated chevron button for inline output, row/header selection no longer toggles or auto-expands output, expansion state is owned by the app across rerenders, completed tool groups stay closed unless explicitly opened, and mobile tap targets for tool expansion/actions are larger. Updated the inspector-preview harness to assert header clicks keep completed tool output hidden before using the explicit output toggle.
- Continued the frontend iteration-speed extraction by moving transcript grouping/render-plan helpers, completed-tool group shell rendering, group-position calculation, running-tool adjacency, element ordering, and default row expansion predicates from `apps/web/src/main.ts` into `apps/web/src/transcript-renderer.ts`, with focused unit coverage so `main.ts` keeps DOM coordination while transcript render rules are easier to test.
- Split the monolithic `apps/web/src/styles.css` into ordered CSS modules under `apps/web/src/styles/` (`base`, `sidebar`, `layout`, `composer`, `transcript`, `right-panel`, `metadata`, and `mobile`) while preserving the exact existing cascade; `styles.css` is now only the ordered import list so UI styling changes have smaller edit surfaces.
- Continued composer/UI extraction by moving prompt image attachment helpers, MIME/path normalization, FileReader wrappers, prompt-image rendering, and focused helper tests into `apps/web/src/prompt-images.ts`; `main.ts` now keeps the upload/send coordination while image rules are separately testable.
- Extracted pure-ish composer draft/send helpers into `apps/web/src/composer-actions.ts`: session-scoped prompt draft and attachment warning storage keys, one-shot attachment-warning state, bash prompt parsing, prompt text fallback, WebSocket send payload construction, and queued-message payload shaping now have focused Bun tests while WebSocket sends/DOM binding stay in `main.ts`.
- Completed the planned three-commit `main.ts` extraction burst: transcript DOM row binding/live patch helpers now live in `apps/web/src/transcript-dom.ts`, composer DOM/event wiring and image picker coordination in `apps/web/src/composer-controller.ts`, and agent/session event normalization in `apps/web/src/session-events.ts`. `main.ts` dropped to about 2.5k lines and remains the app-state/WebSocket/render coordinator.
- Continued the open-source-readiness cleanup chain by moving composer image intake/upload/remove coordination, pending-question panel behavior/rendering, sidebar rendering helpers, and WebSocket/reconnect setup into focused web modules with unit coverage; `main.ts` is now about 2.7k lines and more clearly acts as the app-state coordinator.
- Dogfooding bash command blocking showed the idle-only error appeared too far from the text input; composer-scoped send/attachment notices now render inline in the prompt shell instead of as top-of-main app notices.
- Added focused unit coverage for `apps/web/src/session-events.ts`, including agent event type parsing, slash-command results, bash lifecycle normalization, message/tool transcript conversion, queue updates, and metadata update merging.
- Dogfooding bash drafts on mobile showed the composer still looked like normal running input; prompt drafts beginning with `!` now switch the composer label/hint and textarea styling into the local-bash color family, while `!!` no-context drafts and transcript rows use a distinct amber no-context variant.
- Replaced the noisy brand-new-session transcript card (`Session` / `No messages yet.`) with a quiet centered empty-state hint across desktop and mobile, so empty sessions no longer look like selected transcript content before the first prompt. Updated the empty-session and mobile harness waits to assert the new hint.
- Turned that empty-session state into workflow-discovery quick starts for pi browser dogfooding: `/plan`, screenshot, `@file`, and `!bash` chips explain the main Bakery affordances, and the text chips prefill/focus the composer for keyboard flow. Follow-up mobile dogfooding made the quick starts render as compact single-column rows on narrow screens and suppresses stale Jump-to-latest affordances for empty transcripts.
- Consolidated desktop and mobile model/thinking controls into one compact header picker backed by a small `model-thinking-picker` helper module. The trigger shows a shorthand model label plus non-default thinking level, opens separate Model/Thinking selects, respects controller/viewer disabling, and is covered by focused helper tests plus `model-thinking` and `mobile-layout` harness assertions.
- Replaced the experimental iMessage-style transcript bubbles with a ChatGPT-style conversation layout: assistant messages are content-first/plain with always-visible bottom actions, user prompts are compact right-aligned dark chips with hover/focus actions, image attachments render as separate compact cards with full-screen lightbox expansion, and conversation selection no longer draws blue boxes while tool/system/question rows keep their functional card styling.
- Reworked completed/background tool activity into a quieter indented activity-log treatment: tool lifecycle events now carry start/end/duration metadata into transcript items, grouped completed tools show elapsed wall-clock time from the group's first tool start while the agent is running (with a one-second UI tick) instead of summing/reporting only completed tool durations, individual rows show elapsed duration when expanded/collapsed, long tool labels/commands truncate to a bounded width with full titles available via tooltip, completed image-read/image-result tools now group with adjacent background tools instead of surfacing as standalone cards, completed tool groups/rows dropped the card chrome in favor of a thin rail, expanded completed tools inside groups now keep the log header and render only their output as a contained pane, running/failed agent tool rows now use blue/red rails plus contained output panes instead of full colored cards, user-initiated bash rows use a right-aligned rail/output treatment with the rail on the right edge, no redundant badge label, a macOS Terminal-inspired color-scheme output pane shared with agent terminal output, without fake titlebar chrome, ANSI SGR colors are rendered safely in terminal panes, and the existing no-context variant preserved, answered-question summaries now use a matching accent-rail note style instead of blue cards, inline assistant tool-call intent chips are suppressed so the activity log is the source of truth, stale action menus are cleaned during live transcript patches so multiple ellipsis dropdowns cannot remain open, and desktop system cards no longer stretch across the full transcript width while mobile keeps full-width readability.
- Removed noisy success/info app notice banners for screenshot attach/upload, copy actions, tree navigation, session title save/clear, queued-message edit-back, control requests, and sidebar pinning; these paths now rely on the visible UI state change while failures/blockers still surface notices.
- Moved prompt screenshot thumbnails above the composer mode row so adding images no longer separates/pushes the `Prompt` label away from the textarea.
- `/plan` final summaries now use a shared action marker for Accept plan and Back to chat; the transcript hides both the current two-action marker and legacy four-action marker. The web UI replaces the normal composer with a `Plan ready` takeover panel containing only Accept plan and Back to chat: Accept submits the implementation prompt immediately, while Back to chat simply dismisses the takeover and restores the normal composer.
- Added a focused `mobile-long-transcript-controls` harness scenario for the real-phone dogfooding report that long transcripts made button taps feel about a second behind. The scenario grows a 96-tool/39-image mobile transcript, resets app perf counters, then times representative drawer, model/thinking, composer, completed-tool expansion, and action-menu taps; the first local baseline stayed under 160ms max action latency with no long tasks, render max 21ms, and 500 capped row-update samples.
- Removed the full-app render from mobile hamburger drawer open/close: mobile sidebar/backdrop toggles now patch only the drawer DOM/root classes and preserve the existing transcript DOM instead of rebuilding the whole app shell. The mobile long-transcript harness now asserts hamburger open/close produce zero full renders and zero transcript row updates; the latest focused run measured drawer open/close at 34ms/13ms with `hamburgerPerf` `{ renderDelta: 0, patchDelta: 0, rowUpdateDelta: 0 }`.
- Fixed the follow-up regression from that mobile sidebar patch: session-card click handlers now bind as part of sidebar-local event wiring, so a patched mobile drawer can open an existing session. The `mobile-layout` harness now creates a second real session and selects back to the original from the drawer.
- Started the composer/transcript design-language alignment pass: the prompt shell is now a neutral rounded input dock with a quieter inline mode/meta row, compact transcript-aligned action buttons, calmer image thumbnails, and matching desktop/mobile treatment while preserving existing prompt, screenshot, running, autocomplete, and context-usage behavior. Follow-ups renamed Abort to Stop, kept context usage in the top-right mode row with a quieter progress bar, removed the low-value hint copy, swapped prompt-mode controls to SVG icons, centered the composer column while leaving transcript layout unchanged, fixed bash-mode label switching so it updates while typing instead of on blur, removed the old “Pi is working/responding” composer activity field, optimistically swaps to running controls immediately after prompt send, and moved controls into a ChatGPT-like bottom row inside the composer dock with compact tooltipped SVG buttons.
- Started the matching top-bar design-language pass: the header is now a quieter translucent identity strip with title/workspace/details hierarchy, summary/title generation moved behind a metadata-focused Details popover, and mobile keeps the header visible for stability. Follow-up strengthened the Details popover design language by making workspace and summary into rounded section cards with clearer labels, preview text, and calmer empty-summary copy, removed popover/card transparency so transcript content no longer bleeds through the details surface, and made saved summaries show immediately inside Details instead of requiring a second expand click. Model/thinking moved out of the top bar into a brain-icon composer control with the Show thinking preference inside its menu; the explicit Auto-scroll checkbox was removed so follow-latest is inferred from whether the user is at the bottom of the transcript. Follow-up portaled the mobile model popover to a fixed root overlay, anchored it from the tapped button geometry, and added a small pointer toward the composer button, avoiding iOS/grid stacking issues where transcript content could cover the menu. The Jump-to-latest affordance is now an icon-only down-arrow button with a neutral translucent blurred circular surface while keeping unread update counts in aria-label/title text. Running tool output now patches terminal text in place instead of rebuilding the whole row on every partial update, reducing streaming tool-call jitter. The mobile top-bar hide/reveal feature was removed entirely after dogfooding showed transcript height changes during streaming could still cause instability. Jump-to-latest now measures the queued-message strip and floats above it so the two controls no longer overlap. The header status pill now suppresses the low-value idle state and only appears for active/non-ready states; harness readiness waits now read app state instead of depending on a visible idle chip.
- Removed the controller/viewer/client-count label from the top bar. Multi-client presence now appears only in the composer bottom bar as a compact eye-icon viewer count, and the indicator is hidden when there are no additional viewers.
- Compacted the persistent mobile top bar after dogfooding showed it was taking transcript space again: narrow screens now keep only hamburger, editable title, and an icon-only Details affordance in the header; workspace/summary remain available in the Details popover, passive running/status pills are hidden from the mobile header in favor of composer-side state, desktop keeps the text Details button, the header surface/border are more solid to separate it from the transcript, and `mobile-layout` asserts the mobile workspace line/status row are hidden plus the Details control is icon-only.
- Added the first local Bun distribution/onboarding slice: root `README.md` now documents install, `bun run doctor`, local run, LAN run, commands, and core `PI_WEB_*` variables; `bun run doctor` checks Bun/deps, workspace/data dirs, port availability, host/token safety, and prints local/LAN URLs; `docs/local-network.md` now points LAN users through the doctor and the single-command `bun run dev:lan` path.
- Added the first opt-in session isolation slice: the sidebar can create “New isolated session” entries backed by managed Git worktrees under `PI_WEB_WORKTREE_DIR`, with per-session branch/source/base metadata persisted and surfaced in session labels/details. Isolated sessions now also get a compact persistent header chip that opens Details, including mobile coverage that avoids adding a banner or extra status row. Dirty source repositories warn that the worktree starts from `HEAD` and does not copy uncommitted changes.
- Started the sessions/navigation IA split: `/sessions` is now a dedicated find-and-resume page with search across title/summary/prompt/workspace/branch/status, while the sidebar/drawer is reduced to a navigation shell plus workspace/new-session/settings controls. Existing session detail routes remain `/sessions/:id`, and mobile drawer coverage now asserts the drawer does not duplicate the session list. Follow-ups made the sessions page content use one scroll container so the hero/search area scroll away with the session list instead of feeling sticky on mobile while the app top bar stays visible, removed the redundant Chat route button from the sidebar/drawer, and fixed same-session route navigation from `/sessions` back to chat rendering.
- Added the first dedicated `/settings` page and moved low-frequency app controls there: API base, token, theme, and metadata model. The sidebar/drawer now keeps workspace and new-session actions first, then links to Settings below session creation so navigation stays session-first on mobile. Follow-up tightened mobile settings spacing, compacted typography/cards, and fixed input/button box sizing so fields no longer overflow their cards.
- Reduced mobile transcript image jitter/flash during active runs: running completed-tool elapsed ticks now patch only the group summary text instead of rebuilding the transcript DOM every second, and mobile transcript images use reserved compact-card geometry plus async decoding for markdown, prompt-attachment, and artifact previews while preserving tap-to-expand.
- Follow-up mobile image stability fix preserves existing transcript row/image DOM across transcript structure patches and full app renders, skips identical row HTML rebuilds, and adds `mobile-image-stream-stability` harness coverage that holds a loaded mobile transcript image through a streaming multi-tool run.
- Clarified mobile running composer actions: while an agent run is active, the mobile footer now labels the guidance send action as `Guide` and the queued next prompt as `Follow up`, while keeping Stop as the compact square icon; prompt-sending actions now stay disabled/gray until the composer has text or attached images.
- Fixed delayed composer idle/running mode swaps by routing status changes through an immediate composer DOM patch: the footer running class, mode label/classes, textarea placeholder, Send/Guide tooltip/ARIA text, and Follow up/Stop visibility now update without waiting for a delayed streaming transcript patch or a blur/click-away render. `streaming-responsiveness` now asserts the composer DOM is already in running mode when app state becomes running and back in Prompt mode when the run returns idle.
- Added the first live tool activity stack pass: adjacent SDK tool calls now form one open running stack while active or failed, the stack shows only the five most recent tool receipts with older receipts faded upward, running and failed tool outputs stay collapsed by default, failed tools remain in-place with red summary/rail treatment instead of breaking into an expanded error wall, and completed all-success groups settle back to the existing collapsed group behavior. The UI harness settings bootstrap also force-collapses an initially open mobile sidebar overlay so focused scenarios can reliably reach Settings controls.
- Brought the reduced sidebar/drawer closer to the transcript/composer design language with tokenized elevated surfaces, softer overlay depth/backdrop, card-like navigation/workspace sections, updated focus/hover states, pill-like session/status chips, and mobile drawer shadow/radius alignment while preserving the existing navigation/session behavior.
- Added regression coverage for live grouped tool rows so their summary line stays consistent as `Ran N tools` and does not regress to the ambiguous `Tool activity` label while elapsed time/status badges update.
- Refined grouped tool-call display language toward quiet receipts: live groups now lead with the current running tool name plus count/elapsed metadata, mobile/narrow layouts render live groups compact by default while desktop keeps recent receipts open, completed groups show only a minimal count/duration/primary-tool receipt, and failed completed tools no longer promote red summary/error treatment unless the user expands details.
- Stabilized live grouped-tool rendering for performance and consistency: the first running tool now renders through the same live group component, active group ids stay anchored to the first tool instead of changing as tools append, dirty tool groups patch/insert only their local `<details>` region and existing row elements instead of replacing the transcript structure, and the narrow tool-stream harness now tracks render/patch/row-update counts during tool streaming.
- Reworked grouped tool activity into a stable tool-run card on desktop and mobile: the top row shows elapsed/count metadata, the middle row shows the current/last tool, and a bottom-right Details button reveals individual collapsed tool-call rows only on demand. Tool output remains unrendered until both the run card and the individual tool row are expanded, durations use whole seconds with no sub-10s ms/decimal noise, completed groups use stable per-tool duration data when available, and Q&A ask_question tools remain outside tool-run grouping so decision panels stay visible.
- Added `docs/extensions-design.md`, a Bakery extension architecture draft that keeps pi SDK extensions as the agent/runtime layer while defining trusted local Bakery extension points for web slash commands, backend actions, typed browser UI slots, Web Component authoring, reusable UI primitives, `/plan` as the reference extension, testing, reload/dev-loop expectations, and a phased path from bundled extension registry to trusted local extension loading.
- Started the backend open-source-readiness split by extracting artifact/file-preview route handling from `apps/server/src/index.ts` into `apps/server/src/artifact-routes.ts`, keeping upload/raw-preview behavior and workspace containment checks unchanged while shrinking the server entry point.
- Continued the backend open-source-readiness split by moving session metadata generation/update routes, slash-command/file-search routes, session/tree/fork REST routes, and the WebSocket session hub into focused backend modules. `apps/server/src/index.ts` is now about 114 lines and mainly owns app bootstrap, auth/config/settings routes, and route registration while `apps/server/src/session-hub.ts` owns WebSocket lifecycle/controller handling.
- Completed a four-commit behavior-preserving `main.ts` deslop chain: transcript row actions, composer mode/send patching, session shell event binding, and session status/banner rendering now live in focused web modules. `apps/web/src/main.ts` is down to about 2.3k lines and remains the app-state/render coordinator.
- Added the first Preview Stack MVP for isolated sessions: the session Details popover can start/status/stop a fake-agent backend plus Vite frontend from the session Git worktree on temporary ports, render/copy/open the review URL, honor `PI_WEB_PREVIEW_PUBLIC_BASE_URL` for display URLs, and shut preview processes down with the main server.

## How to run

Default local dev loop:

```bash
bun run dev
```

This ensures the backend is running in the background, then starts Vite in the foreground. Restart only the backend without touching Vite/browser state:

```bash
bun run dev:server:restart
bun run dev:server:logs
bun run dev:server:down
```

The older two-terminal foreground loop still works when desired:

```bash
PI_WEB_WORKSPACE_ROOT="$PWD" bun run dev:server
bun run dev:web
```

Open `http://127.0.0.1:5173/`. The API URL should be `http://127.0.0.1:3141`. Leave token blank unless `PI_WEB_AUTH_TOKEN` is set. For first-run/local readiness checks, use `bun run doctor`.

For LAN/Tailscale access, use the LAN IP printed by `bun run doctor --lan` or configure your own DNS/hosts entry, run `PI_WEB_AUTH_TOKEN=... PI_WEB_WORKSPACE_ROOT="$PWD" bun run doctor --lan`, then run `PI_WEB_AUTH_TOKEN=... PI_WEB_WORKSPACE_ROOT="$PWD" bun run dev:lan`. Set `PI_WEB_VITE_ALLOWED_HOSTS` when using custom hostnames. See `docs/local-network.md`.

For containerized Bakery development, copy `.env.example` to `.env`, set `PI_WEB_AUTH_TOKEN` plus host UID/GID on Linux, then run `docker compose up --build`. This runs the existing Bun/Vite dev workflow in Docker with the repo mounted at `/workspace/bakery`; set `PI_WEB_VITE_ALLOWED_HOSTS` and `PI_WEB_PREVIEW_PUBLIC_BASE_URL` in `.env` for LAN/Tailscale hostname access. Use `compose.docker.yaml` explicitly when Docker socket access is needed inside the container. See `docs/container-development.md`.

## Verification

```bash
bun install
bun run check
bun run test:web-perf
bun run ui:manual
curl http://127.0.0.1:3141/healthz
```

Latest: added the first long-transcript performance baseline instead of optimizing blindly. React now exposes bounded transcript perf metrics for snapshot hydration, event application, and transcript commits; the fake runner persists image-heavy bash/tool rows for reopen baselines; and the harness has reusable long-transcript builders plus focused `long-transcript-reopen`, `long-transcript-streaming`, and `mobile-long-transcript-performance` scenarios with loose guardrails. Baseline examples: reopen restored 193 rows/96 tool rows/96 images with prompt usable and bottom gap 0 in 742ms (`test-results/ui-harness/long-transcript-reopen-2026-05-22T17-56-18-033Z/`); live streaming max input/control latency was 122ms (`test-results/ui-harness/long-transcript-streaming-2026-05-22T17-55-56-644Z/`); mobile long-transcript controls max latency was 343ms with no horizontal overflow (`test-results/ui-harness/mobile-long-transcript-performance-2026-05-22T17-56-01-973Z/`). `CONTEXT.md` now defines Long Transcript Performance Baseline. Validation used `bun run report:iteration --recommend CONTEXT.md apps/server/src/fake-runner.ts apps/web/src/components/transcript/TranscriptView.tsx apps/web/src/hooks/useTranscript.ts apps/web/src/lib/transcript-perf.ts scripts/report-iteration.ts scripts/ui-harness/scenarios/helpers.ts scripts/ui-harness/scenarios/long-transcript.ts scripts/ui-harness/scenarios/metadata.ts scripts/ui-harness/scenarios/registry.ts`, `bun run check`, `bun run report:iteration`, `bun run project:notes`, focused harnesses `long-transcript-reopen`, `question-answer` (`test-results/ui-harness/question-answer-2026-05-22T17-54-28-078Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-22T17-54-33-811Z/`), `long-transcript-streaming`, `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-22T17-54-48-747Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-22T17-54-56-326Z/`), selected follow-ups `mobile-long-transcript-performance`, `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-22T17-55-38-240Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-22T17-55-46-589Z/`). A fresh-context reviewer found no blocker; two validation gaps were addressed by recording transcript commits every commit without changing legacy `renderCount`, and by preserving mobile build metrics before resetting for control latency. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused coverage passed. Refresh the browser page for React metric changes; restart the backend/dev server for fake-runner persistence changes.

Previous latest: restored React Plan Card Accept behavior as a direct background submit. Ready Plan Cards now disable Accept when Bakery cannot send, submit `Proceed with the recommended plan.` when available, keep running accepts as follow-ups while idle accepts become normal prompts, and replace Accept/Reject with disabled local `Accepted`/`Rejected` outcome states. `CONTEXT.md` now records that Accept Plan is not an offline draft-preservation control. Validation used `bun run report:iteration --recommend apps/web/src/pages/SessionPage.tsx apps/web/src/components/transcript/TranscriptRow.tsx scripts/ui-harness/scenarios/slash-commands.ts`, `bun run check`, focused harness `bun scripts/ui-harness.ts --scenario sessions-page` (`test-results/ui-harness/sessions-page-2026-05-21T19-41-02-292Z/`), `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-21T19-41-05-945Z/`), `bun scripts/ui-harness.ts --scenario slash-commands` (`test-results/ui-harness/slash-commands-2026-05-21T19-44-53-929Z/`), `bun scripts/ui-harness.ts --scenario session-routing` (`test-results/ui-harness/session-routing-2026-05-21T19-45-01-304Z/`), `bun scripts/ui-harness.ts --scenario narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-21T19-45-05-389Z/`), and `bun scripts/ui-harness.ts --scenario file-autocomplete` (`test-results/ui-harness/file-autocomplete-2026-05-21T19-45-15-817Z/`). A fresh-context reviewer found no blocker, but noted harness gaps for the running-follow-up and initially-disabled unable-to-send paths. Full `bun run test:web-perf` was intentionally skipped because the selector chose focused-first coverage only. Refresh the browser page for the React change; no backend restart is required.

Previous latest: reproduced and fixed the session `56021562-58c5-4d84-a3ac-384ff1db2124` bottom-scroll flashing. The cause was lazy-loaded inline screenshot/artifact previews initially taking zero height, then expanding near the bottom; the bottom sentinel marked follow-latest as paused and showed Jump-to-latest even though the operator had not scrolled away. Markdown images, transcript image segments, and artifact thumbnails now reserve a stable preview height, and transcript resize handling preserves follow-latest when content grows from an already-bottom position. Repro evidence: delayed-image Playwright smoke failed before the fix with bottom gaps up to 2,098px and passed after covering all inline transcript images with `bottom=0`/no jump button/no zero-height images; artifact path `test-results/manual-repro-56021562-all-inline-images-after-fix/`. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx apps/web/src/components/transcript/TranscriptRow.tsx`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-21T19-05-06-933Z/`), `bun scripts/ui-harness.ts --scenario narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-21T19-05-12-276Z/`), `bun scripts/ui-harness.ts --scenario transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-21T19-04-22-928Z/`), and `bun scripts/ui-harness.ts --scenario transcript-text-selection` (`test-results/ui-harness/transcript-text-selection-2026-05-21T19-05-18-096Z/`). Full `bun run test:web-perf` was intentionally skipped because the selector chose focused-first transcript coverage only. Refresh the browser page for the React change; no backend restart is required.

Previous latest: improved the mobile Question Card option flow from real phone feedback. Normal mobile questions now render all inline options above a still-visible composer instead of clipping behind a card scrollbar, while long option lists show the first choices plus an explicit `Show all options` chooser overlay. The question-answer harness now asserts all normal mobile options are visible, verifies the long-list chooser opens and scrolls, and captures the chooser screenshot. Validation used `bun run report:iteration --recommend apps/web/src/components/QuestionPanel.tsx apps/web/src/index.css scripts/ui-harness/scenarios/session.ts`, `bun run check`, focused harness `bun scripts/ui-harness.ts --scenario question-answer` (`test-results/ui-harness/question-answer-2026-05-21T18-36-13-027Z/`), selected `bun scripts/ui-harness.ts --scenario sessions-page` (`test-results/ui-harness/sessions-page-2026-05-21T18-36-21-662Z/`), and a fresh-context reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector chose focused-first coverage only for this UI slice. Refresh the browser page for the React/CSS change; no backend restart is required.

Previous latest: promoted the default-on individual tool cards out of `Experimental*` naming. Components/files are now `BashToolCard`, `ReadToolCard`, `EditToolCard`, and `SearchToolCard`, with stable `tool-card-*` test IDs/classes while preserving the same default Bash/Read/Edit/Write/Search behavior and leaving ToolGroup removed. Validation used `bun run check`, `bun run report:iteration --recommend apps/web/src/components/transcript/BashToolCard.tsx apps/web/src/components/transcript/EditToolCard.tsx apps/web/src/components/transcript/ReadToolCard.tsx apps/web/src/components/transcript/SearchToolCard.tsx apps/web/src/components/transcript/TranscriptRow.tsx scripts/ui-harness/scenarios/transcript.ts apps/server/src/fake-runner.ts PROJECT_LOG.md`, focused tool-card harnesses `bash-tool-card` (`test-results/ui-harness/bash-tool-card-2026-05-18T16-26-28-680Z/`), `read-tool-card` (`test-results/ui-harness/read-tool-card-2026-05-18T16-26-35-027Z/`), `search-tool-card` (`test-results/ui-harness/search-tool-card-2026-05-18T16-26-37-704Z/`), `edit-tool-card` (`test-results/ui-harness/edit-tool-card-2026-05-18T16-26-39-753Z/`), and selected transcript regressions `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-18T16-26-41-903Z/`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-18T16-26-46-964Z/`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after targeted checks passed. Refresh browser for frontend changes.

Previous latest: merged `origin/main` into `feature/tool-ui-agent-elements-spike` while preserving the default-on individual tool cards and dropping the ToolGroup prototype. Main contributes the Settings default-session-model work and `@earendil-works/*` pi package rename; this branch keeps Bash/Read/Edit/Write/Search cards with compact headers, inline duration/count metadata, right-aligned spinner/chevron controls, collapsed outputs, persisted tool duration hydration, and fake-runner focused fixtures. Validation used `bun install` for the renamed dependencies, `bun run check`, `bun run report:iteration --recommend apps/web/src/components/transcript/BashToolCard.tsx apps/web/src/components/transcript/EditToolCard.tsx apps/web/src/components/transcript/ReadToolCard.tsx apps/web/src/components/transcript/SearchToolCard.tsx apps/server/src/fake-runner.ts apps/server/src/session-hub.ts apps/server/src/pi-runner.ts packages/protocol/src/index.ts apps/web/src/components/SettingsDialog.tsx apps/web/src/hooks/useServerConnection.ts`, selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-18T16-19-14-687Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-18T16-19-21-056Z/`), `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-18T16-19-26-761Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-18T16-19-29-099Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-18T16-19-34-884Z/`), and `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-18T16-19-37-567Z/`), plus focused tool-card regressions `bash-tool-card` (`test-results/ui-harness/bash-tool-card-2026-05-18T16-19-57-186Z/`), `read-tool-card` (`test-results/ui-harness/read-tool-card-2026-05-18T16-20-03-635Z/`), `search-tool-card` (`test-results/ui-harness/search-tool-card-2026-05-18T16-20-06-406Z/`), and `edit-tool-card` (`test-results/ui-harness/edit-tool-card-2026-05-18T16-20-08-548Z/`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after targeted checks passed. Refresh browser for frontend changes; restart backend/dev server for merged Settings default model and renamed pi package behavior.

Previous latest (tool-card branch): collapsed Edit/Write compact receipt output behind the same header chevron pattern as Bash/Read/Search. Edit cards show filename/action/duration by default, with `Successfully replaced...` or created-file text available via the top-right chevron; diff output remains intentionally excluded. ToolGroup was later tried and reverted, leaving individual tool cards as the default.

Previous latest (main): Settings default model for new sessions. Added `defaultSessionModel` to app settings/protocol, surfaced it under Settings > Models, included it in `/api/models` fallback defaults, and passed it into brand-new session promotion while preserving explicit draft model overrides and existing session models.


Previous latest: fixed mobile draft-session workspace selection. The header workspace picker is no longer hidden below the `sm` breakpoint when a draft session has no workspace, and the picker menu uses a viewport-capped mobile width so opening it does not create horizontal overflow. `mobile-layout` now creates a no-cwd draft session on a phone viewport, asserts the workspace picker is visible/tappable, captures the open picker, selects the first workspace, and then continues the existing mobile layout coverage. Validation used `bun run report:iteration --recommend apps/web/src/components/Header.tsx scripts/ui-harness/scenarios/mobile.ts`, `bun run check`, and focused harness `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-16T17-58-37-584Z/`, key screenshot `test-results/ui-harness/mobile-layout-2026-05-16T17-58-37-584Z/mobile-draft-workspace-picker.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose focused static validation and the issue-specific mobile harness passed. The browser page should refresh to pick up the React header change; no backend restart is required.

Previous latest: refined transcript timestamp visibility and reload tolerance. User/assistant message timestamps are now hidden until the specific message row or its action row is hovered/focused; user timestamps sit to the left of copy/fork actions, assistant timestamps sit to the right, and hovering tool rows does not reveal assistant timestamps. The transcript reducer also normalizes numeric SDK `message.timestamp` values to ISO strings so reloaded snapshots that still carry numeric inner timestamps can render the hover timestamp without waiting on branch-entry enrichment. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptRow.tsx apps/web/src/lib/transcript.ts apps/web/src/lib/transcript.test.ts apps/web/src/index.css scripts/ui-harness/scenarios/transcript.ts`, `bun test apps/web/src/lib/transcript.test.ts`, `bun run check`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-16T00-47-22-936Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-16T00-47-28-325Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-16T00-47-34-009Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-16T00-47-39-773Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-16T00-47-42-425Z/`), `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-16T00-47-49-770Z/`), and optional `transcript-text-selection` after fixing its hidden-timestamp wait (`test-results/ui-harness/transcript-text-selection-2026-05-16T00-49-05-371Z/`). Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused transcript/lifecycle checks passed. The browser page should refresh to pick up the React/CSS timestamp hover behavior; no backend restart is required for this slice.

Previous latest: switched real pi restored transcript snapshots to the durable `SessionManager` branch timeline. Real pi snapshots now return current-branch `SessionMessageEntry` rows with canonical `entry.timestamp` values instead of using `session.state.messages` as the restored transcript source, so previous sessions should keep visible user/assistant timestamps after reload; non-message branch entries remain deferred, and verified volatile `session.state.messages` tails are appended only when they match the branch prefix to avoid dropping in-progress streaming rows. Validation used `bun run report:iteration --recommend apps/server/src/pi-runner.ts apps/server/src/pi-runner.test.ts apps/web/src/lib/transcript.ts apps/web/src/lib/transcript.test.ts`, `bun test apps/web/src/lib/transcript.test.ts apps/server/src/pi-runner.test.ts`, `bun run check`, focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-16T00-18-56-320Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-16T00-18-58-672Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-16T00-19-03-683Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-16T00-19-09-852Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-16T00-19-15-645Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-16T00-19-19-068Z/`), and optional `transcript-text-selection` (`test-results/ui-harness/transcript-text-selection-2026-05-16T00-19-26-398Z/`), plus two reviewer passes. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused transcript/lifecycle checks passed. The browser page should refresh, and the backend/dev server must restart to load the real pi snapshot timeline change.

Previous latest: fixed real pi transcript timestamp data. Real pi snapshots now enrich timestamp-less `session.state.messages` with persisted current-branch `SessionManager` message-entry timestamps only when the mapping is unambiguous, so existing sessions can show the already-rendered user/assistant `<time>` labels without inventing approximate times; live message events also use the server wrapper `eventTime` when the message payload lacks its own timestamp. Validation used `bun run report:iteration --recommend apps/server/src/pi-runner.ts apps/server/src/pi-runner.test.ts apps/web/src/lib/transcript.ts apps/web/src/lib/transcript.test.ts`, `bun test apps/web/src/lib/transcript.test.ts apps/server/src/pi-runner.test.ts`, `bun run check`, focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-15T23-24-25-820Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-15T23-24-28-066Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-15T23-24-33-171Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-15T23-24-39-260Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-15T23-24-44-930Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-15T23-24-48-311Z/`), and optional `transcript-text-selection` (`test-results/ui-harness/transcript-text-selection-2026-05-15T23-26-10-139Z/`), plus a reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused transcript/lifecycle checks passed. The browser page should refresh, and the backend/dev server must restart to load the real pi snapshot enrichment.

Previous latest: added subtle transcript message timestamps. User and assistant transcript rows now preserve `createdAt` from snapshot/live message timestamps and render a local-time `<time>` label beside the existing copy/fork actions; tool rows remain duration/status-focused. Validation used `bun run report:iteration --recommend apps/web/src/lib/transcript.ts apps/web/src/components/transcript/TranscriptRow.tsx apps/web/src/lib/transcript.test.ts scripts/ui-harness/scenarios/transcript.ts`, `bun test apps/web/src/lib/transcript.test.ts`, `bun run check`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-15T23-05-10-348Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-15T23-05-15-509Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-15T23-05-21-210Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-15T23-05-26-884Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-15T23-05-29-596Z/`), `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-15T23-05-36-886Z/`), and `transcript-text-selection` (`test-results/ui-harness/transcript-text-selection-2026-05-15T23-09-12-256Z/`), plus a reviewer pass that led to dropping a misleading current-time fallback and adding live-event timestamp coverage. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused transcript/lifecycle checks passed. The already-running browser page should refresh to pick up the React transcript change; no backend restart is required.

Previous latest: fixed existing-session transcript resume position. `TranscriptView` now treats a session switch/open as an initial “go latest” intent, waits until visible rows exist, then scrolls to the bottom across layout frames before handing control back to the normal scroll-protection logic; the focused scroll harness now reopens a previously scrolled-up long session and asserts it lands near the bottom with no Jump-to-latest prompt. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx scripts/ui-harness/scenarios/transcript.ts`, `bun run check`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-15T22-44-27-597Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-15T22-44-33-313Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-15T22-44-39-008Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-15T22-44-44-688Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-15T22-48-05-594Z/`), and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-15T22-45-36-034Z/`), plus a reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused transcript/lifecycle checks passed. The already-running browser page should refresh to pick up the React transcript change; no backend restart is required.

Previous latest: fixed stale selected-session sidebar spinners. The sidebar status helper now lets live selected-session `connectionStatus` win over cached `session.status`, so a stale cached `running` status no longer keeps the row spinner visible after the active run returns idle; non-selected rows still use cached session status. Validation used `bun test apps/web/src/lib/session-utils.test.ts`, `bun run report:iteration --recommend apps/web/src/lib/session-utils.ts apps/web/src/lib/session-utils.test.ts`, `bun run check`, and a reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only. The already-running browser page should refresh to pick up the React helper change; no backend restart is required.

Previous latest: fixed the Accept Plan mode loop. Plan Card acceptance now preloads `Proceed with the recommended plan.` with an explicit Prompt-mode override, so accepting while the composer is still in Plan mode no longer sends `/plan Proceed with the recommended plan.`; the slash-command harness now covers accepting from Plan mode and confirms the composer returns to Prompt. Validation used `bun run report:iteration --recommend apps/web/src/pages/SessionPage.tsx apps/web/src/components/Composer.tsx scripts/ui-harness/scenarios/slash-commands.ts`, `bun run check`, focused harnesses `sessions-page` (`test-results/ui-harness/sessions-page-2026-05-15T22-16-46-374Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-15T22-16-48-543Z/`), `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-15T22-16-51-928Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-15T22-18-04-491Z/`), `session-routing` (`test-results/ui-harness/session-routing-2026-05-15T22-18-12-661Z/`), and `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-15T22-18-14-720Z/`), plus a reviewer pass that led to exporting/using the full `ComposerMode` type. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused composer/session/slash/image checks passed. The already-running browser page should refresh to pick up the React composer change; no backend restart is required.

Previous latest: changed React sidebar recency from access recency to work recency. `lastOpenedAt` is still touched for session restore/internal access metadata, but sidebar cards, recency groups, pinned/workspace ordering, and Session Details now use `lastActivityAt ?? createdAt`; backend session enrichment no longer reports `lastActivityAt = lastOpenedAt` for empty/no-entry sessions. Validation used `bun run report:iteration --recommend apps/web/src/lib/session-utils.ts apps/web/src/components/sidebar/SessionCard.tsx apps/web/src/components/SessionDetailsDialog.tsx apps/server/src/session-routes.ts apps/web/src/lib/session-utils.test.ts`, `bun test apps/web/src/lib/session-utils.test.ts`, `bun run check`, focused harnesses `sessions-page` (`test-results/ui-harness/sessions-page-2026-05-15T22-07-53-662Z/`) and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-15T22-07-55-879Z/`), plus a reviewer pass that led to aligning Session Details fallback. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused/static and sidebar/layout checks passed. The already-running browser page should refresh for the React helper changes, and the backend/dev server must restart to pick up the session-route enrichment change.

Previous latest: fixed the second follow-up container disconnect loop from `npm install -g @howaboua/pi-codex-conversion`. The package needs native builds (`node-pty`, initially failing because `make` was missing) and then crashes Bun on import via an unsupported libuv call, so the container settings overlay now filters `npm:@howaboua/pi-codex-conversion` by default without editing the host `~/.pi/agent/settings.json`. The dev image also includes `build-essential` so other npm pi packages with native build steps have the expected toolchain. The live container overlay was hot-patched and the backend restarted; a direct WebSocket smoke for session `20f2a90b-b519-40f6-96db-70ebcbdde5b6` reached `session_snapshot`. Validation used the earlier `npm install -g @howaboua/pi-codex-conversion` reproduction, `bun run report:iteration --recommend Dockerfile docker/entrypoint.sh docs/container-development.md PROJECT_LOG.md`, `bun run check`, `docker build -t bakery-dev:local .`, and container smoke coverage.

Previous latest: fixed the follow-up container disconnect loop caused by npm global install permissions. After adding `npm`, real pi session connects reached the SDK package installer and failed with `EACCES: permission denied, mkdir '/usr/local/lib/node_modules'`, closing WebSockets in a loop. The container entrypoint/Dockerfile now default `NPM_CONFIG_PREFIX` to writable `/workspace/.bakery-data/npm-global` and put its `bin` directory on PATH. The live container was hot-patched with that prefix and the backend restarted; a direct WebSocket smoke for session `20f2a90b-b519-40f6-96db-70ebcbdde5b6` reached `session_snapshot`. Validation used `bun run report:iteration --recommend Dockerfile docker/entrypoint.sh PROJECT_LOG.md`, `bun run check`, `docker build -t bakery-dev:local .`, and the container smoke from `docs/container-development.md` extended to assert `npm config get prefix`, `npm root -g`, and `npm install -g is-sorted@1.0.5` write into the Bakery data volume.

Previous latest: fixed the live container-backed `agent_error: Failed to run npm root -g: null` startup failure. The Docker dev image now installs `npm`, the in-process runner calls a small PATH repair before creating pi SDK sessions so host/non-login launches can find Homebrew/local package managers, and the currently running dev container was hot-patched with npm plus a backend restart so open browsers should reconnect without a page refresh. Validation used `bun run report:iteration --recommend Dockerfile apps/server/src/package-manager-path.ts apps/server/src/package-manager-path.test.ts apps/server/src/pi-runner.ts`, focused unit tests `bun test apps/server/src/package-manager-path.test.ts apps/server/src/pi-runner.test.ts`, `bun run check`, `docker build -t bakery-dev:local .`, and the container entrypoint/image-package smoke from `docs/container-development.md` with added `command -v npm && npm root -g`. Selector-requested UI harnesses were intentionally skipped because the root cause was a missing container package / SDK startup prerequisite, the backend was restarted successfully, and the full fake-agent suite remained escalation-only.

Previous latest: fixed uploaded attachment visibility for agents. Bakery still stores uploaded images in server artifact storage and shows `.bakery/attachments/...` references in prompt context, but the session hub now resolves those references back to stored image bytes and passes them to the pi SDK prompt/ask/steer/follow-up image payload so model-backed agents do not need filesystem access to `.bakery/attachments`. Prompt attachments are now restricted to SDK-supported PNG/JPEG/GIF/WebP, leaving SVG as an artifact preview format instead of a prompt attachment. Validation used `bun run report:iteration --recommend apps/server/src/session-hub.ts apps/server/src/session-hub.test.ts apps/server/src/artifact-routes.ts`, `bun test apps/server/src/session-hub.test.ts`, `bun run check`, focused image harnesses `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T07-06-34-283Z/`) and `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T07-06-40-034Z/`), plus a reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused server/static and image-intake checks passed. The already-running browser page can keep working for frontend code, but the backend/dev server must restart to load the session-hub and attachment-route changes.

Previous latest: fixed the React duplicate-tab WebSocket disconnect loop. The browser no longer stores per-session WebSocket `clientId`s in shared `localStorage`; the React connection hook keeps server-issued ids in tab-local memory, so separate tabs get separate identities instead of evicting each other as “client reconnected.” The lifecycle harness now relies on that behavior directly by removing its previous `localStorage.removeItem(piWebClientId:...)` workaround before opening viewer/requester pages. Validation used `bun run report:iteration --recommend apps/web/src/hooks/useServerConnection.ts scripts/ui-harness/scenarios/lifecycle.ts`, `bun run check`, focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-10T06-56-24-654Z/`) and `reconnect-draft` (`test-results/ui-harness/reconnect-draft-2026-05-10T06-56-28-284Z/`), a manual two-page Playwright repro against the Tailnet dev URL with a stale shared `piWebClientId` still in localStorage, and a fresh-context reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused lifecycle checks passed. The already-running browser page should refresh to pick up the React hook change; no backend restart is required.

Previous latest: fixed the current React image intake failure where paperclip selection and image paste could silently do nothing before any thumbnail/notice appeared. The composer now keeps a persistent React-owned image file input and resets it after every selection, paste handling distinguishes unsupported image-like clipboard payloads from ordinary text paste, image harness coverage asserts unsupported paste feedback plus paperclip/body-paste/focused-paste attachment paths, and `report:iteration` now selects image-focused scenarios for image-intake files. Validation used `bun run report:iteration --recommend apps/web/src/components/Composer.tsx apps/web/src/lib/prompt-images.ts scripts/ui-harness/scenarios/artifacts.ts scripts/report-iteration.ts`, `bun run check`, `bun run project:notes`, focused harnesses `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T01-55-33-727Z/`), `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T01-55-38-158Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-10T01-55-43-156Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-10T01-55-45-937Z/`), and `image-artifact-drop-upload` (`test-results/ui-harness/image-artifact-drop-upload-2026-05-10T01-55-48-872Z/`), plus a fresh-context reviewer pass. Full `bun run test:web-perf` was intentionally skipped because the selector marked it escalation-only after focused checks passed. Manual headed OS clipboard/native-picker validation still needs operator dogfooding in the already-running browser or `bun run ui:manual`; the changed React/Vite page should be refreshed to pick up the new composer code. Follow-up harness screenshots now capture the transient thumbnail state before removal/send: `image-attachments-thumbnail.png`, `image-attachments-before-send.png`, `image-paste-body-thumbnail.png`, and `image-paste-before-send.png` in the focused image scenario artifact directories. After remote multi-client dogfooding still reported no attachment, the composer now also surfaces picker diagnostics when the native chooser returns focus without a file/change event, when no files are selected, or when remote/mobile clipboard metadata exposes an image-like payload without supported PNG/JPEG/GIF/WebP files. A later pre-React comparison found the React path was still too entangled compared to the old browser-native intake flow, so the current rebuild uses a stable native `<input type="file" accept="image/*">` activated by a label, avoids React state updates before the chooser opens, attaches local thumbnails before background artifact upload, and suppresses stale artifact-path insertion if the attachment batch is removed or sent before upload completion. Follow-up rebuild validation used `bun run report:iteration --recommend apps/web/src/components/Composer.tsx`, `bun run check`, and focused harnesses `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T02-22-51-184Z/`), `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T02-22-54-248Z/`), `image-artifact-drop-upload` (`test-results/ui-harness/image-artifact-drop-upload-2026-05-10T02-22-57-261Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-10T02-22-59-925Z/`), and `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-10T02-23-02-735Z/`), plus reviewer passes; full `bun run test:web-perf` remains escalation-only per the selector. Remote dogfooding still saw picker-open/no-thumbnail/no-notice, so the next diagnostic pass scheduled a no-change notice from pointer/click activation even if browser focus/visibility events do not fire, and treats pasted `text/html` image markup/data URLs as image-like clipboard content. That proved the remote browsers opened the picker but did not deliver files through the hidden/proxy input, so the current fallback makes the toolbar control a plainly visible directly clickable native file input (`#attachImages[type=file]`) instead of a proxy label, transparent overlay, or programmatic click while keeping a small paperclip cue. It also listens to both native `input` and `change` events for browser compatibility. The latest attachment-MVP slice added a session multipart upload route (`POST /api/sessions/:id/attachments`) backed by artifact storage, skips JSON content-type for `FormData`, shows uploaded server-backed thumbnails, inserts `.bakery/attachments/...` references into the draft, and avoids same-millisecond same-name upload collisions with a nonce in stored paths. Validation reran `bun run report:iteration --recommend apps/server/src/artifact-routes.ts apps/server/src/index.ts apps/web/src/components/Composer.tsx apps/web/src/hooks/useServerConnection.ts packages/protocol/src/index.ts scripts/ui-harness/scenarios/artifacts.ts package.json apps/server/package.json bun.lock`, `bun run check`, selected lifecycle harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-10T02-45-35-824Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-10T02-45-37-966Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-10T02-45-43-708Z/`), and focused image harnesses `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T02-46-36-520Z/`), `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T02-46-39-592Z/`), and `image-artifact-drop-upload` (`test-results/ui-harness/image-artifact-drop-upload-2026-05-10T02-46-42-668Z/`), followed by final `bun run check` and a reviewer pass. Full suite remains escalation-only per the selector. Follow-up attachment polish removed the redundant success notice once thumbnails render, keeps raw `.bakery/attachments/...` paths out of the visible draft, injects a compact `Attached files:` context block only when sending, routes drag/drop and pasted image files through the multipart attachment upload path, removes the old local prompt-image/FileReader path from the composer, shrinks uploaded attachment previews into compact filename chips with small thumbnails, returns the composer attachment affordance to a paperclip-only button while keeping the actual file input as the click target, renders sent attachment context as compact transcript chips instead of raw `Attached files:` path text, teaches `report:iteration` to select image/attachment harnesses for `artifact-routes.ts`, and records Attachment as the canonical operator-provided uploaded-file term in `CONTEXT.md`. Validation used `bun run report:iteration --recommend apps/web/src/components/Composer.tsx scripts/ui-harness/scenarios/artifacts.ts`, focused harnesses `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T03-13-51-364Z/`), `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T03-13-58-097Z/`), `image-artifact-drop-upload` (`test-results/ui-harness/image-artifact-drop-upload-2026-05-10T03-19-39-766Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-10T03-19-46-441Z/`), `transcript-text-selection` (`test-results/ui-harness/transcript-text-selection-2026-05-10T03-19-51-978Z/`), selector checks `bun run report:iteration --recommend apps/server/src/artifact-routes.ts scripts/report-iteration.ts` and `bun run report:iteration --recommend CONTEXT.md PROJECT_LOG.md`, and final `bun run check`.

Previous latest: restored the React UI harness suite to green after the rewrite triage. The stack restored the empty-session landing hooks, model/context controls, queue controls, bash/extension transcript hooks, artifact previews/error suppression, lifecycle assertions, mobile/narrow harness coverage, and current metadata/inspector expectations while keeping each fix in its own commit. Validation used `bun run check` repeatedly, focused reruns for every previously failing scenario, and final full-suite `PI_WEB_HARNESS_SERVER_PORT=43250 PI_WEB_HARNESS_WEB_PORT=45250 bun run test:web-perf`, which passed with artifacts at `test-results/ui-harness/all-2026-05-10T01-29-59-454Z/`.

Previous latest: mapped common React rewrite surfaces to focused fake-agent validation in `scripts/report-iteration.ts`, including autocomplete, question cards, transcript rows, session shell/list pages, connection lifecycle, sidebar/layout, and matching harness family files. The selector now round-robins scenario groups so multi-surface changes get one high-signal scenario per surface before the six-scenario cap, and avoids the known-drift queued-follow-up path. While validating, restored stable React/harness hooks for question cards, plan cards, session-list cards, file autocomplete, and current composer controls so the selected scenarios pass against the React app. Validation used `bun run check`, `bun run report:iteration`, `bun run project:notes`, `bun run report:iteration --recommend $(git diff --name-only)`, and focused harnesses `question-answer` (`test-results/ui-harness/question-answer-2026-05-09T23-32-26-383Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-09T23-37-28-999Z/`), `sessions-page` (`test-results/ui-harness/sessions-page-2026-05-09T23-39-45-726Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-09T23-38-46-805Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-09T23-43-52-919Z/`), `file-autocomplete` (`test-results/ui-harness/file-autocomplete-2026-05-09T23-46-47-349Z/`), and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-09T23-38-54-628Z/`). The selector's `## Validation decision` marked full `bun run test:web-perf` as escalation-only, so it was intentionally skipped after focused/static validation passed; `mobile-layout` remains an optional follow-up because that scenario still needs a separate React empty-session layout refresh.

Previous latest: restored `/new` in the React composer as a browser-owned slash command. Exact `/new` now creates and navigates to a fresh session in the current session workspace, invalid `/new ...` shows `Usage: /new` without clearing the draft or changing sessions, and the new session prompt regains focus after navigation. Command autocomplete now works for draft/chat-only sessions by creating a chat-only command handle instead of returning no commands, and the session hub falls back from bundled Bakery extension commands to runner built-in command implementations for web command results such as `/session`. Added a focused `new-session-command` harness scenario plus small stable selector classes used by slash-command coverage. Validation used `bun run check`, `bun scripts/ui-harness.ts --scenario new-session-command` (`test-results/ui-harness/new-session-command-2026-05-09T23-22-11-572Z/`), `bun run report:iteration --recommend apps/web/src/App.tsx apps/web/src/pages/SessionPage.tsx apps/web/src/components/Composer.tsx apps/web/src/components/AutocompletePopup.tsx apps/web/src/components/QuestionPanel.tsx apps/web/src/components/transcript/TranscriptRow.tsx apps/server/src/search-routes.ts apps/server/src/session-hub.ts scripts/ui-harness/scenarios/slash-commands.ts scripts/ui-harness/scenarios/registry.ts scripts/ui-harness/scenarios/metadata.ts`, selected `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-09T23-22-19-587Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-09T23-22-26-753Z/`), and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-09T23-22-33-813Z/`). The selected `controller-handoff-edges` harness still fails before the tested change waiting for `#takeControl`, matching the same known instability from the previous slice (`test-results/ui-harness/controller-handoff-edges-2026-05-09T23-22-37-340Z/`); later selected harnesses and full `bun run test:web-perf` were intentionally not run after that focused failure.

Previous latest: fixed real pi session startup so configured `PI_WEB_DEFAULT_MODEL` is applied to brand-new SDK sessions after `createAgentSession` initializes, preventing the SDK-selected fallback such as GPT 5.1 from replacing the intended Bakery default. The default application is skipped for existing pi session files, restored sessions with messages, unavailable defaults, and defaults excluded by `allowedModels`; pending explicit web model selections still run after promotion and can override the configured default. Validation used `bun test apps/server/src/pi-runner.test.ts`, `bun run report:iteration --recommend apps/server/src/pi-runner.ts apps/server/src/pi-runner.test.ts`, `bun run check`, and focused `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-09T23-05-07-169Z/`). The selected `controller-handoff-edges` harness failed twice before the tested handoff assertion because the requester page never showed `#takeControl`, matching a pre-existing focused-harness instability area and unrelated to this fake-agent-independent real-runner change (`test-results/ui-harness/controller-handoff-edges-2026-05-09T23-05-11-174Z/`, `test-results/ui-harness/controller-handoff-edges-2026-05-09T23-05-36-732Z/`); later selected harnesses and full `bun run test:web-perf` were intentionally not run after that focused failure.

Previous latest: fixed a React transcript reset race where session metadata updates replaced the snapshot wrapper while reusing the old message array, causing live WebSocket rows like the first submitted prompt to disappear until the session was reopened. `useTranscript` now rehydrates only when the active session id or backing snapshot messages array changes. Validation selection used `bun run report:iteration --recommend apps/web/src/hooks/useTranscript.ts` and chose `bun run check` only; full `bun run test:web-perf` was intentionally skipped by the selector for this hook-only change.

Previous latest: added `WEB_OLD_TO_WEB_IMPLEMENTATION.md`, a local priority roadmap for porting useful `apps/web-old` behavior into the React `apps/web` app. It inventories mostly-ported features, then ranks remaining parity slices from P0 correctness/trust through P3 polish with explicit success criteria, target files, difficulty, and validation expectations. Validation selection used `bun run report:iteration --recommend WEB_OLD_TO_WEB_IMPLEMENTATION.md PROJECT_LOG.md` and chose `bun run check` only.

Previous latest: fixed duplicate same-tab WebSocket reconnects being counted as "1 other tab connected" by having `SessionHub` replace an existing socket with the same browser `clientId` and ignore the old close event after replacement. This matches the observed backend logs where one visible browser opened the same session WebSocket twice within milliseconds. Validation used `bun test apps/server/src/session-hub.test.ts`, `bun run report:iteration --recommend apps/server/src/session-hub.ts apps/server/src/session-hub.test.ts`, and `bun run check`; selected UI harnesses remain blocked before scenario execution by stale setup waiting for `#apiBase` at `/settings` (`test-results/ui-harness/subagent-card-reconnect-2026-05-09T03-01-17-361Z/`, same setup failure previously at `test-results/ui-harness/reconnect-controller-2026-05-09T02-54-37-570Z/`).

Previous latest: moved draft-session workspace selection from the composer footer into the top session header subtitle, removed the bottom workspace pill, restored pre-prompt model/context controls from `/api/models`, and fixed pre-start model/thinking changes by keeping them as pending hub settings until the runner is created instead of emitting `session has not been started yet`. Validation used `bun test apps/server/src/session-hub.test.ts apps/server/src/metadata-store.test.ts`, `bun run report:iteration --recommend apps/server/src/session-hub.ts apps/web/src/hooks/useServerConnection.ts apps/server/src/index.ts apps/web/src/lib/model-settings.ts apps/web/src/components/Composer.tsx`, and `bun run check`; selected harnesses are currently blocked before scenario execution by stale setup waiting for `#apiBase` at `/settings` (`test-results/ui-harness/subagent-card-reconnect-2026-05-09T03-01-17-361Z/`, same setup failure previously at `test-results/ui-harness/reconnect-controller-2026-05-09T02-54-37-570Z/`).

Previous latest: tinted the React composer Send/Guide button by active composer mode so `Cmd+.`/`Ctrl+.` mode cycling has a matching button color: default Prompt/Steer stays primary, Ask is emerald, Plan is yellow, and Bash modes are purple. Validation used `bun run report:iteration --recommend apps/web/src/components/Composer.tsx`; the selector chose `bun run check` only and skipped the full fake-agent suite by default. `bun run check` was attempted but is currently blocked by unrelated pre-existing web TypeScript errors in dirty/untracked files such as `apps/web/src/transcript-subagent-card.ts`, `apps/web/src/lib/session-utils.ts`, and `apps/web/src/components/SessionDetailsDialog.tsx`.

Previous latest: tightened the React sidebar New session button spacing by removing the explicit split-button gap and reducing the plus-icon/label gap one step, leaving the divider as the only separation between the primary action and dropdown half. Validation used `bun run report:iteration --recommend apps/web/src/components/sidebar/Sidebar.tsx` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: ported four more high-value `web-old` behaviors into the React app. User transcript rows now resolve session-tree entries and expose icon-only Fork from here actions that create/navigate to forked sessions; Session Details now includes a compact current-path/full-tree view. React loads extension card catalogs/modules and renders extension-card transcript payloads with fallback cards. Transcript markdown now resolves local workspace/artifact image paths through the safe raw endpoints and shows compact image grids for bare local image references. Running steer/follow-up messages now appear in a restrained queued strip with cancel/edit-back affordances, backed by queue update events and optimistic sends. Follow-up type fixes aligned current pinned-session protocol usage and wired the existing pin props/type seams so static validation stays green. Validation used `bun run report:iteration --recommend apps/web/src/lib/session-tree.ts apps/web/src/lib/extension-cards.ts apps/web/src/hooks/useServerConnection.ts apps/web/src/pages/SessionPage.tsx apps/web/src/components/RunningQueueStrip.tsx apps/web/src/components/transcript/TranscriptView.tsx apps/web/src/components/transcript/TranscriptRow.tsx apps/web/src/components/SessionDetailsDialog.tsx apps/web/src/components/sidebar/Sidebar.tsx apps/web/src/components/Layout.tsx apps/web/src/App.tsx apps/web/src/components/ui/command.tsx apps/server/src/metadata-routes.test.ts apps/server/src/session-routes.ts PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: changed React chat transcript copy actions from hidden hover overflow controls to visible ChatGPT-style Copy buttons underneath user, assistant, generating-plan, and Plan Card rows; dense tool/system rows still use the compact overflow menu. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptRow.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: slowed the React Jump to latest custom ease-out timing so the explicit glide feels softer without changing live-follow behavior during streaming. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: replaced the React Jump to latest native smooth scroll plus timer correction with a custom requestAnimationFrame ease-out animation, removing the fast end snap while keeping automatic live-follow scrolling immediate and honoring reduced-motion preferences. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx PROJECT_LOG.md`; the first `bun run check` caught a nullable scroller issue in the animation callback, then `bun run check` passed after capturing a stable scroller reference. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: made the explicit React Jump to latest action smooth while keeping automatic live-follow scrolling immediate. The jump respects reduced-motion preferences, hides the overlay during the smooth glide, and resumes normal follow-latest once the transcript reaches the bottom. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: made the React Jump to latest button harder to lose by moving it into a non-scrolling transcript overlay and backing follow state with a bottom sentinel. The button now appears whenever the transcript bottom is not visible, while unread counts still accumulate only for new output during the paused state. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: tightened React transcript follow semantics so scrolling up immediately pauses follow-latest and shows the Jump to latest button even before new unread output arrives; clicking the button scrolls to the bottom and resumes following subsequent transcript updates. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptView.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: fixed the React Jump to latest follow behavior after dogfooding showed scrolling up could still get yanked to the bottom. The session route now gives transcript scrolling a single owner by removing the outer content scroller, the sessions index owns its own scrolling, and the Jump button overlays the transcript viewport instead of sitting at the bottom of transcript content. Validation used `bun run report:iteration --recommend apps/web/src/components/Layout.tsx apps/web/src/pages/SessionsPage.tsx apps/web/src/components/transcript/TranscriptView.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: reimplemented the first four high-priority `web-old` React gaps in `apps/web`: a header Session Details dialog with workspace/isolation metadata generation and Preview Stack controls, a visible Jump to latest transcript affordance, quiet transcript row copy actions, and a React `/plan` Plan Card with details dialog plus Accept-plan composer prefill. Validation used `bun run report:iteration --recommend apps/web/src/components/SessionDetailsDialog.tsx apps/web/src/components/Header.tsx apps/web/src/components/Layout.tsx apps/web/src/App.tsx apps/web/src/hooks/useServerConnection.ts apps/web/src/components/transcript/TranscriptView.tsx apps/web/src/components/transcript/TranscriptRow.tsx apps/web/src/lib/transcript.ts apps/web/src/pages/SessionPage.tsx apps/web/src/components/Composer.tsx` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: smoothed sidebar startup by passing app bootstrap state into the sidebar and rendering quiet recent-session row skeletons while sessions are still unknown, so the `No recent sessions` empty state only appears after bootstrap completes. The existing deep-session header/page bootstrap behavior remains scoped to session routes. Validation used `bun run report:iteration --recommend apps/web/src/components/sidebar/Sidebar.tsx apps/web/src/components/Layout.tsx apps/web/src/App.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: aligned the Settings dialog chrome with the app sidebar by replacing the remaining `Settings2Icon` uses with the same plain `SettingsIcon` cog in both desktop and mobile dialog headers. Validation used `bun run report:iteration --recommend apps/web/src/components/SettingsDialog.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: added a calm bootstrap path for React deep session reloads. `useServerConnection` now exposes `isBootstrapping`/`bootstrapError`, prefers the route session id during initial data load, and keeps the bootstrap phase active until the first WebSocket `session_snapshot` or a real socket/fetch failure. `/sessions/:id` now renders header/transcript/composer skeletons during that phase instead of flashing `Not connected`, `No sessions yet`, or `No session selected`; invalid route session ids settle into a calm `Session not found.` state after bootstrap. Validation used `bun run report:iteration --recommend apps/web/src/hooks/useServerConnection.ts apps/web/src/App.tsx apps/web/src/components/Layout.tsx apps/web/src/components/Header.tsx apps/web/src/pages/SessionPage.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only. Manual follow-up: open an existing `/sessions/:id`, press `Cmd+R`, and confirm the skeleton holds until the connected session appears; stop the backend and refresh to confirm the real disconnected state still appears after bootstrap; try a missing session id to confirm it says `Session not found.`.

Previous latest: moved the generated chef-hat image from Downloads into the React web app, created cropped app/favicon assets, added a reusable `BrandLogo` tile, and replaced the browser title/favicon with Bakery branding. Follow-ups first fixed the top-left workspace trigger after dogfooding showed it still used the old blue placeholder icon, then simplified that top-left header into a static Bakery identity: no workspace path, no chevron, no dropdown, and no clickable trigger. The duplicate bottom-left Bakery/Local-first footer identity was removed, and Settings now uses the plain cog icon. The logo sits on a warm light tile with theme-aware borders so the black mark remains readable in light and dark mode. Validation used `bun run report:iteration --recommend apps/web/src/components/BrandLogo.tsx apps/web/src/components/sidebar/Sidebar.tsx apps/web/index.html apps/web/src/assets/bakery-logo.png apps/web/public/bakery-logo-96.png`, follow-up `bun run report:iteration --recommend apps/web/src/components/sidebar/Sidebar.tsx apps/web/src/components/BrandLogo.tsx`, follow-up `bun run report:iteration --recommend apps/web/src/components/sidebar/Sidebar.tsx PROJECT_LOG.md`, and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: refined the React composer mode controls after dogfooding. Ask mode now gives the composer shell a green border/background treatment and green mode indicator so it reads as a distinct ask/explain state, Plan is now a first-class mode in the mode menu and cycles after Ask with `Cmd+.`/`Ctrl+.`; sending in Plan mode dispatches the draft through the bundled `/plan` workflow command. Follow-up color mapping now uses Plan = yellow, Ask = green, and Bash/Bash without context = purple across the composer shell, mode indicator, and mode-menu icons. The thinking step slider width fix now reaches the real caller by removing the old `w-[88px]` override in the composer, and the UI harness helpers were nudged toward the current React/sidebar/settings-dialog shape after Playwright exposed stale assumptions. Validation used `bun run report:iteration --recommend apps/web/src/components/Composer.tsx PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only; a focused `model-thinking` harness attempt reached stale custom-element assumptions before the slider assertion, so it needs a separate harness migration pass.

Previous latest: replaced the React `/settings` page with a shadcn `sidebar-13`-style dialog, then tightened the dialog after dogfooding showed redundant section copy. The right pane now shows the selected section title/description once in the header and renders settings controls directly below instead of repeating the section heading inside a nested card. The slice also keeps the existing connection, theme, and session metadata controls, adds the local Base UI dialog primitive, preserves `/settings` as a deep link over the last content route, closes the mobile sidebar drawer before opening the dialog, and fixed two type seams that blocked validation (`renameSession` missing from the web hook handle type and the session PATCH route passing an exact-optional `undefined` title). Validation used `bun run report:iteration --recommend apps/web/src/components/SettingsDialog.tsx apps/web/src/hooks/useServerConnection.ts apps/server/src/session-routes.ts PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: added React session deletion from the sidebar and `/sessions` list, then fixed dogfooding issues where the DELETE request was rejected and a later trash click path could be swallowed. Session rows now show a native always-visible trash button with confirmation, `useServerConnection.deleteSession()` calls `DELETE /api/sessions/:id` and updates local selected/session state, and the shared API helper no longer sends `Content-Type: application/json` for requests without a body, avoiding Fastify's empty-JSON-body 400. Delete failures now alert with the actual client/server error instead of silently leaving the row in place. Follow-ups removed `preventDefault()` from the trash button pointer-down path and added card-level click capture for `[data-delete-session]` so hover opacity or a missed button handler cannot silently eat the delete click. Deleting the open route navigates to the next session or `/sessions`. Validation used `bun run report:iteration --recommend apps/web/src/components/sidebar/SessionCard.tsx apps/web/src/hooks/useServerConnection.ts apps/web/src/App.tsx` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: fixed React sidebar New session/New isolated session navigation so creation returns the new `WebSession` and `App` routes to `/sessions/:id` immediately after the backend creates it. Validation used `bun run report:iteration --recommend apps/web/src/App.tsx apps/web/src/hooks/useServerConnection.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: fixed React controller handoff after dogfooding showed viewer tabs could send and receive repeated `not_controller` server errors with no visible recovery path. The WebSocket hook now stores `controller` snapshots/updates and exposes `takeControl()`, `SessionPage` renders a viewer-mode Take control banner, question answering is controller-gated, and the composer disables send/attach controls while still showing its own Take control button. Validation used `bun run report:iteration --recommend apps/web/src/hooks/useServerConnection.ts apps/web/src/pages/SessionPage.tsx apps/web/src/components/Composer.tsx apps/web/src/App.tsx` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only.

Previous latest: replaced the React sidebar shell with the actual generated shadcn `sidebar-07` primitive family rather than a visual mimic. The slice vendors the real `ui/sidebar.tsx` plus its Base UI support primitives (`input`, `separator`, `skeleton`, `sheet`, `collapsible`, `dropdown-menu`, `avatar`, `use-mobile`), wraps the app shell in `SidebarProvider`/`SidebarInset`, and ports Bakery-specific content into the `sidebar-07` structure: workspace dropdown, navigation group, New/New isolated actions, recent session groups/cards, Settings, footer identity, icon collapse rail, and mobile Sheet drawer. Validation used `bun run report:iteration --recommend apps/web/src/App.tsx apps/web/src/components/Header.tsx apps/web/src/components/Layout.tsx apps/web/src/components/sidebar/Sidebar.tsx apps/web/src/components/sidebar/SessionGroup.tsx apps/web/src/components/sidebar/SessionCard.tsx apps/web/src/components/ui/sidebar.tsx apps/web/src/components/ui/input.tsx apps/web/src/components/ui/separator.tsx apps/web/src/components/ui/skeleton.tsx apps/web/src/components/ui/sheet.tsx apps/web/src/components/ui/collapsible.tsx apps/web/src/components/ui/dropdown-menu.tsx apps/web/src/components/ui/avatar.tsx apps/web/src/components/ui/tooltip.tsx apps/web/src/hooks/use-mobile.ts`, `bun run check`, and a Playwright screenshot smoke against Vite at `test-results/sidebar-07-smoke/sidebar-07-desktop.png`, `test-results/sidebar-07-smoke/sidebar-07-desktop-collapsed.png`, `test-results/sidebar-07-smoke/sidebar-07-mobile.png`, and `test-results/sidebar-07-smoke/sidebar-07-mobile-open.png`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only, with the screenshot smoke added for visual confidence.
Latest: added the first managed workspace expansion slice. The sidebar can persist an existing workspace path, clone a Git repository under an allowed configured workspace root, or create a GitHub repository via backend `GH_TOKEN`/`GITHUB_TOKEN` and clone it into the selected root. Stored workspace entries are persisted in SQLite and listed alongside configured roots, while session cwd authorization remains constrained to backend-configured `PI_WEB_WORKSPACE_ROOT` roots rather than being expanded by the browser. Validation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/server/src/index.ts apps/server/src/workspaces.ts apps/server/src/metadata-store.ts apps/web/src/main.ts apps/web/src/session-shell-events.ts apps/web/src/session-sidebar-controller.ts apps/web/src/styles/sidebar.css`, `bun run check`, focused unit tests `bun test apps/server/src/workspaces.test.ts apps/server/src/metadata-store.test.ts apps/web/src/session-sidebar-controller.test.ts`, and two focused fresh-context reviewer passes for the workspace authorization boundary. Full `bun run test:web-perf` was intentionally skipped because the changed UI has no dedicated harness coverage yet and focused/static validation plus reviewer coverage caught/fixed the main boundary risk.

Previous latest: added trusted-dev Git/GitHub auth opt-ins for the containerized development environment: the dev image now installs GitHub CLI (`gh`), `compose.ssh-auth.example.yaml` forwards an explicit host SSH agent socket, `compose.gh-auth.example.yaml` mounts existing host `gh` auth read-only, and the entrypoint adds the mapped container user to mounted host socket groups for SSH/Docker socket usability. Docs/README now keep the safe default clear while covering SSH-agent, `gh`, token, verification, and troubleshooting paths. Validation used `bun run report:iteration --recommend Dockerfile docker/entrypoint.sh compose.ssh-auth.example.yaml compose.gh-auth.example.yaml .env.example README.md docs/container-development.md`, `bun run check`, `docker compose --env-file .env.example config`, SSH/gh override `docker compose ... config` checks, `docker build -t bakery-dev:local .`, direct image CLI smoke (`gh --version`, `git --version`, `ssh -V`), Docker Desktop SSH socket group smoke (`groups=...,ssh-agent-host`, `ssh-add -l` reached the agent but the Docker Desktop proxy had no identities), entrypoint UID/GID/ownership smoke under `test-results/container-smoke/`, and `docker compose -f compose.yaml -f compose.gh-auth.example.yaml --env-file .env.example run --rm --no-deps bakery-dev bash -lc 'gh --version | head -1; gh auth status || true'` (forwarded host `gh` config, but the host token is currently invalid). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for Docker/docs/container-config changes.

Previous latest: closed the no-token localhost browser drive-by gap by replacing permissive API CORS with an explicit browser-origin policy shared by HTTP and WebSocket routes. No-`Origin` local/CLI requests still use the existing localhost/token checks, default Bakery dev origins remain allowed, custom UI harness/dev origins can use `PI_WEB_ALLOWED_ORIGINS`, and token-protected LAN dev allows same-host Vite/API origins while denying unrelated origins. Validation used `bun test apps/server/src/security-origin.test.ts apps/server/src/session-hub.test.ts`, `bun run report:iteration --recommend apps/server/src/security-origin.ts apps/server/src/security-origin.test.ts apps/server/src/config.ts apps/server/src/index.ts apps/server/src/session-hub.ts scripts/ui-harness.ts`, `bun run check`, and focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-06T04-03-31-819Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-06T04-03-39-423Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-06T04-03-47-639Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-06T04-03-57-913Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-06T04-04-03-929Z/`), and `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-06T04-04-12-031Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first escalation guidance after focused checks passed.

Previous latest: documented Bakery's Git/GitHub auth posture for local vs container development: local Bun piggybacks on host backend-process auth, default Docker Compose does not mount host Git/GitHub credentials, and container auth is a trusted-dev opt-in with SSH agent forwarding recommended before private-key or token mounts. README now points container users at the detailed auth guidance. Validation used `bun run report:iteration --recommend docs/container-development.md README.md PROJECT_LOG.md` and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this docs-only slice.

Previous latest: codified the high-priority cleanup-stack dev-loop learnings in `AGENTS.md` and `.pi/skills/iteration-observability/SKILL.md`: plan the stack once, implement/validate/commit one slice at a time, keep the parent as single writer, use subagents as focused blocker/important review gates, run cheap checks before broad selected harnesses, serialize fixed-port harness scenarios, and run `bun run report:iteration --session-context --brief` near the end of long validation-heavy sessions. Validation used `bun run report:iteration --recommend AGENTS.md .pi/skills/iteration-observability/SKILL.md` and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this process-doc change.

Previous latest: added a first-class latest harness artifact lookup for artifact-first retry loops. `bun run report:iteration --latest-artifact [scenario]` now short-circuits generic report generation without writing `test-results/iteration/iteration-report.json`, accepts raw scenario names plus `ui-harness:<scenario>`/`--scenario <scenario>` shapes, and prints the newest artifact path, status, failure summary, log/screenshot/trace inspection commands, and next action. `--recommend` now includes exact copyable latest-artifact commands for selected focused scenarios, and unknown/missing CLI flags fail early instead of being silently ignored. Added temp-root Bun tests for helper selection/formatting and CLI short-circuit/error behavior. Rollout marker: 2026-05-05T18:46:45Z (implementation commit for this log entry). Pre-rollout evidence from `bun run report:iteration --session-history --days 2 --roi` was 179 sessions, 302 validation runs, 48 failures, 174 reruns, and 39 repeated failed focused-harness runs with 9-20 conservative avoidable reruns; after 7 days or 20+ new sessions, compare normalized repeated failed focused-harness reruns with `bun run report:iteration --session-history --since 2026-05-05T18:46:45Z --latest-sessions 500 --roi` and inspect whether artifact reads precede reruns where telemetry can infer it. Validation so far: `bun test scripts/report-iteration.test.ts`, `bun run report:iteration --latest-artifact slash-commands`, `bun run report:iteration --recommend scripts/report-iteration.ts scripts/report-iteration-artifacts.ts scripts/report-iteration.test.ts AGENTS.md .pi/skills/iteration-observability/SKILL.md PROJECT_LOG.md`, and `bun run report:iteration --recommend apps/web/src/main.ts` focused-advisory smoke. Full `bun run test:web-perf` is intentionally skipped by default for this CLI/docs slice unless final selector/check escalation requires it.

Previous latest: added local-calendar day-window filtering to `bun run report:iteration --session-history` for repeatable today/yesterday optimization reviews. `--days 2` now scans session logs from local midnight yesterday through now using mtime, `--since`/`--until` provide explicit mtime windows, filtering happens before the latest-session cap, and history output/JSON now includes the active filter plus sessions-by-mtime-day counts while still counting all discovered top-level and child session logs. The iteration-observability skill documents `bun run report:iteration --session-history --days 2 --roi` and reinforces metadata-only/context-safe review habits. Validation used `bun run report:iteration --recommend scripts/report-iteration.ts .pi/skills/iteration-observability/SKILL.md PROJECT_LOG.md`, `bun run check`, `bun run report:iteration`, `bun run report:iteration --help`, `bun run report:iteration --session-history --days 2 --roi --output /tmp/iteration-days2.json`, `bun run report:iteration --session-history --since 2026-05-04 --until 2026-05-06 --latest-sessions 500 --output /tmp/iteration-date-window.json`, `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session --output /tmp/iteration-latest10.json`, and an invalid `--days 0` smoke. Full `bun run test:web-perf` was intentionally skipped because the selector chose static/reporting checks only.

Previous latest: hardened the Subagent Card contract after mobile dogfooding showed real execution runs could degrade back into generic `subag...` tool rows. Subagent detection now unwraps same-id merged tool-result raw events so final foreground execution calls keep rendering as standalone cards, while management/list/status calls remain compact; CSS now reapplies the standalone wrapper/body overrides after generic tool-row rules so Subagent Cards avoid rails, nested scrollbars, and inherited max-height. `CONTEXT.md` now records the execution-only, immediate-running-card, concise-final-summary, standalone-layout contract, and the fake-agent `subagent-card` harness asserts non-collapsible standalone chrome, no generic header, no nested body scroll, desktop width cap, overlaid action menu, and mobile standalone rendering. Validation used `bun test apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`, `bun run check`, focused `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-05T16-14-06-071Z/subagent-card.png`, `test-results/ui-harness/subagent-card-2026-05-05T16-14-06-071Z/subagent-card-mobile.png`), and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-05T16-13-20-270Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-05T16-13-25-261Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-05T16-13-30-675Z/`), and `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T16-13-33-328Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first guidance after focused checks passed.

Previous latest: fixed `/plan` Plan Card action state and first-click reliability. Plan Card actions now use a capture-phase delegated click handler so freshly patched inline buttons work on the first tap/click, Accept sends `Proceed with the recommended plan.` once and renders a disabled `Accepted` state, the new small red reject action renders disabled `Rejected` without sending, and normal composer sends while a latest plan is pending mark it `Discussing`. State is local browser UI state only, threaded through preserved transcript rows and the full-plan dialog. Validation used `bun test apps/web/src/transcript.test.ts apps/web/src/ui-action-controller.test.ts`, `bun run check`, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/ui-action-controller.ts apps/web/src/ui-action-controller.test.ts apps/web/src/transcript.ts apps/web/src/transcript-dom.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript.test.ts scripts/ui-harness/scenarios/slash-commands.ts`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-05T15-38-17-911Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T15-38-35-348Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-05T15-38-47-507Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-05-05T15-38-59-924Z/`), and `transcript-scroll-stability` after one flaky first attempt (`test-results/ui-harness/transcript-scroll-stability-2026-05-05T15-40-05-812Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: made `/plan` consume a pi-subagents-style project default planning chain. The plan extension now reads `bakery.plan.defaultChain` from `.pi/settings.json`/user settings, resolves `.pi/chains/dev-loop-plan.chain.md`, embeds a concrete foreground `subagent({ chain: ... })` recipe into the launched planning prompt when `pi-subagents` is available, and falls back with a concise warning only for project-configured defaults that cannot be used. The default chain uses relative `output` paths and explicit no-workspace-handoff-file instructions so pi-subagents writes child outputs under its temporary chain artifact directory instead of dirtying the repo. Validation used focused `bun test apps/server/src/bundled-extensions/plan/plan-chain-config.test.ts`, `bun run report:iteration --recommend apps/server/src/bundled-extensions/plan/plan-chain-config.ts apps/server/src/bundled-extensions/plan/plan-chain-config.test.ts apps/server/src/bundled-extensions/plan/plan-prompt.ts apps/server/src/bundled-extensions/plan/index.ts apps/server/src/extensions.ts apps/server/src/session-hub.ts .pi/settings.json .pi/chains/dev-loop-plan.chain.md`, `bun run check`, and `subagent({ action: "list" })`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: restored row actions on standalone transcript cards after dogfooding showed Subagent Cards had lost the tool-row ellipsis and therefore the `Fork from here` menu item. Standalone cards now render an overlaid message-action menu while preserving their card chrome, so subagent/custom/question cards can still expose copy/fork actions when the session tree maps the event. Validation used focused `bun test apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`, `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/styles/transcript.css`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: fixed sparse `pi-subagents` tool rendering after dogfooding exposed a failed/list-style subagent row rendering as a generic large `Failed` card. Management calls such as `action: "list"` now stay compact/quiet even when details are missing, while real execution calls render the full Subagent Card immediately with fallback rows derived from agent/task/chain/tasks args until rich `details.progress/results` arrive; sparse execution failures now render as failed Subagent Cards. Validation used focused `bun test apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`, `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: restored the quieter collapsed tool-call shape after dogfooding showed the newly visible compact output summaries made tool-heavy turns too busy, then fixed the underlying read/edit live-row regression separately. Collapsed tool rows no longer render output summaries by default, the now-unneeded separator CSS was removed, and same-id sparse final updates now preserve richer existing read/edit titles such as `edit apps/web/src/transcript.ts` while still applying informative final output. Validation used `bun test apps/web/src/transcript-controller.test.ts apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts`, `bun run report:iteration --recommend apps/web/src/transcript-controller.ts apps/web/src/transcript-controller.test.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: fixed collapsed edit/tool receipt separator alignment by adding the same muted dot before a summary when a tool row has no target span. Read rows already had the dot from `.tool-target::before`; edit rows with only action+summary now match that compact rhythm. Validation used `bun run report:iteration --recommend apps/web/src/styles/transcript.css` and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: preserved Details Generated card position across transcript reloads by merging persisted web command results into server snapshots by timestamp instead of appending them after all pi messages. The merge keeps existing append behavior when snapshot timestamps are unavailable and preserves extension-card payloads; focused tests cover interleaving, same-timestamp stability, and fallback behavior. Validation used subagent planning/review, `bun test apps/server/src/session-hub.test.ts`, `bun run report:iteration --recommend apps/server/src/session-hub.ts apps/server/src/session-hub.test.ts`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: restored compact output summaries for collapsed completed tool rows. Expanded `read` rows already showed content, but collapsed receipts could look empty because `compactToolSummary()` was only called for non-tool rows; collapsed tools now render the existing summary span next to their action/target, matching the expected compact read/edit receipt shape without expanding file contents. Validation used focused `bun test apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts`, `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: follow-up fixed read/edit-style tool rows that could still go blank after the same-id `toolResult.toolCallId` merge. The prior duplicate-row cleanup matched remembered behavior where action/result could render as two cards; with one live row, a later blank generic `Tool result: read` message could overwrite richer live output. Same-id generic tool results now merge non-destructively: blank final results preserve the live title/body/segments, while informative final results still update the row without reintroducing duplicates. Validation used `bun run report:iteration --recommend apps/web/src/transcript-controller.ts apps/web/src/transcript-controller.test.ts`, focused `bun test apps/web/src/transcript-controller.test.ts apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`, `bun run check`, and subagent review; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: fixed live running transcript rows that could stay visually empty until refresh when a tool/user stream started with empty text. Running text patching now allows empty-to-non-empty updates when the existing streaming target is present, while mixed/non-pre running tool content uses body/segment keyed rerenders so markdown/image updates do not get stuck behind the text-only fast path. This commit intentionally bundles an uncommitted Subagent Card dogfood polish slice from the same local work session: low-information `pi-subagents` management receipts are suppressed, early non-management subagent calls render as cards, directive/output/session paths are compacted to basenames, agent-specific running fallback copy replaces generic `thinking`, and persisted SDK `toolResult.toolCallId` ids align with live `tool:<id>` rows. Subagent review attributed that bundled slice to the recent Subagent Card dogfooding/session-history context rather than a separate unrelated feature. Validation used `bun test apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts`, `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose focused static checks only.

Previous latest: removed the nested scrollbar from Subagent Cards by exempting `.message.subagent-card-result .message-body` from the generic tool-row scrolling style, so the card wraps/grows like other standalone transcript receipts while the transcript remains the only scroll container. Validation used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/transcript.test.ts scripts/ui-harness/scenarios/transcript.ts`, `bun run check`, and focused `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-05T05-00-33-715Z/subagent-card.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first guidance after focused checks passed.

Previous latest: tightened desktop Subagent Card sizing after dogfooding showed the inherited 820px tool-card width left too much empty space. Desktop subagent tool wrappers now cap at 640px while mobile/narrow layouts keep their existing full-width behavior. Validation used `bun run report:iteration --recommend apps/web/src/styles/transcript.css` and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: made Plan Card Accept submit `Proceed with the recommended plan.` immediately when connected, while preserving the disconnected composer-draft fallback and no-composer-takeover Plan Card model. The slash-command harness now asserts the accepted plan appears as a user message with an empty composer, `CONTEXT.md` reflects the connected-send/fallback behavior, and `AGENTS.md` now includes protected subagent implementation hints for bounded editing/review and validation help. Validation used `bun run report:iteration --recommend apps/web/src/ui-action-controller.ts apps/web/src/transcript.ts scripts/ui-harness/scenarios/slash-commands.ts CONTEXT.md`, `bun run check`, focused `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T04-25-43-557Z/`), `bun run report:iteration --recommend AGENTS.md scripts/assert-workflow-skills.ts`, `bun run check`, and focused `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T04-26-16-102Z/`). Full `bun run test:web-perf` was intentionally skipped per the selectors' focused-first guidance after focused checks passed.

Previous latest: added the first Bakery-owned Subagent Card for existing `pi-subagents` foreground `subagent` tool runs. Live partial results render progress rows from `details.progress`, final results render concise per-agent summaries from `details.results`, the card follows the current Plan/Metadata visual style, and background/async controls remain deferred. `CONTEXT.md` now defines Subagent and Subagent Card; fake-agent/harness coverage emits a deterministic foreground subagent run. Validation used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/session-events.ts apps/web/src/styles/transcript.css apps/server/src/fake-runner.ts scripts/ui-harness/scenarios/transcript.ts scripts/ui-harness/scenarios/index.ts scripts/ui-harness/scenarios/names.ts CONTEXT.md`, focused unit tests (`bun test apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`), `bun run check`, focused `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-05T03-47-11-689Z/`), and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-05T03-47-18-600Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-05T03-47-23-600Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-05T03-47-29-050Z/`), and `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T03-47-31-635Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first guidance after focused checks passed.

Previous latest: fixed Transcript Auto-scroll during long tool-call streams by preserving follow-latest when transcript patches/content growth temporarily leave the scroll container away from bottom without explicit operator scroll intent. The narrow tool-stream harness now asserts that Jump to latest does not appear and the transcript remains pinned during/after many long narrow tool calls; `CONTEXT.md` defines Transcript Auto-scroll as the operator-facing term. Validation used `bun run report:iteration --recommend apps/web/src/transcript-follow.ts scripts/ui-harness/scenarios/transcript.ts CONTEXT.md`, `bun run check`, focused `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-05T03-34-48-394Z/`), and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-05T03-33-19-415Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T03-33-24-367Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-05T03-33-27-007Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-05-05T03-33-29-593Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-05T03-33-32-051Z/`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks for the final changed-file set and the extra focused scroll/tool harnesses passed.

Previous latest: fixed transcript/settings copy buttons in browser contexts where `navigator.clipboard` is unavailable or rejects (for example non-secure/LAN-hosted pages) by routing copy actions through a shared Clipboard API + hidden-textarea fallback helper. Added focused unit coverage for native clipboard, fallback-only, and native-reject fallback paths. Validation used `bun test apps/web/src/clipboard.test.ts`, `bun run report:iteration --recommend apps/web/src/clipboard.ts apps/web/src/clipboard.test.ts apps/web/src/main.ts`, `bun run check`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-05T03-25-06-034Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T03-25-14-645Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-05T03-25-17-395Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-05-05T03-25-20-055Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-05T03-25-22-412Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: added the first Event Fork slice for always-fork rollback/go-to behavior. Fork requests now return a structured `{ session, editorText? }` response, user-message forks copy history before the selected prompt and reopen that text as an editable composer draft, and assistant/tool forks copy history through the selected event. Transcript-to-tree matching now covers assistant and tool rows, assistant rows expose a visible fork icon beside copy when tree-backed, tool rows expose Fork from here in the ellipsis menu, and scheduled transcript follow-scroll now rechecks paused state before snapping to bottom. `CONTEXT.md` defines Event Fork as conversation-history rollback without workspace file restoration. Validation used `bun test apps/server/src/session-routes.test.ts apps/web/src/session-tree.test.ts`, `bun run report:iteration --recommend CONTEXT.md packages/protocol/src/index.ts apps/server/src/session-routes.ts apps/server/src/session-routes.test.ts apps/web/src/session-tree.ts apps/web/src/transcript-row-actions.ts apps/web/src/main.ts apps/web/src/session-tree.test.ts apps/web/src/transcript-follow.ts`, `bun run check`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-05T02-10-42-309Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-05T02-10-47-211Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-05T02-10-49-919Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-05-05T02-10-52-507Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-05T02-10-54-832Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: tuned the generic Assistant Streaming Placeholder to look like a normal assistant message row instead of a Plan Card-style bordered card: the running state is now a subtle inline spinner plus `Pi is responding…`, while `/plan` still keeps its distinct `Generating Plan` card. Validation used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts apps/web/src/styles/transcript.css PROJECT_LOG.md`, focused `bun test apps/web/src/transcript.test.ts`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: hid normal running assistant output behind a generic Assistant Streaming Placeholder (`Pi is responding…`) instead of showing raw pre-render Markdown, while preserving the special `/plan` `Generating Plan` placeholder and final Plan Card behavior. `CONTEXT.md` now defines Assistant Streaming Placeholder. Validation used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css CONTEXT.md`, focused `bun test apps/web/src/transcript.test.ts`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only.

Previous latest: fixed the first real-dogfood regression in terminal Question Cards: composer replies sent while the SDK was still unwinding the `ask_question` turn now abort the checkpointed run before starting the next normal prompt, avoiding `agent_error: Agent is already processing` and stale cancelled receipts. Validation used `bun run check` and focused `question-answer` (`test-results/ui-harness/question-answer-2026-05-03T02-52-54-240Z/`).

Previous latest: hardened Question Card answer submission after real `/plan` dogfooding showed stale duplicate answers could create visible `agent_error: No matching pending question` rows. Pending cards now enter an in-flight `Submitting answer…` disabled state after the first answer send, live-patched pending cards get their answer bindings reliably, fake and real runners ignore stale/mismatched answer payloads as no-ops, and `question-answer` now exercises a duplicate Enter answer path while asserting no red stale-answer error appears. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/question-panel-controller.ts apps/web/src/question-panel-controller.test.ts apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts scripts/ui-harness/scenarios/session.ts apps/web/src/transcript.ts`, `bun run check`, focused unit tests (`bun test apps/web/src/question-panel-controller.test.ts apps/web/src/transcript.test.ts`), and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-02T22-23-36-188Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-02T22-23-36-208Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-02T22-23-27-441Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-05-02T22-23-46-016Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-02T22-23-48-524Z/`), and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-02T22-23-53-429Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: changed Question Cards toward terminal chat checkpoints after `/plan` dogfooding. The `ask_question` tool now surfaces a question checkpoint instead of waiting on an embedded custom-answer form, pending questions no longer block the normal composer, option taps send a normal prompt, freeform replies stay in the composer with normal attachment affordances, and mobile harness coverage now asserts tap-first options with hidden shortcut numbers. `CONTEXT.md` now defines Question Card as a terminal assistant checkpoint rather than a blocking transcript form. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/question-panel-controller.ts apps/web/src/transcript.ts apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts CONTEXT.md`, `bun run check`, and focused harnesses `question-answer` (`test-results/ui-harness/question-answer-2026-05-03T01-54-30-561Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-03T01-54-40-512Z/`), `slash-commands` after updating the plan-workflow snippet assertion (`test-results/ui-harness/slash-commands-2026-05-03T01-55-03-345Z/`), and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-03T01-55-18-599Z/`). The remaining selector scenarios and full `bun run test:web-perf` were intentionally skipped per focused-first escalation guidance after the direct question/lifecycle/slash checks passed.

Previous latest: dogfooded the first configured-path Dynamic Bakery Extension slice. `PI_WEB_EXTENSION_PATHS` extensions now go through stricter manifest/contribution validation, reject unsafe custom-element tags and duplicate command/card identifiers, keep bundled extensions alive when configured paths fail, serve web modules from the active registry after reload, and `/reload` refreshes Bakery extension manifests in fake and real session runners while reporting loaded counts/issues. Added a focused `configured-extension-smoke` UI harness scenario that creates a temp local extension, verifies slash autocomplete, command execution, Web Component custom-card rendering, missing-path issue reporting, and post-reload command availability. Validation used `bun run report:iteration --recommend apps/server/src/extensions.ts apps/server/src/extensions.test.ts apps/server/src/index.ts apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts scripts/ui-harness.ts scripts/ui-harness/scenarios/slash-commands.ts scripts/ui-harness/scenarios/index.ts scripts/ui-harness/scenarios/names.ts docs/extensions-design.md PROJECT_LOG.md`, `bun run check`, focused extension unit tests (`bun test apps/server/src/extensions.test.ts apps/server/src/bundled-extensions/bakery/index.test.ts`), the new extension smoke (`bun scripts/ui-harness.ts --scenario configured-extension-smoke`, `test-results/ui-harness/configured-extension-smoke-2026-05-02T22-06-27-491Z/`), and selector-focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-02T22-08-00-593Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-02T22-08-02-702Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-02T22-08-08-636Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-02T22-07-26-977Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-02T22-08-16-229Z/`), and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-02T22-08-21-249Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after selected focused checks passed.

Previous latest: added Playwright Chromium/headless-shell runtime dependencies and fonts to the Bakery dev Docker image so browser harnesses can launch after `bun x playwright install chromium`, and documented a container Playwright launch smoke plus troubleshooting steps for missing host dependencies. Validation used `bun run report:iteration --recommend Dockerfile docs/container-development.md PROJECT_LOG.md` and `bun run check`. `docker build -t bakery-dev:local .` could not run in this environment because the Docker daemon is unavailable (`Cannot connect to the Docker daemon at unix:///var/run/docker.sock`), so image rebuild and the documented Playwright launch smoke remain the manual follow-up. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for Docker/docs changes.

Previous latest: added the first containerized Bakery development environment as a three-commit stack: Docker dev image/entrypoint with host UID/GID mapping and non-root Bun execution, Docker Compose + `.env.example` for the existing backend+Vite dev flow with the repo mounted at `/workspace/bakery`, explicit Docker socket opt-in through `compose.docker.yaml`, and README/container docs. `CONTEXT.md` now defines Containerized Development Environment and distinguishes it from a production runner, multi-backend CLI, and per-session container isolation. Validation used `bun run report:iteration --recommend Dockerfile docker/entrypoint.sh .dockerignore compose.yaml compose.docker.yaml .env.example README.md docs/container-development.md CONTEXT.md`, `docker build -t bakery-dev:local .`, a direct `docker run` UID/GID/ownership smoke under `test-results/container-smoke/`, `docker compose --env-file .env.example config`, `docker compose --env-file .env.example up --build -d` plus `curl -H 'Authorization: Bearer change-me' http://127.0.0.1:3141/healthz` and `curl http://127.0.0.1:5173/`, `docker compose -f compose.yaml -f compose.docker.yaml --env-file .env.example run --rm --no-deps bakery-dev docker version`, and `bun run check` after `bun install` restored host dependencies. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for Docker/docs/context files.

Previous latest: added `/bakery:generate-details` as a bundled Bakery backend-action extension command, then followed up by replacing its plain command receipt with a structured metadata-details card rendered from extension-provided result data rather than parsing text; later visual tunes simplified that card to Plan Card-like density and removed the generic `/bakery:generate-details` command shell so the card stands alone. The command reuses the metadata generator with optional guidance text, auto-applies generated title/summary with `agent` provenance, protects manual fields unless `--replace` is passed, emits compact web-command receipts/card data, and adds a small WebSocket `command` path so invoking it during an active run queues the out-of-transcript metadata command until the turn becomes idle. `CONTEXT.md` now defines Session Metadata Generation and distinguishes it from the broader Details popover. Validation used `bun run report:iteration --recommend apps/server/src/extensions.ts apps/server/src/metadata-routes.ts apps/server/src/session-hub.ts apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts packages/protocol/src/index.ts apps/web/src/main.ts apps/web/src/session-events.ts apps/web/src/session-metadata.ts CONTEXT.md`, focused unit tests `bun test apps/server/src/bundled-extensions/bakery/index.test.ts apps/server/src/metadata-routes.test.ts`, follow-up `bun test apps/web/src/transcript.test.ts`, `bun run check`, and selector-focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-02T01-25-41-128Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-02T01-25-46-426Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-02T01-25-56-255Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-02T01-26-01-176Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-02T01-26-05-146Z/`), and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-02T01-26-11-881Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused lifecycle/slash/stream checks and static follow-up validation passed.

Previous latest: replaced the `/plan` composer takeover with a transcript Plan Card. The new marker is `Plan actions: Accept plan` with legacy marker compatibility; `/plan` now asks for structured final sections so the card can show summary/next-slice/files/validation, clicking the card opens a fullscreen rendered markdown plan, and Accept plan fills the normal composer with `Proceed with the recommended plan.` instead of submitting. `CONTEXT.md` now defines Plan Card as not a composer takeover. Validation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/server/src/bundled-extensions/plan/plan-prompt.ts apps/server/src/fake-runner.ts apps/web/src/transcript.ts apps/web/src/transcript-shell.ts apps/web/src/ui-action-controller.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript.test.ts scripts/ui-harness/scenarios/slash-commands.ts scripts/assert-workflow-skills.ts CONTEXT.md`, `bun test apps/web/src/transcript.test.ts`, `bun run check`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-02T01-09-05-129Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-02T01-09-10-415Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-02T01-09-15-653Z/`), and `slash-commands` after one harness-selector fix (`test-results/ui-harness/slash-commands-2026-05-02T01-09-55-810Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused transcript/workflow checks passed.

Previous latest: added the Preview Stack MVP for isolated worktree sessions. Backend preview-stack routes manage an in-memory fake-agent backend+Vite process pair per isolated session, allocate temporary localhost ports, expose start/status/stop, and stop child processes during server shutdown; the web Details popover now shows Preview Stack state, URL/open/copy controls, logs path, mode/ports, and start/stop/refresh actions. `VITE_PI_WEB_API_BASE` lets each preview frontend point at its own preview backend, while `PI_WEB_PREVIEW_PUBLIC_BASE_URL` rewrites only the displayed/copied URL host for Tailscale-style review. `CONTEXT.md` now defines Preview Stack. Validation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/server/src/config.ts apps/server/src/preview-stacks.ts apps/server/src/preview-stack-routes.ts apps/server/src/index.ts apps/web/src/main.ts apps/web/src/settings-panel.ts apps/web/src/styles/layout.css CONTEXT.md`, `bun run check`, selector-focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-02T00-26-05-906Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-02T00-26-08-068Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-02T00-26-13-767Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-02T00-26-16-893Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-02T00-26-18-980Z/`), and `question-answer` (`test-results/ui-harness/question-answer-2026-05-02T00-26-23-770Z/`), plus a direct PreviewStackManager smoke that started/stopped a preview pair with display URL `http://preview.local:<port>` and logs under `test-results/preview-smoke/preview-smoke/preview-stack.log`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused lifecycle/UI checks and the direct preview smoke passed.

Previous latest: added a conservative ROI mode to `bun run report:iteration`: `--roi` now pairs session-history telemetry with commits in the scanned session window, groups commits into heuristic workstreams, maps related focused harness scenarios, and prints conservative estimates for avoidable focused-harness reruns, raw rerun runtime, broad context reduction, and edit retry failures. Validation used `bun run report:iteration --recommend scripts/report-iteration.ts`, `bun run check`, `bun run report:iteration`, `bun run project:notes`, and `bun run report:iteration --session-history --latest-sessions 30 --exclude-current-session --roi`; full `bun run test:web-perf` was intentionally skipped because the selector marked this as CLI/reporting-only with no browser runtime changes.

Previous latest: refined pending-question option numbers after `/plan` debate: desktop option shortcuts now render as small muted right-side metadata instead of chunky keycaps, the answer label plus `Recommended` badge stay visually primary, and mobile hides per-option shortcut numbers entirely. Validation used `bun run report:iteration --recommend apps/web/src/question-panel-controller.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css apps/web/src/question-panel-controller.test.ts scripts/ui-harness/scenarios/session.ts`, `bun test apps/web/src/question-panel-controller.test.ts`, `bun run check`, and focused `question-answer` (`test-results/ui-harness/question-answer-2026-04-30T19-01-30-961Z/question-answer-recommended-option.png`, `test-results/ui-harness/question-answer-2026-04-30T19-01-30-961Z/question-answer-mobile-touch-hint.png`, `test-results/ui-harness/question-answer-2026-04-30T19-01-30-961Z/question-answer.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only and the focused question harness passed.

Previous latest: compacted the desktop pending-question panel after `/plan` debate: desktop now starts custom answers behind a `Custom answer…` toggle, keeps the recommended option focused/marked, shortens option cards, demotes cancel to low-emphasis text, and keeps the same centered composer width while mobile continues showing the full touch-friendly custom input. Validation used `bun run report:iteration --recommend apps/web/src/question-panel-controller.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css scripts/ui-harness/scenarios/session.ts`, `bun test apps/web/src/question-panel-controller.test.ts`, `bun run check`, and focused `question-answer` (`test-results/ui-harness/question-answer-2026-04-30T18-56-25-286Z/question-answer-recommended-option.png`, `test-results/ui-harness/question-answer-2026-04-30T18-56-25-286Z/question-answer-mobile-touch-hint.png`, `test-results/ui-harness/question-answer-2026-04-30T18-56-25-286Z/question-answer.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only and the focused question harness passed.

Previous latest: completed the composer-area visual integration pass from the `/plan` interview: the footer no longer has a hard transcript boundary, the reserved composer area uses a soft fade/ambient wash in both themes, the transcript shell adds a bottom fade so pending questions do not sit on a hard shelf, the prompt card keeps Bakery's centered width while putting controls in a compact bottom-right lane, and the pending `ask_question` panel/UI-action takeover surfaces share the same melted composer zone. The question-answer harness was hardened to assert the durable Q&A summary rows instead of transient live tool rows that disappear after snapshot refresh. Validation used `bun run report:iteration --recommend apps/web/src/styles/layout.css apps/web/src/styles/composer.css apps/web/src/styles/mobile.css scripts/ui-harness/scenarios/session.ts`, `bun run check`, and focused harnesses `empty-session-layout` (`test-results/ui-harness/empty-session-layout-2026-04-30T18-47-45-324Z/empty-session-layout.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-30T18-47-19-618Z/question-answer-light.png`, `test-results/ui-harness/question-answer-2026-04-30T18-47-19-618Z/question-answer.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-30T18-47-46-670Z/transcript-scroll-stability.png`), and `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-30T18-48-00-379Z/mobile-long-transcript-controls.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only and focused visual/layout harnesses passed.

Previous latest: completed the behavior-preserving `/plan` bundled-extension extraction: the canonical prompt and compact launch text now live under `apps/server/src/bundled-extensions/plan/`, `apps/server/src/extensions.ts` registers that bundled extension directly, and `apps/server/src/workflow-skills.ts` remains a compatibility shim for existing imports/tests. `/plan` still reports `source: "skill"`, launches the same prompt, and keeps compact transcript/sidebar snippets. Validation used `bun run report:iteration --recommend apps/server/src/extensions.ts apps/server/src/workflow-skills.ts apps/server/src/bundled-extensions/plan/index.ts apps/server/src/bundled-extensions/plan/plan-prompt.ts scripts/assert-workflow-skills.ts`, `bun run check`, and selector-focused `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-30T18-21-43-082Z/`). Full `bun run test:web-perf` was intentionally skipped because the selector did not choose it for this server-side organization slice and the focused slash-command check passed.

Previous latest: completed the next `/plan` Phase 2 seam by keeping marker-to-typed-action normalization as the persistence bridge while generalizing the web composer takeover path: `apps/web/src/ui-action-controller.ts` now handles typed UI contribution/action ids, `transcript-shell` renders generic `ui-action-*` takeover classes while preserving `plan-*` compatibility selectors, and transcript marker matching is routed through a small contribution matcher registry with `/plan` as the only registered contribution. Validation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/web/src/transcript.ts apps/web/src/transcript-shell.ts apps/web/src/ui-action-controller.ts apps/web/src/main.ts apps/web/src/transcript.test.ts scripts/ui-harness/scenarios/slash-commands.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css`, `bun run check`, selector-focused `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-30T18-14-41-140Z/`), and `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-30T18-16-10-926Z/`). Selector-requested `question-answer` still fails at the existing wait for a visible `.message.tool` containing `Bug fix` after keyboard answer (`test-results/ui-harness/question-answer-2026-04-30T18-14-51-680Z/`), matching the prior known failure outside the touched `/plan` action path; later selector commands and full `bun run test:web-perf` were intentionally not run under focused-first stop-on-failure policy.

Previous latest: continued the `docs/extensions-design.md` extension path with the first protocol-backed inline action slot for `/plan`: `packages/protocol` now exports typed UI action contribution schemas, transcript marker detection normalizes current and legacy `/plan` final markers into a `bakery.workflow.plan.actions` composer takeover contribution, and the existing Accept plan / Back to chat controls render and dispatch through generic `data-ui-action` buttons while preserving `data-plan-action` compatibility for harness selectors. Validation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/web/src/transcript.ts apps/web/src/transcript-shell.ts apps/web/src/main.ts apps/web/src/plan-action-controller.ts apps/web/src/transcript.test.ts scripts/ui-harness/scenarios/slash-commands.ts`, `bun test apps/web/src/transcript.test.ts`, `bun run check`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-30T18-03-25-865Z/`) and `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-30T18-03-32-641Z/`). Selector-requested `question-answer` failed twice at its existing wait for a visible `.message.tool` containing `Bug fix` after the keyboard answer path (`test-results/ui-harness/question-answer-2026-04-30T18-03-36-122Z/`, rerun `test-results/ui-harness/question-answer-2026-04-30T18-03-58-683Z/`); artifacts were inspected and the failure appears outside the touched `/plan` action path, so later selector commands and full `bun run test:web-perf` were intentionally not run under the focused-first stop-on-failure policy.

Previous latest: removed the overbuilt grouped tool-activity card architecture in a four-commit cleanup: transcript rendering now emits flat `pi-transcript-row` shells for running, completed, failed, image, and developer-bash tools; the app no longer tracks grouped activity expansion ids or runs a 250ms grouped receipt patch timer; running tool rows use a simple CSS-only spinner; and the tool-focused UI harnesses now assert row-level behavior instead of wrapper/card selectors. Validation used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-live-controller.ts apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript-renderer.test.ts scripts/ui-harness/scenarios/transcript.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-30T14-24-29-463Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-30T14-24-34-725Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-30T14-24-36-800Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-30T14-24-39-584Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-30T14-24-41-838Z/`), plus tool-specific `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-30T14-24-46-604Z/`) and `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-30T14-25-18-853Z/`) after one alignment fix. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: completed the requested four-commit behavior-preserving `main.ts` deslop chain: `apps/web/src/autocomplete-controller.ts`, `apps/web/src/plan-action-controller.ts`, `apps/web/src/running-queue-controller.ts`, and `apps/web/src/settings-panel.ts` now own file/slash autocomplete orchestration, plan accept/back-to-chat behavior, running queue controls/patching, and settings/session-detail rendering. `apps/web/src/main.ts` dropped to about 2.0k lines. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/autocomplete-controller.ts apps/web/src/plan-action-controller.ts apps/web/src/running-queue-controller.ts apps/web/src/settings-panel.ts`, `bun run check`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T23-43-42-722Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T23-43-47-624Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T23-43-49-695Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T23-43-52-471Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T23-43-54-850Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: completed the agreed behavior-preserving `main.ts` deslop chain across four commits: `apps/web/src/transcript-row-actions.ts`, `apps/web/src/composer-mode-controller.ts`, `apps/web/src/session-shell-events.ts`, and `apps/web/src/session-status-controller.ts` now own transcript action orchestration, composer mode/send patching, shell/sidebar/settings event binding, and status/banner rendering respectively. `apps/web/src/main.ts` dropped to about 2.3k lines. Validation used `bun run report:iteration --recommend ...` for each focused pair, `bun run check`, and selector-focused harnesses after each commit: `streaming-responsiveness`, `slash-commands`, `question-answer`, `inspector-preview`, and `transcript-scroll-stability` (latest artifact sets: `test-results/ui-harness/streaming-responsiveness-2026-04-29T23-03-16-469Z/`, `test-results/ui-harness/slash-commands-2026-04-29T23-03-21-691Z/`, `test-results/ui-harness/question-answer-2026-04-29T23-03-23-764Z/`, `test-results/ui-harness/inspector-preview-2026-04-29T23-03-26-523Z/`, and `test-results/ui-harness/transcript-scroll-stability-2026-04-29T23-03-28-868Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's focused-first escalation guidance after targeted checks passed.

Previous latest: completed the higher-risk backend open-source-readiness split by extracting WebSocket session lifecycle/controller handling from `apps/server/src/index.ts` into `apps/server/src/session-hub.ts`. The new registry owns hub creation, metadata broadcasting, session deletion disposal, `/api/sessions/:id/ws` registration, and shutdown cleanup while preserving existing prompt/bash/controller behavior; `apps/server/src/index.ts` dropped to about 114 lines. Added focused Bun coverage in `apps/server/src/session-hub.test.ts` for `/name` parsing and image data URL boundary parsing. Validation used `bun run report:iteration --recommend apps/server/src/index.ts apps/server/src/session-hub.ts apps/server/src/session-hub.test.ts`, `bun test apps/server/src/session-hub.test.ts`, `bun run check`, and selector-focused lifecycle/slash harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-29T19-07-53-311Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-29T19-08-10-619Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-04-29T19-08-18-099Z/`), and `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T19-08-23-526Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's focused-first guidance after targeted checks passed.

Previous latest: completed an ambitious transcript cleanup chain that moved transcript item state/dirty tracking/tool timing into `apps/web/src/transcript-controller.ts`, agent-event-to-transcript orchestration into `apps/web/src/transcript-event-controller.ts`, transcript-preserving live DOM patch helpers into `apps/web/src/transcript-live-controller.ts`, and empty-transcript/plan-composer transcript shell rendering into `apps/web/src/transcript-shell.ts`. `apps/web/src/main.ts` is down to about 2.45k lines and is more focused on app coordination. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript-controller.ts apps/web/src/transcript-event-controller.ts apps/web/src/transcript-live-controller.ts apps/web/src/transcript-shell.ts apps/web/src/transcript-controller.test.ts`, `bun run check`, `bun test apps/web/src/transcript-controller.test.ts apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T15-58-59-832Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T15-59-04-951Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T15-59-07-019Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T15-59-09-748Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T15-59-12-104Z/`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after targeted checks passed.

Previous latest: UI harness scenario-family split moved shared Playwright helpers into `scripts/ui-harness/scenarios/helpers.ts`, moved scenario bodies from `scripts/ui-harness/scenarios/index.ts` into family modules (`session`, `mobile`, `transcript`, `slash-commands`, `lifecycle`, `artifacts`, and `visual`), and left `index.ts` as the stable dispatcher/threshold surface so `bun scripts/ui-harness.ts --scenario <name>` and `bun run test:web-perf` keep the same CLI shape. Validation used `bun run report:iteration --recommend scripts/ui-harness.ts scripts/ui-harness`, `bun run check`, selector-focused harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T15-06-01-836Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T15-06-48-094Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T15-06-54-231Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T15-07-00-617Z/`), plus direct family smoke coverage for `image-artifact-paths` (`test-results/ui-harness/image-artifact-paths-2026-04-29T15-08-02-546Z/`) and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-29T15-08-06-307Z/`). `bun run test:web-perf` was escalated because the dispatcher split was broad; it stopped at `empty-session-layout` (`test-results/ui-harness/all-2026-04-29T15-07-23-621Z/`) on the existing footer-height assertion (`162px > 125px`), and the focused smoke suite above passed after artifact inspection.

Previous latest: codebase-efficiency spike added `docs/codebase-efficiency-audit.md`, capturing current organization/iteration hotspots, telemetry evidence, operating principles, and ranked follow-up slices for `apps/web/src/main.ts`, `scripts/ui-harness.ts`, `apps/server/src/index.ts`, validation reruns, and extension-shaped work. Validation used `bun run report:iteration --recommend docs/codebase-efficiency-audit.md PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector marked this as docs/project-log only.

Previous latest: tool activity durations now survive a browser refresh in the same browser by caching live tool timing metadata per web session in localStorage keyed by SDK toolCallId, then applying that cache during snapshot hydration where pi's persisted toolResult messages do not carry startedAt/endedAt/durationMs. This preserves completed group duration receipts after refresh without changing backend/session files. Validation used `bun run report:iteration --recommend apps/web/src/main.ts`, `bun run check`, selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T06-07-06-459Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T06-07-11-424Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T06-07-13-491Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T06-07-16-314Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T06-07-18-684Z/`), plus focused `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T06-07-23-472Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: grouped tool activity receipts now avoid changing tool-name previews and show only stable metadata: duration, call count, and failed count when present. This keeps live activity cards predictable while detailed tool names/output remain available after expanding the group. The narrow tool-stream harness was updated to assert the parent activity stays expanded when an individual nested row is collapsed. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T06-02-41-634Z/`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T06-03-16-326Z/`), and selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T06-02-57-054Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T06-03-01-806Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T06-03-03-864Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T06-03-06-682Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T06-03-09-075Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T06-03-13-817Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: fixed expanded tool activity groups so collapsing an individual nested tool row no longer collapses the entire parent activity card on rerender. The app now stores expanded activity group ids and passes them back into transcript rendering, preserving the parent card's expanded DOM/ARIA state across row-level output toggles and other renders. Regression coverage asserts expanded tool activity state survives transcript rerendering. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T05-57-17-092Z/`), and selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-57-19-561Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-57-24-302Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-57-26-365Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T05-57-29-162Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T05-57-31-511Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: grouped tool activity durations now use one consistent wall-clock meaning: running cards tick from the first tool in the activity run, and completed cards freeze at first-tool start to final-tool end. Individual expanded tool rows still keep per-tool durations, while the overarching receipt differs mainly by whether its elapsed time is still incrementing. Regression coverage now asserts live groups no longer reset to the current tool's start and completed groups prefer wall-clock timestamps before duration-sum fallback. Validation used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, plus focused visual harnesses `bun scripts/ui-harness.ts --scenario tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T05-50-32-188Z/`) and `bun scripts/ui-harness.ts --scenario narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T05-50-34-821Z/`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for these files and the extra focused tool harnesses passed.

Previous latest: transcript jitter observability added reasoned browser perf events for render/patch samples, structure patches, running receipt timer patches, render fallbacks, queue-height changes, scroll corrections, and auto-scroll transitions; the UI harness now records event/reason counts plus recent events in `piWebPerf`. The first diagnostic run already points at likely future jitter work: `narrow-tool-stream` recorded 1 structure patch, 2 render fallbacks, 23 scroll corrections, and 6 auto-scroll transitions, with repeated `detached-during-patch` → `scroll-near-bottom` cycles during active tool streaming. Validation used `bun run report:iteration --recommend apps/web/src/utils.ts apps/web/src/main.ts apps/web/src/transcript-follow.ts scripts/ui-harness.ts`, `bun run check`, selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-41-03-914Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-41-33-408Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-41-35-575Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T05-41-38-366Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T05-41-10-853Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T05-41-15-608Z/`), plus diagnostic `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T05-41-40-714Z/tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: tool activity elapsed-time investigation fixed the jarring live receipt jump by making active grouped-tool duration follow the current running tool's `startedAt` instead of the oldest completed tool in the group; the timer patch now reuses the full receipt label from the render model so full/structure renders and 1s interval patches stay consistent, including failed-count metadata. Regression coverage now asserts the old completed + newly running tool case stays at `1s` instead of jumping to the group age, and that full render/timer-patch receipt text matches. Validation used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/main.ts apps/web/src/session-events.ts apps/web/src/transcript-renderer.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-34-19-217Z/`), `bun scripts/ui-harness.ts --scenario slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-34-26-731Z/`), `bun scripts/ui-harness.ts --scenario question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-34-28-789Z/`), `bun scripts/ui-harness.ts --scenario inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T05-34-31-527Z/`), and `bun scripts/ui-harness.ts --scenario transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T05-34-33-886Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: tool-run detail alignment follow-up normalized expanded successful/failed tool receipt header grids so the disclosure chevron, title, overflow menu, and header height stay aligned; the `tool-grouping` harness now records horizontal and vertical toggle-offset alignment for failed rows. Validation used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T05-27-53-280Z/tool-grouping-expanded.png`), `bun scripts/ui-harness.ts --scenario narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T05-28-01-263Z/tool-stream.png`), and selector-requested `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T05-28-15-026Z/mobile-running-composer-controls.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-28-17-473Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-28-19-540Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-28-22-248Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: tool-run card unification used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-renderer.test.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/main.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused tool coverage `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T04-22-35-859Z/tool-stream.png`) and `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T04-22-41-410Z/tool-grouping-expanded.png`), plus selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T04-22-56-257Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T04-23-00-998Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T04-23-25-550Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T04-24-40-260Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T04-24-42-988Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T04-24-47-749Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: adaptive mobile running tool activity used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-renderer.test.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/main.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused tool coverage `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-59-10-504Z/tool-stream.png`), and selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-59-23-538Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-59-28-285Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-59-30-301Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-59-33-090Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-59-35-384Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-59-40-125Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: extension architecture design used `bun run report:iteration --recommend docs/extensions-design.md DESIGN.md` and `bun run check`. The validation selector chose static checks only for this docs/design-only slice; full `bun run test:web-perf` was intentionally skipped because no browser UI/protocol/session lifecycle code changed.

Previous latest: stable live grouped-tool patching used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/main.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-renderer.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-23-08-661Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-23-13-422Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-23-15-496Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-23-18-228Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-23-20-516Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-23-25-246Z/mobile-running-composer-controls.png`), plus tool-specific `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-22-56-855Z/narrow-tool-stream.png`, with `toolStreamPerf` render/patch/row-update counts) and `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T03-23-27-722Z/tool-grouping-expanded.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: quiet adaptive grouped tool-call display used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript-renderer.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, selector-requested harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-11-52-879Z/mobile-running-composer-controls.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-11-55-435Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-11-57-601Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-12-01-525Z/`), plus tool-specific `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T03-11-40-291Z/tool-grouping-expanded.png`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-11-42-596Z/narrow-tool-stream.png`) coverage. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: delayed composer mode-switch fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-01-12-740Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-01-22-319Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-01-24-506Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-01-28-475Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-01-30-760Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-01-35-515Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: live tool-group label regression coverage used `bun run report:iteration --recommend apps/web/src/transcript-renderer.test.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this test-only follow-up.

Previous latest: sidebar visual-language pass used `bun run report:iteration --recommend apps/web/src/styles/sidebar.css apps/web/src/styles/mobile.css apps/web/src/main.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T02-44-01-934Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T02-44-06-892Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T02-44-09-125Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T02-44-13-042Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T02-44-15-518Z/`), plus extra sidebar-adjacent `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T02-44-40-107Z/`), `sessions-page` (`test-results/ui-harness/sessions-page-2026-04-29T02-44-42-692Z/`), and `themes` (`test-results/ui-harness/themes-2026-04-29T02-44-44-001Z/`) coverage. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: `/plan` canonical grill-me prompt refinement used `bun run report:iteration --recommend apps/server/src/workflow-skills.ts scripts/assert-workflow-skills.ts`, `bun run check`, and focused slash-command coverage `bun scripts/ui-harness.ts --scenario slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T02-37-51-948Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: live tool activity stack used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts apps/web/src/styles/transcript.css apps/web/src/main.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused tool/scroll harnesses `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T22-22-16-529Z/`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-28T22-22-05-293Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T22-22-07-880Z/`), and selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T22-22-21-616Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T22-22-26-348Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T22-22-28-691Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T22-22-32-593Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T22-22-35-053Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile initial-sidebar fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T20-33-15-983Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T20-33-20-806Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T20-33-23-048Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T20-33-27-026Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T20-33-29-575Z/`), and updated `mobile-layout` coverage seeded with desktop-open/pinned sidebar persistence (`test-results/ui-harness/mobile-layout-2026-04-28T20-33-07-582Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile image stream-stability follow-up used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, the new targeted `mobile-image-stream-stability` scenario (`test-results/ui-harness/mobile-image-stream-stability-2026-04-28T19-55-02-007Z/mobile-image-stream-stability.png`), and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T19-55-19-329Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T19-55-24-169Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T19-55-08-659Z/` after fixing focus preservation), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T19-55-26-402Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T19-55-28-919Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T19-55-33-772Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile settings spacing follow-up used `bun run report:iteration --recommend apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and extra visual confidence via `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T18-11-43-990Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this CSS-only follow-up.

Previous latest: settings-page IA slice used `bun run project:notes`, `/plan` interview, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/router.ts apps/web/src/router.test.ts apps/web/src/styles/layout.css scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T18-07-39-465Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T18-08-30-413Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T18-08-44-840Z/` after one transient first-run miss), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T18-08-51-158Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T18-08-53-706Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T18-09-11-598Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile transcript image jitter slice used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/mobile.css`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-39-14-545Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T17-39-19-793Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-39-21-948Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-39-26-073Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-39-28-568Z/`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected checks passed.

Previous latest: sidebar Chat removal/same-session route follow-up used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-28-37-077Z/`), `slash-commands` after fixing the same-session chat route regression (`test-results/ui-harness/slash-commands-2026-04-28T17-28-42-278Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-28-44-427Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-28-48-519Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-28-51-031Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T17-28-55-764Z/mobile-layout.png`), plus extra `sessions-page` coverage (`test-results/ui-harness/sessions-page-2026-04-28T17-28-58-127Z/sessions-page.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected checks passed.

Previous latest: sessions page scroll follow-up used `bun run report:iteration --recommend apps/web/src/styles/layout.css`, `bun run check`, and focused `sessions-page` harness (`test-results/ui-harness/sessions-page-2026-04-28T17-23-48-709Z/sessions-page.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for the CSS-only change; the focused sessions-page harness was run additionally for visual coverage. Follow-up corrected the scroll container so the top app bar remains visible while only the sessions content scrolls.

Latest: composer send-availability follow-up keeps Send/Guide/Follow up disabled/gray until the composer has text or attached images. Validation reused `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-49-23-683Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T17-49-34-701Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-49-36-858Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-49-40-977Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-49-43-462Z/`), and `mobile-layout` with disabled/enabled composer assertions (`test-results/ui-harness/mobile-layout-2026-04-28T17-49-48-230Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile running composer button-label slice used `bun run project:notes`, `/plan` interview, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-45-30-249Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T17-45-37-301Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-45-39-451Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-45-43-573Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-45-46-078Z/`), and `mobile-layout` with new running-control label assertions and screenshot (`test-results/ui-harness/mobile-layout-2026-04-28T17-45-50-834Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: sessions/navigation IA split used `bun run report:iteration --recommend apps/web/src/router.ts apps/web/src/router.test.ts apps/web/src/main.ts apps/web/src/session-sidebar.ts apps/web/src/styles/sidebar.css scripts/ui-harness.ts`, `bun install` after the isolated worktree lacked dependencies, `bun run check`, new focused `sessions-page` (`test-results/ui-harness/sessions-page-2026-04-28T16-54-23-629Z/sessions-page.png`), and selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T16-54-29-140Z/`), `slash-commands` final rerun (`test-results/ui-harness/slash-commands-2026-04-28T16-56-30-047Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T16-56-36-183Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T16-56-40-321Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T16-56-42-827Z/`), and `mobile-layout` final rerun (`test-results/ui-harness/mobile-layout-2026-04-28T16-58-00-860Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: PR screenshot handoff follow-up removed the tracked PR PNG artifacts again to avoid committing transient screenshots to repo history, updated PR #1 back to local screenshot artifact paths, and clarified `AGENTS.md` to list `test-results/...` paths by default unless the operator chooses a GitHub attachment upload flow. Validation used `bun run check`; full `bun run test:web-perf` was intentionally skipped because this was docs/process-only.

Previous latest: PR handoff guidance now asks agents to create/update GitHub PRs with a human-readable title/body derived from the final handoff, including summary, validation, manual test notes, and screenshot artifact paths instead of relying on `gh pr create --fill`. Validation used `bun run check`; full `bun run test:web-perf` was intentionally skipped because this was docs/process-only.

Previous latest: current-dev-loop PR handoff guidance added an `AGENTS.md` end-of-session step for `bakery/session/*` branches with authenticated `gh`: push the isolated branch and create/update a GitHub PR for operator review/merge. Validation used `bun run check`; full `bun run test:web-perf` was intentionally skipped because this was docs/process-only.

Previous latest: isolated-session header cue used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/layout.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T14-52-35-324Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T14-52-40-809Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T14-52-42-924Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T14-52-47-032Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T14-52-49-531Z/`), and `mobile-layout` first as part of the full selected sequence (`test-results/ui-harness/mobile-layout-2026-04-28T14-52-54-269Z/`) then rerun after adding explicit isolated-header screenshot/assertions (`test-results/ui-harness/mobile-layout-2026-04-28T14-53-42-266Z/mobile-isolated-header.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: opt-in Git worktree session isolation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/server/src/config.ts apps/server/src/metadata-store.ts apps/server/src/workspaces.ts apps/server/src/index.ts apps/web/src/main.ts apps/web/src/session-metadata.ts apps/web/src/session-sidebar.ts`, `bun run check`, a temp-repo smoke of `createGitWorktreeSession`, and selector-requested focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-28T14-43-42-137Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-28T14-43-47-667Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-04-28T14-43-54-098Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T14-44-00-124Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T14-44-04-121Z/`), and `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T14-44-11-344Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused lifecycle/protocol checks passed.

Previous latest: local Bun distribution/onboarding slice used `bun run report:iteration --recommend package.json README.md scripts/doctor.ts docs/local-network.md apps/web/vite.config.ts docs/remote-artifacts.md`, `bun run check`, `bun run doctor`, expected-failure `env -u PI_WEB_AUTH_TOKEN -u PI_WEB_HOST PI_WEB_WORKSPACE_ROOT="$PWD" bun run doctor --lan`, and successful `env PI_WEB_AUTH_TOKEN=test PI_WEB_WORKSPACE_ROOT="$PWD" bun run doctor --lan`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for docs/package/script/Vite config changes with no browser UI/protocol/session lifecycle impact. Public docs/config now use generic LAN IP/custom-host examples instead of machine-local domain names.

Previous latest: pending question UI modernization used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T14-17-21-856Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T14-17-28-888Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T14-17-32-595Z/question-answer.png`, `test-results/ui-harness/question-answer-2026-04-28T14-17-32-595Z/question-answer-dark.png`, `test-results/ui-harness/question-answer-2026-04-28T14-17-32-595Z/question-answer-mobile-touch-hint.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T14-17-38-658Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T14-17-43-936Z/transcript-scroll-stability.png`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T14-17-50-189Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed. Follow-up real-session dogfooding reported the updated question UI looks good.

Previous latest: mobile top-bar compaction, contrast, and passive-status removal used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css apps/web/src/styles/layout.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T06-12-36-082Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T06-12-41-336Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T06-12-43-525Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T06-12-47-491Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T06-12-49-879Z/transcript-scroll-stability.png`), and `mobile-layout` with new mobile header assertions (`test-results/ui-harness/mobile-layout-2026-04-28T06-12-54-616Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T06-12-54-616Z/mobile-metadata-popover.png`, `test-results/ui-harness/mobile-layout-2026-04-28T06-12-54-616Z/mobile-queued-expanded.png`); the CSS-only contrast follow-up then used `bun run report:iteration --recommend apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and targeted visual `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T06-15-38-599Z/mobile-layout.png`). The passive-status removal used `bun run report:iteration --recommend apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, and focused `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T06-18-15-747Z/mobile-layout.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T06-18-18-546Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T06-18-20-739Z/question-answer.png`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T06-18-24-664Z/streaming-responsiveness.png`); the first `mobile-layout` rerun exposed hidden `.status.running` waits in the harness, so those waits now use app state instead. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: idle-status suppression used `bun run report:iteration --recommend apps/web/src/main.ts` followed by the final `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-55-32-459Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-55-37-715Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-55-39-864Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-55-43-818Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-55-46-311Z/transcript-scroll-stability.png`), and final `mobile-layout` smoke after the status color/harness updates (`test-results/ui-harness/mobile-layout-2026-04-28T05-56-42-399Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T05-56-42-399Z/mobile-queued-expanded.png`). The first `streaming-responsiveness` run failed because the harness still waited for `.status.idle`; harness waits now inspect app idle state instead. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: Jump-to-latest/queue overlap fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/transcript.css`, `bun run check`, targeted `queued-follow-up` (`test-results/ui-harness/queued-follow-up-2026-04-28T05-49-32-808Z/queued-follow-up.png`, `test-results/ui-harness/queued-follow-up-2026-04-28T05-49-32-808Z/queued-follow-up-final.png`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-49-38-962Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T05-49-38-962Z/mobile-queued-expanded.png`), and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-49-49-562Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-49-54-813Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-49-56-954Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-50-00-938Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-49-41-699Z/transcript-scroll-stability.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: remove mobile header auto-hide used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-46-45-390Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-47-00-400Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-47-02-571Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-47-06-498Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-46-50-652Z/transcript-scroll-stability.png`), plus targeted mobile/tool smokes `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-46-36-565Z/mobile-layout.png`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T05-46-39-839Z/tool-stream.png`, `test-results/ui-harness/narrow-tool-stream-2026-04-28T05-46-39-839Z/narrow-tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: mobile header jitter follow-up used `bun run report:iteration --recommend apps/web/src/main.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-44-52-540Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-45-09-132Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-45-11-301Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-45-15-229Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-44-57-816Z/transcript-scroll-stability.png`), plus targeted mobile/tool smokes `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-44-43-679Z/mobile-layout.png`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T05-44-47-003Z/tool-stream.png`, `test-results/ui-harness/narrow-tool-stream-2026-04-28T05-44-47-003Z/narrow-tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: running tool-call jitter follow-up used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript.test.ts`, `bun run check`, focused harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-42-42-043Z/mobile-layout.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-42-45-141Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-42-47-290Z/question-answer-pending.png`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-42-51-248Z/streaming-responsiveness.png`), plus targeted `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T05-42-26-306Z/tool-stream.png`, `test-results/ui-harness/narrow-tool-stream-2026-04-28T05-42-26-306Z/narrow-tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: neutral Jump-to-latest style follow-up used `bun run report:iteration --recommend apps/web/src/styles/transcript.css` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow CSS color tuning.

Previous latest: icon-only Jump-to-latest follow-up used `bun run report:iteration --recommend apps/web/src/transcript-follow.ts apps/web/src/styles/transcript.css` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow affordance markup/CSS change.

Previous latest: mobile model menu anchor follow-up used `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-23-51-262Z/model-thinking-mobile.png`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-24-07-969Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because this was a focused mobile popover placement fix after the broader composer-side model/settings focused suite had passed.

Previous latest: mobile model menu portal fix used `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-22-08-698Z/model-thinking-mobile.png`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-22-21-442Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because this was a focused mobile popover layering/placement fix after the broader composer-side model/settings focused suite had passed.

Previous latest: mobile model menu stacking follow-up raised the open composer/model menu above transcript overlay z-indexes and used `bun run check` plus `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-19-37-723Z/model-thinking-mobile.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow CSS stacking fix after the composer-side model/settings focused suite had passed.

Previous latest: mobile model menu stacking fix used `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-17-23-932Z/mobile-layout.png`), and `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-17-27-185Z/model-thinking-mobile.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow CSS stacking fix after the composer-side model/settings focused suite had passed.

Previous latest: composer-side model/settings first pass used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/model-thinking-picker.ts apps/web/src/model-thinking-picker.test.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun test apps/web/src/model-thinking-picker.test.ts`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-14-13-269Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-14-23-980Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-14-26-122Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-14-30-052Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-14-32-759Z/transcript-scroll-stability.png`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-14-38-162Z/mobile-layout.png`), and `model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-14-40-893Z/model-thinking.png`, `test-results/ui-harness/model-thinking-2026-04-28T05-14-40-893Z/model-thinking-mobile.png`). Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance after focused checks passed.

Previous latest: Details summary reveal follow-up used `bun run check` and targeted visual smoke `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-09-33-485Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow details-popover markup/style follow-up after the previous selector chose static checks for this styling area.

Previous latest: Details popover opacity follow-up used `bun run check` and targeted visual smoke `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-07-38-571Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow CSS-only readability fix after the previous selector chose static checks for the details popover styling slice.

Previous latest: Details popover visual follow-up used `bun run report:iteration --recommend apps/web/src/session-metadata.ts apps/web/src/styles/metadata.css apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and targeted visual smoke `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-06-12-214Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow markup/style follow-up and the targeted mobile visual smoke passed.

Previous latest: top-bar identity redesign used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/layout.css apps/web/src/styles/mobile.css apps/web/src/styles/metadata.css apps/web/src/styles/composer.css`, `bun run check`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-00-51-637Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-00-59-612Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-01-01-761Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-01-05-695Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-01-08-425Z/transcript-scroll-stability.png`), and targeted `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-02-48-292Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T05-02-48-292Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance after focused checks passed. An exploratory `session-metadata` rerun exposed a pre-existing/generic-placeholder harness expectation mismatch after `/name --clear` (`New session` vs workspace fallback) before reaching the updated Details-menu path; it was not included in the selected validation set.

Previous latest: ChatGPT-like composer bottom-row follow-up used `bun run check`, `bun test apps/web/src/composer-controller.test.ts`, focused `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T04-30-30-887Z/streaming-responsiveness.png`) for optimistic running-control swap, and focused `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-34-50-648Z/mobile-layout.png`) for compact in-dock controls. Full `bun run test:web-perf` was intentionally skipped because these were narrow composer markup/style follow-ups and focused checks passed.

Previous latest: centered composer-only follow-up used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css apps/web/src/styles/layout.css apps/web/src/styles/mobile.css apps/web/src/styles/transcript.css PROJECT_LOG.md`, `bun run check`, `bun test apps/web/src/composer-controller.test.ts`, focused visual smokes `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-17-02-764Z/mobile-layout.png`) and `theme-gallery` (`test-results/ui-harness/theme-gallery-2026-04-28T04-17-06-216Z/theme-gallery-dark.png`, `test-results/ui-harness/theme-gallery-2026-04-28T04-17-06-216Z/theme-gallery-light.png`), plus selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T04-17-39-985Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T04-17-45-577Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T04-17-48-091Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T04-17-51-823Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T04-17-54-671Z/transcript-scroll-stability.png`). Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance after focused checks passed; follow-up reverted transcript centering while keeping the new centered composer.

Previous latest: composer compact-controls/context follow-up used `bun run check` and focused visual smoke harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-08-43-014Z/mobile-layout.png`) and `theme-gallery` (`test-results/ui-harness/theme-gallery-2026-04-28T04-08-46-458Z/theme-gallery-dark.png`, `test-results/ui-harness/theme-gallery-2026-04-28T04-08-46-458Z/theme-gallery-light.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow composer style/markup follow-up after the selector chose static checks for the CSS-only slice.

Previous latest: composer design-language pass used `bun run report:iteration --recommend apps/web/src/styles/composer.css apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and visual smoke harnesses `theme-gallery` (`test-results/ui-harness/theme-gallery-2026-04-28T04-03-10-683Z/theme-gallery-dark.png`, `test-results/ui-harness/theme-gallery-2026-04-28T04-03-10-683Z/theme-gallery-light.png`) and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-03-18-357Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this CSS-only slice; the two focused visual harnesses were run for desktop/light/dark and mobile screenshot review.

Previous latest: clean session-route slice used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/router.ts apps/web/src/router.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/router.test.ts`, `bun run check`, and focused harnesses `session-routing` (`test-results/ui-harness/session-routing-2026-04-28T03-56-30-234Z/session-routing.png`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T03-55-51-217Z/streaming-responsiveness.png`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-55-56-586Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: Mobile sidebar session-selection fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-42-24-652Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance; the selector also listed streaming/slash/question/inspector/scroll due to `main.ts` churn, but this bug fix only moves session-card binding into sidebar-local wiring and extends the targeted mobile drawer scenario.

Previous latest: Mobile hamburger local-patch fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-28T03-37-52-080Z/mobile-long-transcript-controls.png`), `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-38-01-685Z/mobile-layout.png`), and `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T03-38-05-996Z/streaming-responsiveness.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed; slash/question/inspector/scroll were not rerun because this slice only changes mobile sidebar toggling plus extracted sidebar binding/rendering, and the targeted mobile/new benchmark plus streaming smoke covered the touched interaction path.

Previous latest: Mobile long-transcript control benchmark used `bun run report:iteration --recommend scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-28T03-31-28-716Z/mobile-long-transcript-controls.png`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-31-40-300Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused harnesses passed; the selector also recommended slash/question/streaming for broader harness-script changes, but this slice only adds an isolated scenario and reran the new scenario plus mobile layout smoke.

Previous latest: Back to chat composer-takeover fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario slash-commands`, and `bun scripts/ui-harness.ts --scenario mobile-layout`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: full composer takeover `/plan` actions used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/composer.css apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, and focused harnesses `slash-commands`, `streaming-responsiveness`, `question-answer`, `inspector-preview`, `transcript-scroll-stability`, and `mobile-layout`. Follow-up dogfooding caught that the takeover stayed visible after submitting feedback while a run was still ending; fixed by showing takeover only when the latest transcript item is the marked plan summary, and by submitting running actions as queued follow-ups. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: `/plan` inline action visual/focus follow-up used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript.ts apps/web/src/main.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario slash-commands`, and `bun scripts/ui-harness.ts --scenario mobile-layout`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after the focused slash/mobile checks passed.

Previous latest: inline `/plan` final-action buttons used `bun run report:iteration --recommend apps/server/src/workflow-skills.ts scripts/assert-workflow-skills.ts apps/web/src/transcript.ts apps/web/src/main.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/server/src/fake-runner.ts scripts/ui-harness.ts apps/web/src/transcript.test.ts`, `bun run check`, focused harnesses `slash-commands`, `streaming-responsiveness`, `question-answer`, `inspector-preview`, `transcript-scroll-stability`, `narrow-tool-stream`, and optional `mobile-layout`. The first `slash-commands` run caught the fake-agent plan prompt's new “Cancel plan” marker accidentally triggering the fake cancel-question path; fixed and reran `slash-commands` after sharing the marker through `packages/protocol`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused harnesses passed.

Previous latest: success/info app notice cleanup used `bun run report:iteration --recommend apps/web/src/main.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because this was a narrow notice-state cleanup and static checks passed.

Previous latest: prompt screenshot thumbnail placement used `bun run report:iteration --recommend apps/web/src/main.ts`, `bun run check`, and focused image attachment harness `bun scripts/ui-harness.ts --scenario image-attachments` (`test-results/ui-harness/image-attachments-2026-04-28T02-28-38-140Z/image-attachments.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because this was a narrow composer layout change and the focused image path passed.

Previous latest: screenshot attach/upload banner removal used `bun run report:iteration --recommend apps/web/src/main.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because this was a narrow success-notice copy/state change and static checks passed.

Previous latest: failed local bash duplicate-card fix used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/main.ts apps/web/src/transcript.test.ts` plus the follow-up selector `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/transcript.test.ts`, focused unit tests (`bun test apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`), `bun run check`, targeted `bash-commands` reruns after both live-path follow-ups (`test-results/ui-harness/bash-commands-2026-04-28T02-14-43-111Z/`, `test-results/ui-harness/bash-commands-2026-04-28T02-17-11-130Z/`), and the earlier focused harnesses: `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T02-11-43-525Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T02-11-49-292Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T02-11-51-821Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T02-11-55-194Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T02-11-58-073Z/transcript-scroll-stability.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks/harnesses passed.

Previous latest: tool activity receipts/durations and no-card activity-log follow-up used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript-renderer.ts apps/web/src/session-events.ts apps/web/src/main.ts apps/web/src/styles/transcript.css apps/server/src/fake-runner.ts apps/web/src/session-events.test.ts apps/web/src/transcript-renderer.test.ts`, the CSS follow-up selector `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, focused unit tests (`bun test apps/web/src/session-events.test.ts apps/web/src/transcript-renderer.test.ts apps/web/src/transcript.test.ts`), `bun run check`, and focused harnesses: `streaming-responsiveness` (latest inline-tool-chip suppression rerun: `test-results/ui-harness/streaming-responsiveness-2026-04-28T02-02-25-538Z/streaming-responsiveness.png`), `slash-commands` (rerun after question/system width follow-up: `test-results/ui-harness/slash-commands-2026-04-28T01-24-13-967Z/`), `question-answer` (latest accent-rail rerun: `test-results/ui-harness/question-answer-2026-04-28T01-32-56-676Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T01-20-39-575Z/inspector-preview.png`), `transcript-scroll-stability`, `narrow-tool-stream` (latest no-card rerun: `test-results/ui-harness/narrow-tool-stream-2026-04-28T01-27-28-085Z/narrow-tool-stream.png`), `tool-grouping` (latest inline-tool-chip suppression rerun: `test-results/ui-harness/tool-grouping-2026-04-28T02-02-31-384Z/tool-grouping-expanded.png`), `narrow-tool-stream` (latest ANSI terminal rendering rerun: `test-results/ui-harness/narrow-tool-stream-2026-04-28T02-06-59-145Z/narrow-tool-stream.png`), `bash-commands` (latest ANSI terminal rendering rerun: `test-results/ui-harness/bash-commands-2026-04-28T02-06-52-885Z/`), and `tool-image-heavy-transcript` (latest no-card rerun: `test-results/ui-harness/tool-image-heavy-transcript-2026-04-28T01-27-35-956Z/tool-image-heavy-transcript.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused transcript/tool scenarios passed.

Latest: repo-local Bakery Launcher prototype used `bun run project:notes`, the configured `/plan` subagent chain, `bun run report:iteration --recommend README.md DESIGN.md CONTEXT.md package.json scripts/bakery-cli.ts PROJECT_LOG.md`, `bun run check`, `bun run bakery -- --help`, and `bun run bakery -- --version`. A manual foreground smoke from a temporary cwd with alternate ports (`PI_WEB_PORT=43142`, `PI_WEB_VITE_PORT=45174`) confirmed the banner printed the temp workspace, backend/Vite started, and timeout-triggered shutdown reached the backend; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this docs/launcher-process slice.

Previous latest: sidebar backdrop hover blank-screen fix used `bun run project:notes`, `bun run report:iteration --recommend apps/web/src/styles/sidebar.css`, and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow CSS-only fix.

Previous latest: inspector/sidebar removal used `bun run project:notes`, targeted code inspection, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/transcript-dom.ts apps/web/src/styles/base.css apps/web/src/styles/mobile.css apps/web/src/styles.css apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts scripts/ui-harness.ts`, `bun run check`, focused harness scenarios `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T04-45-50-470Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T04-45-56-035Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T04-45-58-250Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T04-46-02-206Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T04-46-04-913Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-46-10-340Z/mobile-layout.png`), and the updated transcript-fork coverage in `tree-fork-navigation` (`test-results/ui-harness/tree-fork-navigation-2026-04-28T04-46-29-967Z/transcript-fork-no-tree-ui.png`). Final static validation reran `bun run check`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because focused checks covered the removed inspector/tree UI and transcript action paths.

Previous latest: ChatGPT-style transcript conversation layout used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript-dom.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css PROJECT_LOG.md`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T00-56-10-407Z/streaming-responsiveness.png`), `bun scripts/ui-harness.ts --scenario inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T01-04-05-900Z/inspector-preview.png`), `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T01-00-40-426Z/mobile-layout.png`), and `bun scripts/ui-harness.ts --scenario image-attachments` (`test-results/ui-harness/image-attachments-2026-04-28T00-59-40-382Z/`). Follow-up final static validation reran `bun run check`. Full `bun run test:web-perf` was intentionally skipped because focused checks/harnesses covered the changed transcript/action/image surfaces.

Previous latest: chat bubble overflow affordance tweak used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, `bun run check`, and `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T00-46-42-007Z/streaming-responsiveness.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this small CSS-only change.

Previous latest: chat bubble optical-centering tweak used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, `bun run check`, and `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T00-44-52-464Z/streaming-responsiveness.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this small CSS-only change.

Previous latest: autocomplete keyboard regression fix used `bun run report:iteration --recommend apps/web/src/composer-controller.ts apps/web/src/main.ts apps/web/src/composer-controller.test.ts`, `bun test apps/web/src/composer-controller.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness`, `bun scripts/ui-harness.ts --scenario slash-commands`, `bun scripts/ui-harness.ts --scenario question-answer`, `bun scripts/ui-harness.ts --scenario inspector-preview`, and `bun scripts/ui-harness.ts --scenario transcript-scroll-stability`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance.

Previous latest: mobile quick-start fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T00-38-33-364Z/mobile-empty-quick-starts.png`), and `bun scripts/ui-harness.ts --scenario empty-session-layout` (`test-results/ui-harness/empty-session-layout-2026-04-28T00-38-49-645Z/empty-session-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance.

Previous latest: empty-session quick-start discovery used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/transcript.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario empty-session-layout` (`test-results/ui-harness/empty-session-layout-2026-04-28T00-35-32-473Z/empty-session-layout.png`), `bun scripts/ui-harness.ts --scenario slash-commands`, `bun scripts/ui-harness.ts --scenario mobile-layout`, and `bun scripts/ui-harness.ts --scenario streaming-responsiveness`. One parallel mobile/slash harness attempt failed from shared fixed ports and passed when rerun serially. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance.

Previous latest: chat bubble UX pass used `bun run report:iteration --recommend apps/web/src/styles/base.css apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, `bun run check`, an exploratory `bun scripts/ui-harness.ts --list` invocation that defaulted to/passed `streaming-responsiveness` with screenshots under `test-results/ui-harness/streaming-responsiveness-2026-04-28T00-17-31-923Z/`, and follow-up mobile bubble tuning validated with `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T00-22-00-029Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose focused static checks only for CSS-token/style changes.

Previous latest: model/thinking picker regression coverage used `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking`, and `bun scripts/ui-harness.ts --scenario mobile-layout`; `model-thinking` now repeats the picker check at mobile width, verifies selected model/thinking values persist, asserts the popover stays onscreen, and captures `model-thinking-mobile.png`. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: mobile model/thinking popover viewport fix used `bun run check` and `bun scripts/ui-harness.ts --scenario mobile-layout`; the first mobile harness rerun caught right-edge overflow, then passed after centering the mobile popover and applying border-box sizing. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: mobile model/thinking picker fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/model-thinking-picker.ts apps/web/src/model-thinking-picker.test.ts apps/web/src/styles/composer.css scripts/ui-harness.ts PROJECT_LOG.md`; `bun test apps/web/src/model-thinking-picker.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout`, and `bun scripts/ui-harness.ts --scenario model-thinking` passed. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: consolidated model/thinking picker used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css scripts/ui-harness.ts`; `bun test apps/web/src/model-thinking-picker.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking`, and `bun scripts/ui-harness.ts --scenario mobile-layout` passed. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: bounded recent-history telemetry used `bun run report:iteration --recommend scripts/report-iteration.ts .pi/skills/iteration-observability/SKILL.md PROJECT_LOG.md`; `bun run check`, `bun run report:iteration`, `bun run project:notes`, and `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session` passed. Full `bun run test:web-perf` was intentionally skipped because the selector marked this script/skill-doc change as script-only with no browser harness coverage needed.

Previous latest: quiet empty-session UX used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/transcript.css scripts/ui-harness.ts`; `bun run check`, `empty-session-layout`, `streaming-responsiveness`, `slash-commands`, `question-answer`, `inspector-preview`, `transcript-scroll-stability`, and `mobile-layout` passed. Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance.

Previous latest: documented the local `pi-subagents` mitigation in `AGENTS.md`: top-level subagent runs in this repository should use `output: false` unless a saved artifact is explicitly needed, because builtin defaults like `context.md`/`plan.md` can dirty the repo or case-collide with tracked root files such as `CONTEXT.md`. Validation used `bun run report:iteration --recommend AGENTS.md` and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this process-doc change.

Previous latest: renamed the streaming `/plan` Plan Card placeholder from `Generating` to `Generating Plan` while keeping the existing spinner, and broadened the Plan Card glossary entry to cover both generating and ready states. `apps/web/src/transcript.test.ts` now asserts the generating card copy, spinner, and aria label. Validation used `bun run report:iteration --recommend CONTEXT.md apps/web/src/transcript.ts apps/web/src/transcript.test.ts`, focused `bun test apps/web/src/transcript.test.ts`, and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only and the focused transcript test passed.

Latest: fixed SSH auth forwarding for containerized development. The original override did pass the host `$SSH_AUTH_SOCK` bind into the container, but on this OrbStack/macOS setup the mounted shell socket produced `Error connecting to agent: Connection refused`; the Docker/OrbStack host-services socket at `/run/host-services/ssh-auth.sock` worked. `compose.ssh-auth.example.yaml` now defaults to `/run/host-services/ssh-auth.sock` for Docker Desktop/OrbStack, still supports `PI_WEB_HOST_SSH_AUTH_SOCK`/`PI_WEB_CONTAINER_SSH_AUTH_SOCK` overrides for Linux/custom setups, and mounts host `known_hosts` read-only so non-interactive `git pull`/`git push` over SSH do not fail host-key verification. A follow-up fixed container startup crashes from bind-mounted stale `.bakery/dev/server.pid` files by storing pid namespace metadata and ignoring metadata-less host pid files inside containers. Validation used `bun run report:iteration --recommend compose.ssh-auth.example.yaml docs/container-development.md .env.example`, `docker-compose -f compose.yaml -f compose.ssh-auth.example.yaml --env-file .env.example config`, a no-extra-env in-container smoke `ssh-add -l && git ls-remote git@github.com:seemethere/bakery.git HEAD`, `docker-compose -f compose.yaml -f compose.ssh-auth.example.yaml up -d --build` followed by API/web curls, `docker-compose exec -T -u bun bakery-dev ssh-add -l && git ls-remote git@github.com:seemethere/bakery.git HEAD`, and `bun run check`; full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this Compose/docs slice.

Previous latest: updated Bakery to the renamed local pi package line. Local `~/.bun/install/global/node_modules/@earendil-works/pi-coding-agent/package.json` reports `@earendil-works/pi-coding-agent@0.74.1`, with matching `@earendil-works/pi-ai`, `pi-agent-core`, and `pi-tui` packages; npm reports the same 0.74.1 versions. Server imports, docs, `apps/server/package.json`, and `bun.lock` now use `@earendil-works/pi-ai`/`@earendil-works/pi-coding-agent` `^0.74.1`. Validation used `bun test apps/server/src/pi-runner.test.ts apps/server/src/session-hub.test.ts apps/server/src/metadata-store.test.ts`, `bun run report:iteration --recommend apps/server/package.json bun.lock apps/server/src/fake-runner.ts apps/server/src/index.ts apps/server/src/metadata-routes.ts apps/server/src/pi-runner.ts apps/server/src/pi-runner.test.ts apps/server/src/session-routes.ts README.md DESIGN.md docs/codebase-efficiency-audit.md PROJECT_LOG.md`, `bun run check`, and selected focused harnesses `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-17T16-40-13-678Z/`), `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-17T16-40-24-198Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-17T16-40-28-453Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-17T16-40-34-860Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-17T16-40-43-866Z/`), and `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-17T16-40-52-020Z/`). Full `bun run test:web-perf` was intentionally skipped because the selector chose focused lifecycle/subagent scenarios rather than the full suite.

Previous latest: compacted mobile running/question stacked states for the React mobile cleanup. Pending Question Cards and running queues now have mobile-only wrapper hooks, capped heights, and internal scrolling; if both strips appear together, CSS lowers their caps to protect transcript/composer space. The fake runner can emit a many-option question fixture, `question-answer` now verifies many options cap/scroll internally, and `mobile-layout` queues eight running steers, expands the queue, and verifies it caps/scrolls internally without overlapping transcript or composer. Validation used `bun run report:iteration --recommend apps/web/src/pages/SessionPage.tsx apps/web/src/components/RunningQueueStrip.tsx apps/web/src/index.css scripts/ui-harness/scenarios/mobile.ts scripts/ui-harness/scenarios/session.ts`, `bun run check`, `question-answer` (`test-results/ui-harness/question-answer-2026-05-10T06-10-10-053Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-10T06-10-13-497Z/`), `sessions-page` (`test-results/ui-harness/sessions-page-2026-05-10T06-06-23-864Z/`), and `session-routing` (`test-results/ui-harness/session-routing-2026-05-10T06-06-34-322Z/`). Reviewer subagent found no blockers; a simultaneous queue+question coverage note was partially addressed with combined-state CSS caps, though no fake scenario currently renders both together. Full `bun run test:web-perf` was intentionally skipped per focused-first guidance.

Previous latest: hardened mobile transcript rich-content rendering for the stacked cleanup. Markdown, code/pre/table, user bubbles, assistant rows, tool bodies, system rows, and Subagent Card wrappers now carry shrink/overflow guards so long unbroken content does not create horizontal scrolling; the fake runner has an explicit `mobile overflow` fixture with long unbroken Markdown, inline code, and fenced code tokens. The mobile long-transcript harness sends that fixture, asserts it rendered, then checks `.message`, `.markdown-body`, `pre`, `code`, `.artifact-image`, and `.message-action-menu` bounds; the mobile image-stability harness now also checks document width. Validation used `bun run report:iteration --recommend apps/web/src/components/transcript/TranscriptRow.tsx apps/server/src/fake-runner.ts scripts/ui-harness/scenarios/mobile.ts`, `bun run check`, `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-05-10T06-00-38-505Z/`), `mobile-image-stream-stability` (`test-results/ui-harness/mobile-image-stream-stability-2026-05-10T05-58-18-853Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-10T05-58-28-585Z/`), `subagent-card-reconnect` (`test-results/ui-harness/subagent-card-reconnect-2026-05-10T05-58-36-553Z/`), `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-10T05-58-46-328Z/`), `subagent-card` (`test-results/ui-harness/subagent-card-2026-05-10T05-58-56-490Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-05-10T05-59-03-207Z/`), and `transcript-text-selection` (`test-results/ui-harness/transcript-text-selection-2026-05-10T05-59-13-346Z/`). Reviewer subagent found no blockers; its fixture-rendering coverage note was fixed by asserting the overflow probe text. Full `bun run test:web-perf` was intentionally skipped per focused-first guidance.

Previous latest: constrained mobile dialogs/popovers for the stacked mobile cleanup. Session Details and Settings now use dynamic-viewport mobile height caps and tighter mobile headers/padding, and the composer mode menu uses a fixed mobile placement so it stays onscreen above the composer; existing model/thinking popover constraints remain covered. The `mobile-layout` harness now opens Settings, Session Details, and the composer mode menu at the 360px lower-bound portrait width and asserts each stays within the viewport without horizontal overflow. Validation used `bun run report:iteration --recommend apps/web/src/components/SessionDetailsDialog.tsx apps/web/src/components/SettingsDialog.tsx apps/web/src/components/Composer.tsx apps/web/src/index.css scripts/ui-harness/scenarios/mobile.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-10T05-52-08-695Z/`), plus pre-gap-fix focused composer/image scenarios `question-answer` (`test-results/ui-harness/question-answer-2026-05-10T05-50-22-109Z/`), `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T05-50-28-947Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-10T05-50-33-704Z/`), `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T05-50-39-936Z/`), and `image-artifact-drop-upload` (`test-results/ui-harness/image-artifact-drop-upload-2026-05-10T05-50-45-102Z/`). Reviewer subagent found no blockers; its initial 390px-only coverage note was addressed by moving the dialog/menu assertions to 360px. Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first guidance.

Previous latest: tuned the React mobile drawer as the next stacked mobile cleanup slice. The mobile session drawer now skips the static Bakery brand block and visible command-search affordance so it opens directly on New Session plus session groups, while desktop branding/search and the command dialog keyboard shortcut remain intact. The `mobile-layout` harness asserts the drawer omits mobile branding/search, keeps New Session before Settings, hides API settings from the drawer body, preserves the session list/backdrop, and still preserves desktop pin preference; `sessions-page` covered the desktop/session-list side. Validation used `bun run report:iteration --recommend apps/web/src/components/sidebar/Sidebar.tsx apps/web/src/components/ui/sidebar.tsx apps/web/src/components/Layout.tsx scripts/ui-harness/scenarios/mobile.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario sessions-page` (`test-results/ui-harness/sessions-page-2026-05-10T05-47-52-652Z/`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-10T05-47-59-112Z/`). Reviewer subagent found no blockers. Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first guidance.

Previous latest: simplified the React mobile header chrome for the stacked mobile cleanup. Portrait mobile headers now keep the hamburger, session title, and Details action while hiding workspace metadata, the draft workspace picker, and passive status pills below `sm`; mobile padding/gaps are also tighter so the transcript keeps more space. The `mobile-layout` harness now checks the post-prompt 360px header path for hamburger access, visible title, Details spacing, hidden workspace/status chrome, and no horizontal overflow. Validation used `bun run report:iteration --recommend apps/web/src/components/Header.tsx scripts/ui-harness/scenarios/mobile.ts`, `bun run check`, and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-10T05-46-16-796Z/`). Reviewer subagent found no blockers. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks and the focused mobile harness covered the deliberately broader mobile-header surface.

Previous latest: started the stacked mobile React cleanup by hardening the composer for portrait phones. Mobile `#prompt` now computes to 16px with a compact CSS max-height that the autosize effect respects, mobile toolbar spacing is tighter, the context badge collapses to protect composer space, and the native attachment input is clipped inside the paperclip target so Send taps do not fall through to attachments. The `mobile-layout` harness now checks the compact 360px composer path for font size, horizontal overflow, footer/transcript budget, attach/send overlap, and a real Send click. Validation used `bun run report:iteration --recommend apps/web/src/components/Composer.tsx apps/web/src/index.css scripts/ui-harness/scenarios/mobile.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-05-10T05-38-02-793Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-05-10T05-38-18-478Z/`), `image-attachments` (`test-results/ui-harness/image-attachments-2026-05-10T05-38-23-417Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-10T05-38-28-031Z/`), `image-paste-attachments` (`test-results/ui-harness/image-paste-attachments-2026-05-10T05-38-33-475Z/`), and `image-artifact-drop-upload` (`test-results/ui-harness/image-artifact-drop-upload-2026-05-10T05-38-39-523Z/`). A reviewer subagent found no blockers and noted that real iOS focus-zoom still needs device/manual confidence beyond Chromium's computed-font guard. Full `bun run test:web-perf` was intentionally skipped per the validation selector's focused-first guidance.

Previous latest: added `ripgrep` and `fd` to the base Bakery dev Docker image and fixed host-managed binary shadowing from mounted `$HOME/.pi`. `Dockerfile` installs Debian `ripgrep` and `fd-find`, then creates `/usr/local/bin/fd` pointing at `/usr/bin/fdfind`; `docker/entrypoint.sh` removes mounted `/home/bun/.pi/agent/bin` from the container PATH and sets `PI_CODING_AGENT_DIR` to a container-local overlay under `/workspace/.bakery-data/pi-agent` with Linux-native `fd`/`rg` symlinks while symlinking auth/settings/resources/sessions back to the mounted pi config. The container smoke docs now verify `rg`, `/usr/local/bin/fd`, and the pi overlay tool symlinks. Validation used `bun run report:iteration --recommend Dockerfile docker/entrypoint.sh docs/container-development.md`, `bash -n docker/entrypoint.sh`, and `bun run check`. `docker build -t bakery-dev:local .` could not run in this environment because the Docker daemon is unavailable (`Cannot connect to the Docker daemon at unix:///var/run/docker.sock`), so the image build/entrypoint smoke remains the manual follow-up. Full `bun run test:web-perf` was intentionally skipped per the selector's Docker/docs-only static-check guidance.

Previous latest: aligned container/dev iteration telemetry with the mounted pi session store. `compose.yaml` now sets `PI_WEB_SESSION_DIR=/home/bun/.pi/agent/sessions`, `.env.example` and container docs describe the override, and `bun run report:iteration --session-context` now recursively scans candidate session directories including `~/.pi/agent/sessions`, so cwd-slugged pi JSONL logs are discovered without manually passing `--session`. Validation used `bun run report:iteration --recommend scripts/report-iteration.ts compose.yaml .env.example docs/container-development.md`, `bun run check`, `bun run report:iteration`, `bun run project:notes`, and a direct `bun run report:iteration --session-context` smoke that found `/home/bun/.pi/agent/sessions/.../2026-05-02T21-19-34-407Z_019dea8f-a006-757c-87ff-acb35064da92.jsonl`. `docker compose --env-file .env.example config --quiet` could not run in this container because the Docker CLI lacks the Compose plugin (`docker: 'compose' is not a docker command`); full `bun run test:web-perf` was intentionally skipped per the selector's script/docs/container-config scope.

Previous latest: moved the interactive `ask_question` UI into the transcript as a compact core Question Card instead of a composer panel or extension card. Pending questions now synthesize a blocking transcript checkpoint, focus/keyboard handling targets the card, the composer shows a small answer-in-transcript hint and refuses normal sends until answered, answered/cancelled question receipts render as read-only cards, and the underlying `ask_question` tool card is hidden from visible transcript noise. `CONTEXT.md` now defines Question Card as core Bakery interaction infrastructure. Validation used `bun run report:iteration --recommend apps/web/src/question-panel-controller.ts apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/composer.css apps/web/src/styles/mobile.css apps/web/src/question-panel-controller.test.ts apps/web/src/transcript.test.ts scripts/ui-harness/scenarios/slash-commands.ts CONTEXT.md`, focused unit tests (`bun test apps/web/src/question-panel-controller.test.ts apps/web/src/transcript.test.ts`), and `bun run check`. The selected browser harnesses could not run in this container after `bun x playwright install chromium` because Playwright reported missing host libraries (`libglib2.0-0t64`, `libnss3`, `libatk1.0-0t64`, `libgbm1`, and related X/GTK/ALSA dependencies); the failed attempt artifact is `test-results/ui-harness/streaming-responsiveness-2026-05-02T21-08-48-169Z/`. Full `bun run test:web-perf` was not run because focused browser launch is blocked by the host dependency issue.

Previous latest: suppressed raw final `/plan` output while it streams by detecting running assistant messages that have begun the required `## Plan summary` final-plan shape and rendering a Plan Card-like `Generating` placeholder with a spinner until the final `Plan actions: Accept plan` marker arrives. The completed marker path still renders the full Plan Card and details affordance. Validation used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/transcript.test.ts`, `bun run check`, and focused `bun test apps/web/src/transcript.test.ts`; the selector chose static checks only and full `bun run test:web-perf` was intentionally skipped.

Previous latest: added the first true Bakery extension UI slice. The extension registry now loads bundled extensions plus operator-configured additional paths, exposes extension command/card/web-module metadata, serves trusted extension web modules, and imports them in the browser; `/bakery:generate-details` now declares a `transcript.customCard` and renders through a bundled Web Component instead of a hard-coded transcript renderer. Added `Dynamic Bakery Extension` and `Extension Card` context terms plus ADR 0002 for trusted main-document web extensions, and updated the extension design doc to match the configured-path-first direction. Validation used `bun run report:iteration --recommend CONTEXT.md docs/extensions-design.md docs/adr/0002-trusted-bakery-web-extensions.md packages/protocol/src/index.ts apps/server/src/extensions.ts apps/server/src/extensions.test.ts apps/server/src/config.ts apps/server/src/index.ts apps/server/src/session-hub.ts apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts apps/server/src/bundled-extensions/bakery/index.ts apps/server/src/bundled-extensions/bakery/web/metadata-details-card.js apps/web/src/extension-cards.ts apps/web/src/transcript.ts apps/web/src/main.ts scripts/assert-workflow-skills.ts`, focused unit tests (`bun test apps/server/src/extensions.test.ts apps/web/src/transcript.test.ts`), `bun run check`, and selector-requested harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-05-02T02-45-57-101Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-05-02T02-45-59-103Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-05-02T02-46-04-810Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-05-02T02-46-07-948Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-05-02T02-46-10-190Z/`), and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-05-02T02-46-14-997Z/`). An optional exploratory `session-metadata` rerun still fails early on the existing sidebar active-title assertion before it reaches metadata generation (`test-results/ui-harness/session-metadata-2026-05-02T02-44-38-047Z/`), so it was not included as selected validation. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: aligned `/plan` with the docs-aware grill workflow. Added root domain documentation conventions (`CONTEXT.md`, `CONTEXT-FORMAT.md`, `ADR-FORMAT.md`) plus ADR 0001 for `/plan` updating domain docs inline, then updated the bundled `/plan` prompt/assertions to discover context/ADR docs, challenge glossary conflicts, update `CONTEXT.md` as terms resolve, and create ADRs only for hard-to-reverse/surprising/tradeoff decisions. Validation used `bun run check`, `bun run report:iteration --recommend apps/server/src/bundled-extensions/plan/plan-prompt.ts scripts/assert-workflow-skills.ts`, and selector-requested `bun scripts/ui-harness.ts --scenario slash-commands` (`test-results/ui-harness/slash-commands-2026-05-01T23-54-27-712Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's focused-first guidance.

Previous latest: structured quiet tool receipts used `/plan` interview decisions, `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts scripts/ui-harness/scenarios/transcript.ts`, focused unit tests (`bun test apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts`), `bun run check`, and direct visual/regression harnesses `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-30T14-40-14-265Z/narrow-tool-stream.png`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-30T14-40-47-511Z/tool-grouping.png`), and `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-30T14-40-50-101Z/mobile-long-transcript-controls.png`). The first `tool-grouping` run exposed a text-selector mismatch after action/target markup split; adding literal whitespace between action and target preserved accessibility/search text and the rerun passed. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only and focused transcript/mobile harnesses passed.

Previous latest: mobile flat-tool receipt readability tweak used `bun run report:iteration --recommend apps/web/src/styles/mobile.css`, `bun run check`, and direct visual/regression harnesses `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-29T03-52-49-446Z/mobile-long-transcript-controls.png`) and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-52-51-630Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this mobile CSS-only follow-up and focused mobile harnesses passed.

Previous latest: flat tool activity simplification used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/main.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun test apps/web/src/transcript-renderer.test.ts`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-48-56-502Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-49-14-857Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-49-16-928Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-49-19-602Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-49-21-889Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-49-08-776Z/`), plus direct tool/mobile regression harnesses `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-51-13-763Z/`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T03-51-19-095Z/`), and `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-29T03-51-21-322Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: direct take-control UI reconciliation used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, direct lifecycle harnesses `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-28T20-08-36-975Z/`) and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-28T20-08-46-863Z/`), plus selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T20-08-53-658Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T20-08-58-480Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T20-09-00-729Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T20-09-04-759Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T20-09-07-347Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T20-09-12-188Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: direct take-control semantics used `bun run report:iteration --recommend apps/server/src/index.ts apps/server/src/config.ts apps/web/src/main.ts packages/protocol/src/index.ts scripts/ui-harness.ts`, `bun run check`, and focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-28T19-14-45-953Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-28T19-14-50-453Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-04-28T19-14-55-547Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T19-15-01-783Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T19-15-18-582Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T19-15-27-740Z/`). One parallel `question-answer` attempt failed due to harness port contention and passed when rerun serially. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused lifecycle/protocol checks passed.

Latest: completed a four-commit documentation drill-down for new operators and day-to-day operation. Added `docs/README.md` as the durable docs map, `docs/quickstart.md` for the text-only clone-to-first-safe-session path, `docs/operation.md` with Mermaid diagrams for local runtime, workspace boundaries, and session flow, and `docs/troubleshooting.md` with command maps, launcher-vs-dev-loop and connection troubleshooting diagrams. README, LAN, and container docs now cross-link the first-run, safe-operation, and troubleshooting paths. Validation was run per commit with the selector and `bun run check`; the final selector was `bun run report:iteration --recommend README.md docs/README.md docs/quickstart.md docs/operation.md docs/troubleshooting.md docs/container-development.md`, which chose static checks only. Full `bun run test:web-perf` was intentionally skipped because this was docs-only and the selector did not escalate.

Previous latest: evolved the sidebar Add Workspace dialog toward a local folder selector while preserving the Browse Root / Approved Workspace security model. Browsing is now primary with root-bounded breadcrumbs, single-click selection plus double-click/Browse navigation, a quiet Open here/Open selected action, manual exact-path approval tucked into a low-emphasis fallback, and the backend labels a configured home Browse Root as `~`. A follow-up UI/UX skill audit reduced visual noise by removing per-row full paths/badges, replacing repeated Browse text with a secondary chevron, and simplifying helper copy. Validation used `bun run report:iteration --recommend apps/web/src/components/workspaces/WorkspaceBrowserDialog.tsx apps/web/src/components/sidebar/Sidebar.tsx apps/web/src/components/ui/alert.tsx apps/web/src/components/ui/badge.tsx apps/server/src/workspaces.ts apps/server/src/workspaces.test.ts`; selector chose `bun run check`, which passed, and focused backend regression `bun test apps/server/src/workspaces.test.ts` also passed. Follow-up quiet pass reran `bun run check`, which passed. Full `bun run test:web-perf` was intentionally skipped by the selector for this focused UI/backend-label slice.

Previous latest: workspace approval backend foundation used the bundled `/plan` interview to settle Browse Root vs Approved Workspace terminology, updated `CONTEXT.md`, and implemented shared protocol browse schemas, safe workspace browsing, explicit add/revoke approval semantics, runtime permission checks, and metadata-store revoke support. Focused validation passed with `bun test apps/server/src/workspaces.test.ts apps/server/src/metadata-store.test.ts` and `bun run check`; selector-requested `bun scripts/ui-harness.ts --scenario subagent-card-reconnect` was attempted twice but failed before exercising this change while waiting for `#apiBase` (`test-results/ui-harness/subagent-card-reconnect-2026-05-10T01-04-39-540Z/`, `test-results/ui-harness/subagent-card-reconnect-2026-05-10T01-05-55-976Z/`), matching the degraded pre-existing artifact pattern reported by `bun run report:iteration --latest-artifact subagent-card-reconnect`. Full `bun run test:web-perf` was not run because the first selected focused harness was blocked.

## Next priorities

1. Use the new long-transcript baseline to choose the first evidence-backed optimization slice: compare persisted reopen, live streaming, and mobile-control metrics from the focused scenarios; prioritize render/commit or image/DOM work before protocol windowing unless real dogfooding shows histories much larger than the synthetic baseline.
1. Dogfood the updated mobile React cleanup on a real phone: confirm the composer no longer zooms, Send no longer opens attachments, normal Question Cards show all options above the composer, long-question `Show all options` chooser feels clear, custom question answers via composer feel obvious without the removed in-card input, header/drawer/dialogs/transcript/queue surfaces feel usable at 360-430px, and decide whether any remaining mobile paper cut warrants another focused slice before returning to the broader backlog.
1. Dogfood the file-manager-like sidebar Add Workspace UI in a real browser: restart the backend for the `~` Browse Root label if needed, refresh the Vite page for the dialog code, confirm a home Browse Root displays as `~`, confirm a repo-only Browse Root allows browsing inside but breadcrumbs never expose parents above it, add a workspace from a Browse Root, add one through manual exact-path fallback outside the Browse Root, confirm each opens a new session, and revoke an Approved Workspace to verify existing sessions remain visible while runtime operations are blocked when no other root covers the path.
1. Dogfood the new `/plan` default planning-chain integration in real model sessions: confirm project `.pi/settings.json` makes `dev-loop-plan` default, the injected `subagent({ chain: ... })` recipe runs before the interview/final Plan Card when pi-subagents is loaded, project-default warnings appear only for unusable configured defaults, and the hybrid gate still stops before edits.
1. Dogfood Event Fork in real model sessions: fork from a prior user prompt and confirm editable composer prefill, fork from assistant output and tool rows to confirm history continues through the selected event, verify the source session remains unchanged, and decide whether UI copy needs an explicit “files are not restored” reminder before designing Git checkpoint/file-state rollback.
1. Dogfood the new containerized development environment from a clean clone/volume on both macOS Docker/OrbStack and Linux Docker: confirm UID/GID ownership, `.env` token setup, `$HOME/.pi` auth/resource behavior, default repo-only workspace root, SSH-agent forwarding, `gh` auth forwarding, and explicit Docker socket override all feel safe and understandable before designing a production image or Bakery CLI.
1. Dogfood the completed UI harness family split when adding or debugging scenarios: keep `scripts/ui-harness/scenarios/index.ts` as a thin dispatcher, put new scenario bodies in the matching family module, and revisit the `empty-session-layout` footer-height assertion before relying on full `bun run test:web-perf` as a green baseline.
2. Continue open-source-readiness cleanup with a fresh hotspot check before the next refactor: backend route/WebSocket hub boundaries, web-controller seams, and the bundled `/plan` extension extraction now exist, so prefer narrowly product-tied session lifecycle/composer image extraction over broad splitting unless a feature demands it.
3. Dogfood `/bakery:generate-details` in real model sessions: try idle and running-turn invocation, guidance text, manual-field protection, and `--replace`; decide whether the compact receipt copy is enough and whether a shorter alias is warranted.
4. Dogfood true Bakery extensions in a real model/dev session using the new configured-path smoke as the baseline: try editing/reloading the local extension repeatedly, confirm page-refresh expectations for changed browser JS are clear, then decide whether the next slice should be project/global auto-discovery or a minimal `packages/extension-api` DX helper.
4. Dogfood the Preview Stack MVP from real isolated sessions: start a preview from a clean worktree and a dirty-source-created worktree, confirm the URL opens the branch UI with fake-agent data, stop/status/log copy is understandable, and decide whether dependency install/preflight copy is needed before broader use.
5. Add explicit worktree status/change-summary display for isolated sessions: branch, base commit, dirty state, changed-file count, preview-stack status, and a copyable manual review command before considering merge/apply or PR actions.
6. Dogfood opt-in isolated worktree sessions in a real Git repo together with Preview Stacks: confirm agent edits stay out of the source checkout, branch/worktree details are understandable, and the warning about dirty source changes is clear.
7. Dogfood the new local install docs/doctor/launcher path on a clean-ish machine or fresh clone: confirm `bun install`, `bun run doctor`, `bun run bakery`, local `PI_WEB_WORKSPACE_ROOT="$PWD" bun run dev`, and token-protected `bun run doctor --lan` copy produce the right next actions for a first pi-developer user.
7. Consider the next Bakery Launcher distribution slice after dogfooding: decide package name/scope, npm `bin`, static frontend serving vs. two-port runtime, whether the launcher should run doctor/preflight checks, and whether an explicit `--open` flag is worth adding before real `bunx bakery` packaging.
8. Dogfood the new terminal-chat Question Card model in real `/plan` and `ask_question` sessions on desktop plus a real phone/narrow browser: confirm the agent actually stops cleanly after asking, the normal composer answer path feels natural with text/images, option taps as normal sends are understandable, viewer/disconnected states are clear, and the checkpoint receipt gives enough transparency without resurrecting the hidden `ask_question` tool card.
9. Dogfood the new top-bar identity strip plus composer brain model/settings control on desktop and a real phone/narrow browser: confirm the title/workspace/details hierarchy gives enough orientation, Details is discoverable, and the composer-side model menu placement does not crowd Prompt/Context.
10. Explore the follow-up connection/control status treatment: decide how controller/viewer/take-control and red/yellow/green connection state should move toward composer-side status without conflicting with composer mode/context/model indicators.
11. Dogfood inferred auto-scroll behavior: confirm users understand that staying at the bottom follows latest output, scrolling up pauses follow-latest, and Jump to latest is enough without an explicit Auto-scroll checkbox.
12. Dogfood the adaptive mobile running tool activity on a real phone/narrow browser: confirm the summary-only default feels calmer during long tool runs, tapping the activity row to reveal receipts is discoverable enough, and desktop still has enough visible progress detail.
13. Dogfood the empty-session quick starts on desktop and a real phone/narrow browser: confirm the `/plan`, screenshot, `@file`, and `!bash` affordances teach the expected Bakery usage without feeling like a noisy onboarding tour, and decide whether the screenshot chip should open the picker or stay guidance-only.
14. Dogfood `!`/`!!` bash command support in real sessions: confirm failed commands now update the same local-bash card instead of rendering a duplicate SDK echo, the distinct expanded local-bash styling is readable in both themes, `!!` context exclusion is useful, idle-only blocking is acceptable, and decide whether the next slice should allow bash while an agent turn is running.
15. Dogfood the focused-first validation decision block plus the new artifact-quality retry guidance in future sessions: confirm agents stop after the first focused harness failure, inspect usable artifacts/logs before rerunning, regenerate only the same focused scenario when artifacts are degraded/missing, skip full `bun run test:web-perf` for script/docs-only changes, and escalate only for protocol/session lifecycle, broad UI, or unexpected focused failures.
16. Dogfood `/skill:iteration-observability` in real pi sessions and tune `.pi/skills/iteration-observability/SKILL.md` if agents still rerun failed focused scenarios without artifact inspection, print too much, or produce generic process advice instead of evidence-backed repository recommendations.
17. Dogfood `bun run project:notes --handoff` at the end of future sessions; tune its anchors/line hints if agents still read broad `PROJECT_LOG.md` ranges or miss the right update targets.
18. Keep dogfooding the `Action summary` and `Rerun opportunity` sections in `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session` and `--session-context`: the latest bounded 10-session review estimated 7-15 avoidable focused-harness reruns while avoiding contamination from the active conversation; next tune thresholds/wording only if repeated sessions show noise instead of actionable recommendations.
19. Dogfood the CSS module split in the next real UI styling session; if the boundaries feel off, tune the module names/section placement before deeper style edits.
20. Continue the main-file extraction dogfood only with a fresh hotspot check: autocomplete, plan actions, running queue, settings, transcript state/event/live/shell, row actions, composer mode, shell events, and status seams now exist, so the next cleanup should target a remaining app-coordinator island such as session lifecycle/routing or composer image/drop handling rather than re-growing `main.ts`.
21. Consider the next observability slice: add explicit phase markers/intent labels or expose validation/edit telemetry in a user-facing session diagnostics panel if JSONL-derived reports prove valuable but too offline/manual.
22. Dogfood the new structured quiet tool receipts in real sessions: confirm action-first labels are understandable, ≥1s duration visibility is enough, failed rows feel aligned with successful rows aside from the red chevron, and decide whether duration metadata should be persisted into snapshot history beyond live web transcript items.
23. Dogfood the mobile sidebar drawer on a real phone/narrow browser: confirm the recency groups feel useful with real session history, Today/Yesterday expanded defaults are right, backdrop/toggle close behavior feels obvious, header density is still acceptable, and settings remain lower-priority than session switching.
24. Dogfood the desktop sidebar overlay in real work: confirm the header hamburger is discoverable enough without the old 48px rail, backdrop/Escape/toggle close behavior feels right, pinned docked mode remains understandable, and grouped sessions reduce noise without hiding needed continuations.
25. Dogfood the shared editable metadata suggestion flow on desktop and a real phone/narrow browser: confirm ✨ generation progress, provider errors, editable title/summary fields, per-field ✓/× controls, and the trigger-adjacent mobile popover feel obvious; tune saved-summary visibility on mobile if hiding the collapsed summary still feels like a gap after applying.
26. Dogfood the mobile layout on a real phone/narrow browser: confirm the hamburger drawer feels quick, session-first ordering is right, no side rails steal space, the compact `Ctx` pill is readable in the lower composer bar, and hiding Details/Preview on mobile does not block common work. Tune drawer close behavior/backdrop/header density or continue desktop/mobile parity gaps as needed.
27. Continue the full transcript performance spike focused on decoupling chat rendering from the rest of the app shell. Use the real `Fix Screenshot Attachment Flow` session plus `tool-image-heavy-transcript` and `mobile-long-transcript-controls` as benchmarks; next targets are inspector/session chrome/show-thinking updates so they patch local UI islands instead of causing transcript row hydration/traversal, and a dedicated transcript component/controller boundary before deeper virtualization.
28. Dogfood the long-transcript performance path in the real mobile session that felt sluggish. The focused fake-agent mobile control baseline is currently healthy, so if the real session still lags, capture whether the pain is larger transcript scale than 96 tools, initial snapshot hydration, browser/device class, image loading, or a control path not covered by the benchmark.
29. Dogfood the simplified screenshot flow in the original remote-session browser: drag, paste, and paperclip should all attach prompt image bytes and insert/render `.bakery/artifacts/...` transcript preview paths without any image mode choice. If this feels good, consider removing or shortening older artifact-mode docs/log references in follow-up cleanup.
30. Continue the architecture-review/iteration-speed pass with the next smallest extraction only after checking current hotspots: likely session lifecycle/controller helpers, composer image/drop handling, or a pure rendering/state island, using `bun run report:iteration --recommend <changed files>` before validation.
31. Dogfood `bun run project:notes` at the start of future sessions; tune its sections/caps if it omits needed handoff context or still prints too much.
32. Dogfood the new `## Validation decision` handoff block in future sessions; tune `scripts/report-iteration.ts` guidance or `scripts/assert-workflow-skills.ts` assertions if agents still skip the report, omit full-suite run/skip rationale, or if recommendations are noisy.
33. Continue dogfooding `/plan` in real model sessions and tune launcher prompt/tool guidance if the model drifts from the canonical `grill-me` behavior: relentless but practical one-question-at-a-time interviewing, codebase exploration before asking discoverable questions, recommended answers for every question, and decision-tree branch resolution before final handoff.
34. Continue keyboard-first polish from dogfooding if new edge cases appear: reconnect timing beyond snapshot reload, Q&A viewer take-control flow from the question panel, transcript fork action accessibility, and additional shortcut coverage.
35. Continue evolving the typed bundled workflow-skill registry into a fuller default-installed skills layer: editable/content-like instructions, clearer resource provenance in UI/API surfaces, and reusable launch paths for additional bakery-owned workflows.
36. Tune answered-question transcript summaries from real-use feedback: visual density, whether to show the recommendation/chosen option metadata, and interaction with tool-card grouping/collapse.
37. Continue the theme/color pass from real-use feedback using `theme-gallery` first: inspect the gallery screenshots for remaining light/dark inconsistencies, then tune selected-row emphasis, tool/activity grouping, tree drawer, and lower-frequency overlays with contrast checks rather than trying to mimic GitHub directly.
38. Consider adding a lightweight contrast/token audit for core foreground/background pairs now that the visual gallery exists.
39. Tune session identity/sidebar details: adjust status labels, snippets, auto-collapse behavior, pin-mode copy, and summary preview density if they feel noisy in ongoing use.
40. Continue attention hierarchy from new real-use feedback: current-action strip, user-input-needed states, selected-row emphasis, and completed activity grouping now have a first pass but may need tuning during real dogfooding.
41. Fix any real-session lifecycle/UI state issues that surface: duplicate snapshots, stale controller state, scroll edge cases, reconnect copy, retry behavior, or remaining selection-preservation edge cases during live streaming.
42. Tighten web-perf thresholds over time once more baseline runs are available, and consider reporting percentile timings in addition to max timings.
43. Add sanitized real-event playback to complement the deterministic fake runner if real SDK/provider event patterns diverge from fake-agent coverage.
44. Expand branch/tree support beyond basic navigation/current-path clarity: add keyboard navigation, summarize-before-navigation flow, label/bookmark editing, filter/search modes, and clearer current-path rendering refinements from dogfooding.
45. Explore `@earendil-works/pi-web-ui` adapter once the remote agent state shape is clearer.

## Session handoff convention

At the end of each work session:

1. Run `bun run check`.
2. Update `PROJECT_LOG.md` current status / next priorities if anything changed.
3. Tell the human operator how to test the changes manually, including whether the current browser page can pick them up automatically, requires a page refresh, or requires a backend/dev-server restart.
4. Commit with a concise Conventional Commit message.
5. In the next AI session, start by reading `DESIGN.md` and `PROJECT_LOG.md`.

## UI validation convention

- For any UI feature touching typing, autocomplete, streaming, scrolling, or keyboard navigation: avoid full-app renders in the interaction path; patch local DOM islands and add focused harness coverage for responsiveness.
- UI-affecting changes should run `bun run report:iteration --recommend <changed files>` first, then `bun run check` plus the selected focused harness scenarios; run full `bun run test:web-perf` only when the selector/lifecycle breadth/focused failures justify escalation.
- Use `bun run ui:manual` for headed fake-agent exploratory validation in a temp workspace.
- Include harness scenario names and artifact paths in handoffs.
- When harness screenshots are generated, include the most relevant PNG file paths as workspace-relative paths in the handoff, not just artifact directories, so the web transcript renders thumbnail previews directly in the dev loop.
- If a new UI feature is not covered by the harness, extend `scripts/ui-harness.ts` with a scenario before relying on human-only manual testing where practical.
