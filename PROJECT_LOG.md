# Project Log

Use this file to preserve context between coding sessions. Keep entries short and update the top section before committing.

## Current status

Implemented the first basic vertical slice scaffold plus initial multi-client lifecycle support, improved transcript readability, file/slash-command autocomplete, transcript scroll protection with a floating "Jump to latest" affordance, the first session identity/sidebar usability pass, a compact-successful-tool transcript pass, and the first canonical session title/summary metadata pass. The latest transcript noise, running-control, message-action, session-sidebar, controller handoff, reconnect/restart, scroll-protection, session-metadata, and inspector-removal changes have now been dogfooded/partially harness-validated; current direction remains workflow usability over broad polish, with follow-up work focused on incremental refinements from real-use feedback:

- Bun workspaces monorepo with `apps/server`, `apps/web`, and `packages/protocol`.
- Shared Zod/TypeScript protocol definitions, including controller state, session lifecycle config, and runtime model/thinking settings.
- Fastify backend with localhost/token auth, workspace allowlist checks, config/workspace/model endpoints, session CRUD, SQLite metadata, and WebSocket session endpoint.
- In-process pi SDK session runner wired for prompt, steer, follow-up, abort, model/thinking changes, snapshots, and normalized event streaming.
- WebSocket session hubs now support reconnect snapshots, multiple clients, first-client controller assignment, take-control, per-client sequence envelopes, and disconnected idle disposal.
- Minimal Vite web component UI for API/token settings, workspace/session list, session creation/opening, WebSocket connection, readable transcript rendering, a consolidated model/thinking picker, prompt/steer/follow-up input, abort controls, controller/viewer status, and TUI-like transcript follow-latest auto-scroll.
- Assistant transcript rendering now handles Markdown via `marked`, hides thinking by default with a `Show thinking` toggle, renders readable thinking traces dim/italic when enabled, and formats inline tool calls compactly instead of dumping raw provider JSON/encrypted thinking payloads.
- Tool execution cards now use more TUI-like compact titles, green/blue/red status backgrounds, and cleaner result extraction from text/image/diff/stdout/stderr result payloads.
- Tool activity rendering has been simplified again: nested live/completed tool stacks were removed in favor of flat tool receipt rows plus one stable running activity strip, reducing mobile inconsistency and deleting the fragile group patch/restore path.
- Web dev server now disables Vite browser HMR/reload by default so in-browser agent edits do not refresh/kill the UI session; set `PI_WEB_VITE_HMR=true` to opt back in. Server `dev` now runs without Bun watch by default so edits to backend/shared packages do not restart and kill in-process pi sessions; use `bun run dev:server:watch` to opt into backend watch mode. The default local bootstrap is now `bun run dev`, which ensures a detached backend is up and then runs Vite in the foreground; use `bun run dev:server:restart` to restart only the backend during the dev loop. The web UI also remembers and reopens the last selected session after a page reload.
- Added the first clean-path route boundary: opening/creating/selecting a session pushes `/sessions/:id`, direct loads of `/sessions/:id` open that session, Back/Forward switches session routes, and Back to `/` clears the selected session while keeping the current app shell. Route parsing/building lives in a small tested web helper instead of introducing a full router dependency.
- File search/complete endpoints now call the ignore-aware workspace scanner, validate query params with shared Zod schemas, cap result limits, skip default heavy/binary paths, and keep nested `.gitignore` rules scoped to their subtree.
- Prompt input now has `@file` autocomplete backed by the file search/complete endpoints, with keyboard navigation, click selection, directory continuation, and prompt draft preservation across live transcript rerenders; fixed dropdown closing during rerenders while the prompt remains focused.
- Added shared command metadata protocol, `GET /api/sessions/:id/commands`, and prompt-box slash-command autocomplete for built-ins, extension commands, prompt templates, and skills; terminal/UI-only built-ins are marked unsupported in metadata.
- WebSocket prompt handling now intercepts built-in slash commands before they reach the LLM as normal prompts. Implemented web results for `/reload`, `/changelog`, `/session`, `/compact`, `/name`, and `/copy`; unsupported built-ins render a structured web error instead of being sent to the agent.
- Prompt drafts that start with `!` or `!!` now run through pi's native bash execution path over WebSocket: `!` records output in session context, `!!` marks the bash execution excluded from LLM context, snapshot hydration renders persisted `bashExecution` rows as terminal-style tool cards, and the first web slice blocks direct bash while an agent turn is already running. Developer-run bash rows now stay expanded by default, avoid completed-tool grouping, use distinct local-bash styling so they do not look like ordinary agent tool calls, and coalesce SDK `bashExecution` echoes into the optimistic local bash row so failed commands do not render as a second card.
- Fixed the server startup regression from the `/changelog` implementation by replacing Bun-incompatible `createRequire(...).resolve("@mariozechner/pi-coding-agent")` usage with `import.meta.resolve` for locating the pi package changelog.
- Fixed command and file autocomplete keyboard navigation so the dropdown scrolls to keep the selected row visible after arrow-key selection changes; later fixed the extracted composer keydown handler to read live autocomplete state after async result patches, restoring Arrow/Tab/Enter selection for slash commands and `@file` matches.
- Fixed the follow-up regression from that change: initial render no longer calls `querySelector("")`, so the app refreshes workspaces/sessions instead of staying empty.
- Continued the behavior-preserving `main.ts` deslop chain: autocomplete orchestration, plan-action handling, running-queue controls, and settings/session-detail rendering now live in focused web modules instead of the app coordinator.
- Added a right-side Details/Preview inspector panel: transcript messages/tool cards are selectable, selection and collapse state persist locally, Details shows compact metadata/content plus collapsible raw event data with copy controls, and Preview renders Markdown/code plus sandboxed HTML/SVG snippets.
- Improved streaming UI responsiveness by throttling WebSocket-driven renders, reducing transcript follow-scroll layout work, and caching rendered transcript segment HTML.
- Added basic session tree/fork/navigation support: shared tree/fork/navigate protocol schemas, `GET /api/sessions/:id/tree`, `POST /api/sessions/:id/fork`, and `POST /api/sessions/:id/tree/navigate`. The visible right-inspector Tree tab and wide `/tree` drawer were later removed with the rest of the inspector sidebar; transcript user-message entries still use tree data internally to offer "Fork from here".
- Reduced streaming render lockups by throttling live renders further, patching only dirty transcript items during active runs instead of replacing the whole app shell, and rendering live assistant Markdown as escaped plain text until the message completes.
- Added an agent-operable Playwright UI harness: `PI_WEB_FAKE_AGENT=1` enables a deterministic fake session runner, `bun run test:web-perf` starts backend/web against temp dirs, drives the real browser UI, measures prompt/control responsiveness during synthetic streaming, and writes screenshots/traces/metrics under ignored `test-results/`; `bun run ui:manual` opens a headed fake-agent browser session for exploratory manual validation until Ctrl+C.
- The left workspace/session sidebar is now collapsible, with state persisted in local storage and compatible grid sizing when the right inspector is also collapsed.
- Assistant Markdown, assistant/user image content parts, and tool-result image content now render safe inline images (`http(s)`, `file`, app-relative, and base64 `data:image/png|jpeg|gif|webp`); tool cards containing rendered images stay open by default, and the fake-agent runner can emit a sample inline PNG when prompts mention images/screenshots for visual validation.
- Prompt composer now supports image attachments for new prompts via paste, drag/drop, or file picker, with thumbnails/removal before send; attached images are sent over the WebSocket prompt message into `AgentSession.prompt(..., { images })` and render back in the transcript. After send, attachment thumbnails clear immediately, and image-only sends use a default inspection prompt.
- Expanded `bun run test:web-perf` into an all-scenarios fake-agent suite covering streaming responsiveness, inspector Details/Preview, slash commands, tree/fork/navigation, reconnect/controller handoff, and narrow tool-heavy streams. Harness metrics now include Chromium long-task samples plus app-exported render/patch timing via `window.__piWebPerf`; fake-agent sessions now persist enough JSONL tree data to exercise tree navigation and fork flows.
- Refactored transcript rows into dedicated `<pi-transcript-row>` custom elements. Live patches now update existing row instances, and actively streaming assistant/user text updates the existing `<pre>` `textContent` without replacing the row HTML. `window.__piWebPerf` now also records row-update timings.
- Added web-perf threshold checks to the Playwright harness for long tasks plus render/patch/row-update max timings. Thresholds are intentionally loose and configurable with `PI_WEB_PERF_MAX_*`; set `PI_WEB_PERF_THRESHOLDS=off` to disable locally.
- Fixed transcript selection scroll jumps: clicking an earlier message/image/tool while auto-scroll is enabled now preserves the current transcript scroll position instead of jumping to the bottom.
- Made the fake-agent runner more realistic and deterministic: uneven chunk sizes/cadence, no-delay event bursts, repeated partial Markdown/code fences, and delayed/bursty tool updates interleaved during assistant streaming.
- Added focused Playwright harness coverage for `@file` autocomplete/search+directory continuation, prompt image attachment add/remove/send/render flows, and model/thinking selector updates. This caught and fixed duplicate trailing slashes for directory autocomplete insertion/rendering.
- Added reconnect/restart UX basics: a visible connection-state banner (`connected`/`connecting`/`reconnecting`/`disconnected`/`retry failed`), automatic WebSocket reconnect with backoff, clearer disconnected/send-failed copy, per-session prompt draft persistence in `localStorage`, and warnings that image attachments are prompt-only and not restored after refresh. Added fake-agent harness coverage for reload/reconnect draft preservation and an actual backend process restart (`backend-restart`) that verifies reconnect, draft preservation, and post-restart send usability.
- Simplified multi-tab control handoff: any connected viewer can take control immediately at any time without requesting approval from the previous controller. Removed the pending takeover timeout/config path, approval/denial protocol messages, and stale Control requested/Approve/Deny UI copy; reconnect/controller harness coverage now asserts immediate steal-and-send behavior.
- Transcript scrolling now protects scrolled-up reading during streaming: leaving the bottom pauses follow-latest without yanking the transcript, tracks unread transcript item updates, and shows a floating "Jump to latest" button that resumes follow-latest. WebSocket connection close/retry notices stay in the banner instead of adding transcript rows.
- Dogfooding usability findings addressed/validated in real use: running activity/tool output is quieter with successful tools collapsed/grouped into background activity; connection lifecycle events stay out of the transcript; composer/buttons are more compact and TUI-like; steer/follow-up affordances and queued chips are visible in the compact composer; inspector remains a low-frequency/debug UI; the left sidebar can auto-collapse during active sessions; message actions live in overflow menus.
- Session identity/sidebar usability pass: header now shows an editable session title with repo/path metadata below it; session lists hide raw UUIDs by default, sort by last activity, show last prompt snippets, relative activity time, and status chips; older-than-one-week sessions are hidden behind a persisted "show older" toggle. `/api/sessions` now enriches metadata from pi JSONL/session state with last activity, last user prompt, and active status.
- Successful tool executions now automatically fold back into a smaller, muted activity row when they transition from running to done, with a compact result preview in the header; running, failed, newly selected, and image-bearing tool rows stay expanded/prominent. Re-clicking an expanded tool header now collapses it instead of immediately reselecting/reopening it; transcript row selection binding is restored after full renders and ignores collapsible headers. The Preview inspector now renders assistant/user content segments so inline image parts preview correctly. The Playwright harness now pre-seeds its API base in localStorage so local dev servers on the default port do not race isolated fake-agent runs.
- Prompt composer/running controls are now more compact and TUI-like: shorter textarea, explicit idle vs running mode hint, Enter/Alt+Enter shortcut labels on buttons, running-state footer emphasis, queued steer/follow-up chips in the composer, and `queue_update` events no longer add transcript noise.
- Transcript rows now have quiet per-message overflow menus for Copy, Details, Preview, and valid user-message Fork actions. The actions select/open the inspector without adding always-visible clutter, and the inspector-preview harness now exercises menu-driven Preview/Details flows.
- The session sidebar now communicates whether it is pinned open or will auto-collapse, and opening an existing recent session auto-collapses the sidebar unless the operator has pinned it open.
- Desktop left-sidebar distraction spike: unpinned sessions now open from the header hamburger as a temporary overlay with backdrop/Escape/toggle close behavior, while pinned mode remains a docked left column. New/forked sessions and automatic reload restore now collapse the temporary overlay by default so chat controls are not blocked. The recent-session list is grouped by recency (`Today`, `Yesterday`, `Earlier this week`, `Older`) with group collapse state persisted, and main-area notices remain visible when the sidebar overlay is closed. Harness helpers now explicitly open/close the overlay when scenarios need session cards/settings vs. chat controls. Fixed a follow-up where hovering the sidebar backdrop inherited the global `button:hover` background and visually blanked the chat area.
- Mobile sidebar follow-through: the hamburger drawer now reuses the same recency-grouped session list, keeps Today/Yesterday expanded and older groups collapsed by default, omits the desktop-only Pin affordance, renders a backdrop while open, and keeps session creation/groups ahead of lower-priority settings in the drawer. `mobile-layout` now asserts these drawer details. Mobile initial render also ignores persisted desktop-open/pinned sidebar state so the transcript shell paints first with the temporary drawer closed, while mobile drawer interactions preserve the desktop pin preference.
- Expanded tool-output ergonomics for long-running bash-style commands: tool result text now renders as terminal-like preformatted output while preserving Markdown image payloads, expanded tool bodies are capped to an internal scroll viewport instead of dominating the full transcript, running tool output pins to the bottom while streaming, and the fake-agent narrow tool harness now emits/asserts long terminal output.
- Added safe local image artifact rendering for workspace-relative paths in transcript text/tool output: `GET /api/sessions/:id/files/raw?path=...` serves image previews after session/workspace realpath checks, transcript rendering adds thumbnail cards for detected image paths, and the fake-agent harness now covers an `image-artifact-paths` scenario.
- Tuned compact successful-tool rows to fade further into background activity: repeated completed tools pack more tightly, done badges are quieter, and compact summaries skip boilerplate like `running fake tool...`, stdout labels, and successful exit-code lines while noting long output line counts.
- Adjacent completed non-image tool rows now render as a tighter visual activity group; live and snapshot hydration drop assistant messages that only repeat tool-call metadata, carry their labels onto corresponding generic tool-result rows, and merge obvious duplicate adjacent tool-result rows when they repeat the same successful command output.
- Removed cross-message suppression for local image artifact thumbnails after dogfooding showed repeated screenshot paths could be hidden even when the current message clearly referenced them; each message can now render its own artifact previews.
- Added `repeated-image-artifact-paths` and `artifact-path-formats` Playwright regression scenarios to cover repeated local screenshot paths across assistant messages plus inline-code, fenced-code, and long nested `test-results/.../*.png` artifact path shapes; both are included in the full fake-agent scenario set.
- Added a TUI-like context usage indicator inline with the web composer mode label showing estimated used tokens vs. the active model context window, including percentage and a small progress bar. The indicator is sourced from pi `AgentSession.getContextUsage()`, updates after completed turns, and the fake-agent harness asserts it via a `context-usage` scenario.
- `/new` now works as a web slash command: typing exactly `/new` creates and opens a fresh empty session in the current session's workspace, keeps the previous session running if needed, blocks when prompt images are attached, leaves invalid `/new ...` drafts intact with usage copy, and appears as supported in slash-command autocomplete.
- The UI harness now captures backend/Vite child process output to `server.log`/`web.log` artifacts instead of streaming it to stdout by default, keeping `bun run test:web-perf` concise for agent context; use `PI_WEB_HARNESS_CHILD_LOGS=1` or `--verbose` to stream logs while debugging.
- `/new` now keeps keyboard flow intact by focusing the prompt textbox in the newly opened session once its WebSocket snapshot/controller state is ready; slash-command harness coverage asserts this focus behavior.
- Queued running-control chips now support canceling individual steering/follow-up messages, editing a queued item back into the composer, and sending screenshots with queued steer/follow-up messages. The queue now renders as a visually distinct “Queued for this run” strip over the bottom of the transcript, above the running-input footer, with image-count badges for queued attachments and compact overflow behavior that shows the first three items plus a `+N more`/`Show less` toggle. The backend exposes typed queued-image payloads plus `cancel_queued_message`; real sessions remove items by clearing/requeueing remaining SDK queues while preserving web-queued images, fake-agent sessions mirror the behavior, and the harness covers mixed steer/follow-up/image edit/cancel/overflow flows.
- Added canonical web session title/summary metadata plumbing: persisted title/summary provenance, migration/source inference for existing titles, manual title protection, derived fork metadata, `/name` canonical web-title handling (`/name`, `/name <title>`, `/name --clear`) with pi-name mirroring only for manual titles, WebSocket `session_metadata_update`, header title edit preservation, generic-first-prompt handling that leaves “what's next?” sessions as provisional “New session,” summary chrome collapsed by default, sidebar summary previews, app metadata model setting storage, and a user-triggered ✨ title+summary suggestion flow. Automatic background generation is intentionally deferred/disabled to avoid surprise token spend; generation currently uses a deterministic heuristic/fake path rather than a real model-backed SDK call.
- Added agent commit-hygiene guidance in `AGENTS.md`: prefer concise Conventional Commit messages and one coherent vertical-slice commit when possible.
- Fixed session metadata suggestion apply failures from the browser by allowing CORS preflight for `PATCH` (and other non-GET methods) on the API server.
- Added dedicated fake-agent session metadata harness coverage for generic first prompts, header title edits, `/name`, `/name --clear`, explicit ✨ title-only suggestions, suggestion apply, summary expand/collapse, and sidebar summary previews. Also fixed metadata-update merging so enriched `lastUserPrompt` survives WebSocket metadata broadcasts, adjusted header stacking so summary toggles remain clickable, preserved prompt drafts synchronously on reload, and restored the full fake-agent harness pass including transcript scroll stability.
- Added a `"What's next?" dev loop` section to `AGENTS.md` so future planning prompts re-read `PROJECT_LOG.md`, propose the top slices, recommend one vertical slice, and state scope/validation before implementation.
- Wired real model-backed explicit session metadata generation for non-fake-agent sessions: ✨ now selects the configured metadata model (falling back to current/default/available authenticated model), asks for strict JSON title+summary suggestions outside the transcript/session tree, keeps fake-agent deterministic with the title-only heuristic, and increments generation metadata only for non-deferred suggestions. Removed the metadata-only temperature option after real Codex/OpenAI-compatible providers rejected it, and route metadata generation errors through a cleaner 502 response instead of a raw Fastify 500. Tightened the ✨ button so it sits next to the title text and renders smaller in the header. Polished the suggestion panel with clearer title/summary labels, less alarming model/reason copy, compact action buttons, a Regenerate action, and friendlier generation error text.
- Refined transcript/tool-output ergonomics: expanded tool output viewports use responsive clamp sizing, running tools get a little more vertical room while staying internally scrollable, terminal output wraps less aggressively, and inline/local image previews have responsive thumbnail and expanded sizing with smaller tool-card thumbnails.
- Tightened compact successful-tool rows again: summaries now strip ANSI/control noise and boilerplate, use first+last meaningful lines for long output, focused harness coverage asserts noisy labels stay out of collapsed summaries, and completed tools no longer auto-expand just because their text mentions local image artifact paths.
- Added a higher-level completed-tool activity group: adjacent successful non-image tools now collapse into one `Ran N tools` card that expands to reveal the individual compact tool rows, reducing the vertical “barcode” effect during tool-heavy agent turns. The fake-agent runner and Playwright harness now cover multi-tool grouping. Follow-up visual hierarchy tuning moved tool activity groups and their nested compact rows toward neutral/slate tones so they are easier to distinguish from green assistant conversation cards, then added more transcript/card spacing so action boundaries are clearer.
- Added a first attention-hierarchy pass for active runs and user-input-needed states: active run detail now appears inline in the composer mode row next to `Running input` instead of as a large transcript overlay, completed tool history remains grouped/subordinate, historical selection outlines are quieter, and a separate attention-needed banner is reserved for actionable states like controller handoff, viewer mode, disconnect/retry failure. Fixed the remaining successful-tool expansion bug by keeping completed done tool rows collapsed even when selected; only running/error rows auto-open.
- Added the first Workbench theme foundation: dark/light CSS tokens, a persisted System/Workbench Dark/Workbench Light selector in the sidebar, system color-scheme syncing, broad replacement of the existing hard-coded palette through token aliases, and fake-agent theme harness screenshots for both modes.
- Removed the unexplained green conversation/tool backgrounds from the primary transcript hierarchy: assistant messages are neutral, user messages are blue/info-tinted, tool activity is slate/neutral with success green reserved for small status text, and both themes now use more modern accent/depth tokens for layered cards and transcript surfaces.
- Suppressed empty assistant transcript placeholders so a running turn with only active tool cards/status does not show a large blank assistant rectangle before text arrives.
- Polished local image artifact cards/captions for light mode: theme-aware card/caption surfaces, better contrast, softer radius/shadow, and split path captions with the filename emphasized while preserving full-path title/tooltips.
- Fixed the light-mode running composer footer so the bottom bar uses theme-aware light surfaces/blue border/shadow instead of the previous dark running gradient.
- Fixed the queued follow-up/steer strip in light mode by moving its dark amber gradient, pill backgrounds, borders, and labels onto theme-aware queue tokens.
- Clarified completed tool cards that appear immediately after a running tool: they now render as dashed, muted “also ran” background activity instead of looking like unexplained output attached to the active command.
- Added a small theme audit pass for remaining dark overlays: autocomplete popovers, inline tool/image chips, session summary/suggestion panels, and app settings now use theme-aware surface/border/shadow tokens instead of hard-coded dark translucent backgrounds.
- Added a dedicated `theme-gallery` fake-agent harness scenario that captures Workbench Dark/Light screenshots with user/assistant messages, local image artifacts, grouped completed tools, an active running tool, queued follow-up strip, and running composer footer visible together for faster theme regression review.
- Added first-class web Q&A plumbing: a bakery-owned `ask_question` SDK custom tool for real sessions, shared pending-question/answer WebSocket protocol, transient per-session pending question state that survives browser reconnects while the backend stays alive, controller-only `answer_question` handling, a sticky composer-area “Answer needed” panel with options/custom answer/cancel and display-only recommendations, fake-agent question simulation, and `question-answer` Playwright coverage for option answer, cancel, and reload/reconnect preservation.
- Added the first bundled workflow launcher for dogfooding `ask_question`: `/grill-me` appears in slash autocomplete as a skill-style command, accepts an optional focus, expands to a codebase-interview prompt that tells the model to inspect project context and ask one recommended question at a time via `ask_question`, and is covered through the slash-command fake-agent harness path.
- Dogfooded `/grill-me` in a real session: the model used `ask_question` correctly. Follow-up UX improvements now make recommended answer options visually explicit, focus the question panel when a new question arrives, and support arrow-key/Home/End navigation across options before pressing Enter.
- Answered `ask_question` interactions now leave durable compact transcript context: completed/cancelled question tool results derive a quiet Q&A summary row showing the question and chosen/custom/cancelled answer, while the original tool card remains available for details/debug. Snapshot hydration also derives the same summaries so answered questions stay scannable after reload.
- Fixed a sporadic blank assistant transcript card seen during real dogfooding: non-renderable assistant events are now removed from transcript state and live DOM patches remove any existing row instead of updating it into an empty `Pi` card.
- Added explicit recommended-option plumbing for `ask_question`: the tool schema and shared pending-question protocol now accept `recommendedOptionIndex`, fake-agent questions populate it, and the web UI uses it for option highlighting/focus with the previous recommendation-text heuristic as fallback. The Q&A harness now captures pending-question screenshots before answering so artifacts actually show the recommended-option highlight and arrow-key navigation state, and running `ask_question` tool cards are collapsed by default while the sticky question panel is active instead of showing duplicated/raw question args. When an explicit recommended option is present, the sticky question panel now shows the recommendation only on the option badge/card and suppresses the redundant top recommendation line. Keyboard handling now supports number-key option submission and `c` to focus the custom answer even if focus takeover missed the panel, with visible keycap hints on option cards, the custom-answer row, submit button, and footer hint text. Question options now render vertically so Up/Down navigation matches the layout and option cards have more room for richer content.
- Fixed completed read/image tool cards expanding by default just because they contain image results or local artifact paths; visual tool results now auto-open only while running/error, keeping completed image reads as compact activity unless selected/expanded.
- Continued keyboard-first Q&A polish: pending question panels now advertise and support `Esc` cancellation, focus the panel when controller controls are disabled, guard answer/cancel sends to controllers with an active connection, return focus to the prompt textbox after option/custom/cancel answers, and explain that keyboard answer shortcuts are disabled in viewer/disconnected states. The Q&A harness now covers `Esc` cancel, prompt focus restoration after answers, focus takeover after reload, Tab movement through options, viewer shortcut no-ops, and Workbench Dark/Light screenshots for controller and viewer-disabled pending-question states.
- Modernized the pending Q&A composer panel to match the newer neutral composer/top-bar design language: options now render as clearer decision cards, recommended choices use calmer accent treatment, the custom answer row is secondary, desktop keeps keyboard shortcut hints, and mobile/narrow layouts replace those hints with touch-first guidance. Initial real-session dogfooding reported the updated question UI looks good.
- Fixed live transcript patch ordering for tool rows: patch updates now find rows nested inside completed-tool groups, newly patched rows insert according to transcript order instead of always appending, and manual expand/collapse preserves group positioning so expanding a lower tool row no longer appears to “fix” a weird order.
- Fixed pending Q&A Enter handling: focused/recommended option cards now answer on `Enter`/Space via explicit keyboard handling instead of relying on browser button activation, with focused harness coverage. Also made `/new` prompt focus restoration deterministic after the full-suite slash-command scenario exposed a race.
- Added a typed bundled workflow-skill registry and moved `/grill-me` metadata/prompt generation into it, so real and fake runners share the same launcher plumbing while preserving current slash-command behavior.
- Fixed completed markdown-card text selection being cleared by row click handling: transcript rows now preserve browser text selection after drag/highlight gestures instead of selecting the row and triggering a full render, with focused Playwright coverage.
- Renamed the bundled planning workflow launcher from `/grill-me` to `/plan`, tightened its generated prompt around the repository “what's next?” loop, one-question-at-a-time `ask_question` behavior, selectable recommendations, and final validation-plan handoff, and added prompt assertions to `bun run check`.
- Tightened `/plan` again to follow the canonical `grill-me` skill more closely: it now frames planning as a relentless design/codebase interview, walks decision-tree branches one-by-one, explores discoverable codebase/project-note answers instead of asking the operator, requires a recommended answer for each question, and keeps the final handoff focused on the smallest implementable slice.
- Compact workflow launch prompts in the transcript: `/plan` still sends the full planning instructions to the agent, but user-visible transcript rows now show a short “Launched /plan workflow” summary with focus instead of the full generated prompt block; raw details remain available in the inspector JSON.
- Extended workflow launch compaction to session/sidebar snippets and session tree labels, and added slash-command harness assertions that `/plan` stays compact outside the main transcript too.
- Added a local iteration telemetry JSON generator (`bun run report:iteration`) that aggregates recent git commit churn, `PROJECT_LOG.md` verification command mentions, and `test-results/ui-harness/**/metrics.json` artifacts into `test-results/iteration/iteration-report.json` for choosing speed-of-iteration improvements from evidence.
- Extended the iteration telemetry script with a validation selector (`bun run report:iteration --recommend <changed files...>`) that maps changed files to focused check/harness commands plus optional full-suite escalation guidance.
- Added an agent action insights mode (`bun run report:iteration --agent-actions`) that turns current telemetry into agent-behavior optimization guidance: validation selection, high-churn edit surfaces, recurring harness loops, and an explicit missing per-tool telemetry gap.
- Added agent iteration telemetry guidance to `AGENTS.md` and root check assertions so future agents are directed to use `bun run report:iteration --agent-actions --recommend` for iteration/agent-behavior planning and `bun run report:iteration --recommend <changed files>` before validation selection.
- Made validation selection more handoff-ready: `bun run report:iteration --recommend ...` now prints a copyable `## Validation decision` block with changed files, commands, optional escalation, and explicit full-suite run/skip guidance; `AGENTS.md` asks future agents to include that decision for non-trivial validation choices.
- Added `bun run report:iteration --session-context`, which reads local pi JSONL session logs, reports per-tool call counts/result payload sizes, largest tool-result contributors, and latest model usage without printing tool contents; use `--session <path>` to inspect a specific log.
- Refined session-context reporting with offline tool-input summaries: top read paths, top bash commands, repeated tool inputs, largest-result input labels, and actionable recommendations for repeated/large context contributors.
- Extended session-context reporting into a fuller current-session workflow telemetry view: validation command summaries with rerun/failure counts and rough durations, edit/write attempt success/failure counts by file, largest tool-result durations, and recommendations for validation reruns, edit retry loops, and mobile-layout artifact handoffs. Added `bun run report:iteration --session-history` to backfill all available local JSONL session logs into aggregate tool/validation/edit/context trends without printing raw tool contents. Added the project-local `.pi/skills/iteration-observability` skill so pi agents can explicitly load the telemetry workflow and turn the data into process recommendations.
- Added bounded recent-history iteration telemetry: `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session` now scans only the latest N local JSONL logs while skipping the current/latest session, and the iteration-observability skill documents this recent-history workflow. The same report now prints focused harness artifact pointers for repeated failed scenarios so agents can inspect `failure.txt`, logs, and screenshots before rerunning.
- Added `bun run project:notes`, a compact project handoff command that summarizes the design goal, recent status, latest verification, and next priorities while capping long `PROJECT_LOG.md` history; `AGENTS.md` now points agents to it before broad log reads.
- Started the architecture-review/iteration-speed refactor by extracting transcript rendering/model helpers and the `<pi-transcript-row>` custom element from `apps/web/src/main.ts` into `apps/web/src/transcript.ts`, with shared HTML/path/perf utilities in `apps/web/src/utils.ts`; this is intentionally move-only to reduce future main-file edit blast radius without changing behavior.
- Continued the iteration-speed refactor by extracting autocomplete token/render helpers and state types into `apps/web/src/autocomplete.ts`, adding focused Bun tests in `apps/web/src/autocomplete.test.ts`, and keeping the app-shell API/timer/focus wiring in `apps/web/src/main.ts`.
- Continued the architecture-review/iteration-speed pass by extracting pure session-tree flattening/path/render/fork-match helpers into `apps/web/src/session-tree.ts` with focused Bun tests in `apps/web/src/session-tree.test.ts`, while leaving DOM focus, API navigation/forking, and drawer state in `apps/web/src/main.ts`.
- Removed redundant connection banner copy, then made the connection row contextual: plain connected sessions no longer spend a full-width row, while reconnect/disconnect/retry and draft/image-refresh warnings still surface there.
- Added a focused `connection-disconnected` UI harness scenario that stops the fake backend and captures the contextual reconnect/disconnect banner for screenshot review.
- Fixed workspace-relative Markdown image links in assistant transcript text so they are rewritten through the safe session raw-file endpoint instead of rendering as broken browser-relative images; bare local image paths still render artifact cards.
- Reduced transcript image layout jitter by reserving stable image/card geometry for Markdown, rendered inline, and artifact images; artifact thumbnails keep a fixed thumbnail height until explicitly expanded.
- Fixed the new/empty-session composer layout regression seen during dogfooding: the prompt textarea can no longer be vertically resized enough to squeeze the empty transcript, and the send/follow-up controls stay bottom-aligned instead of stretching to the textarea height.
- Fixed the real empty-session layout regression behind that symptom: optional connection/attention banners now occupy explicit grid rows so the transcript remains the flexible row and the footer stays compact; hidden idle controls are truly hidden, prompt width no longer overlaps controls, and a focused `empty-session-layout` harness scenario guards the layout.
- Added LAN dev entry points: `dev:server:lan` binds the API on `0.0.0.0`, `dev:web:lan` binds Vite on `0.0.0.0`, custom hostnames can be allowed with `PI_WEB_VITE_ALLOWED_HOSTS`, the browser default API base now follows the page hostname, and `docs/local-network.md` documents token-protected LAN setup. The single-command `bun run dev:lan` now restarts the managed backend in LAN mode and starts the LAN Vite frontend.
- Added first remote-shaped screenshot artifact support: transcript image detection now recognizes absolute workspace paths and `file://` Markdown image URLs, the browser rewrites only paths under the selected session workspace to the existing safe raw-file endpoint, and a new `remote-image-artifact-paths` fake-agent harness scenario covers absolute + `file://` references without direct browser `file://` loads. The UI harness also clears inherited `PI_WEB_AUTH_TOKEN` so local auth env does not break isolated fake-agent runs.
- Added the first explicit remote screenshot artifact upload bridge: shared protocol schemas, `POST /api/sessions/:id/artifacts` for base64 image uploads, `GET /api/sessions/:id/artifacts/raw?path=...` for stable per-session previews from `PI_WEB_ARTIFACT_DIR`, browser fallback from remote absolute/`file://` paths to the artifact endpoint, and a `remote-image-artifact-upload` fake-agent harness scenario that pre-uploads a remote-only screenshot path and verifies transcript previews use `/artifacts/raw`.
- Added a practical remote-side upload helper, `bun run artifact:upload -- --session <id> --api <url> --path <remote-path> <image-file>`, plus `docs/remote-artifacts.md`; the harness now uses that CLI in `remote-image-artifact-upload` so the operator path is covered, not just the HTTP endpoint.
- Spiked/fixed the screenshot jitter/404 loop: failed transcript images now notify the app, failed URLs are suppressed for a short TTL and clear the rendered-segment cache, local/`file://` Markdown images no longer fall back to direct browser `file://`/relative loads when the safe resolver rejects them, and a `missing-remote-image-artifact` harness scenario guards against repeated failed-image rerenders.
- Composer image intake now has one automatic behavior instead of a Prompt/Artifact mode toggle: drag, paste, or paperclip always attaches supported images to the outgoing prompt so the agent receives image bytes, and also uploads session-scoped transcript artifact previews when a session is open, inserting `.bakery/artifacts/...` paths that render through `/artifacts/raw`. The `image-artifact-drop-upload` harness covers both the user-message image payload and artifact preview while `image-attachments` guards the paperclip path. Follow-up hardening made the drop zone accept file drags even when the browser/OS does not expose `image/*` during `dragover`, snapshots the live `FileList` before the file input is cleared, infers image MIME types from common image extensions when needed, renders prompt thumbnails before artifact upload work, surfaces visible attachment/upload failure notices instead of silently doing nothing, and switched the harness to Playwright's real file chooser path with an artificial picker delay plus a forced app rerender while the chooser is open. The paperclip uses a transient per-click file input outside the rerendered app template, so unrelated rerenders cannot replace the input that owns the native picker selection; the temporary Image debug readout was removed after real-browser diagnostics identified and fixed the `crypto.randomUUID` compatibility issue. The Fastify body limit now matches the intended artifact upload path, avoiding noisy `413 FST_ERR_CTP_BODY_TOO_LARGE` notices for normal screenshot-size base64 JSON uploads.
- Added a focused long tool/image-heavy transcript performance spike: the fake-agent runner can emit 96 alternating assistant/tool rows with repeated screenshot paths, `tool-image-heavy-transcript` measures post-growth responsiveness plus render/patch/row-update p95 timings, and collapsed transcript rows now defer expensive body HTML/image DOM until expanded. The focused scenario produced 96 tool rows with 39 visible artifact images, no long tasks, max render 19ms, and post-growth interaction latency under 75ms on the local harness.
- Tested the optimization against the real `Fix Screenshot Attachment Flow` session (`8a356ee5-d7c6-44b9-93d6-505b2634acd2`): 142 transcript rows, 108 tool rows, 102 collapsed rows, 95,636 transcript chars, open/render about 621ms, no long tasks, prompt fill 12ms, but full-app controls still showed cost (`toggleRightPanel` 671ms, `showThinking` 351ms, render p95 314ms). This points to the next full performance spike: decouple chat/transcript rendering from the rest of the app shell so inspector/session chrome/settings changes do not rehydrate or traverse transcript rows.
- Started that shell/transcript decoupling spike by splitting transcript structure dirtiness from full-app renders: completed-tool grouping/removal now rebuilds only the transcript section shell and hydrates row elements instead of replacing the whole app shell, while true snapshot/question/session chrome changes still use full render. The focused `tool-image-heavy-transcript` harness stayed responsive (latest max post-growth action latency 71ms, no long tasks, patch max 10ms). Follow-up fixed the focused `slash-commands` regression by keeping system slash-command result rows open by default instead of collapsing their bodies.
- Added the first mobile layout pass from dogfooding feedback: narrow screens now use a hamburger-triggered session drawer with sessions/new-session before lower-frequency settings, omit the right inspector rail entirely, and hide mobile Details/Preview message actions while keeping desktop inspector behavior and `/tree` drawer support intact. The mobile harness now asserts no side rails, session-first drawer order, inspector detachment, and the compact mobile context-usage pill in the lower composer bar.
- Reworked explicit title/summary suggestion UX into a shared editable model for desktop and mobile: ✨ suggestions now populate editable title/summary fields with per-field ✓ apply and × discard controls; desktop keeps the inline metadata panel while mobile shows the same controls in a trigger-adjacent top popover so generation progress/errors/actions remain visible even though saved summary chrome is hidden on narrow screens. Harness coverage now edits/applies the suggested title on desktop and asserts the mobile popover is visible near the ✨ button.
- Tightened the regular dev-loop validation guidance from iteration telemetry: `bun run report:iteration --recommend ...` now prints a focused-first strategy, stop-on-first-failure copy, and explicit full-suite `RUN`/`ESCALATE ONLY`/`SKIP by default` guidance; `AGENTS.md` and the iteration-observability skill now treat full `bun run test:web-perf` as an escalation instead of the default for every UI-adjacent change.
- Added `docs/codebase-efficiency-audit.md`, a codebase organization and iteration-efficiency spike that captures telemetry-backed hotspots (`apps/web/src/main.ts`, `scripts/ui-harness.ts`, `apps/server/src/index.ts`, validation reruns, and context-heavy logs), operating principles, and ranked multi-session follow-up slices for future refactors.
- Continued the iteration-speed/main-file refactor by extracting session-sidebar recency grouping, persisted group-collapse helpers, and session-card rendering from `apps/web/src/main.ts` into `apps/web/src/session-sidebar.ts`, reducing the main component surface without changing sidebar behavior.
- Fixed a mobile transcript jump/flicker where browser viewport/layout resize scroll events could disable follow-latest without a user scroll gesture, leaving the top of the current chat card clipped with the Jump-to-latest button visible. Transcript auto-follow now only turns off after wheel/touch user intent, and visual viewport resizes keep following the bottom.
- Added compact structured action summaries to `bun run report:iteration --session-context` and `--session-history`: tool call/result totals, validation runs/failures/reruns, edit attempts/failures, unique read/bash input counts, largest result/session sizes, and approximate event-span timing now appear before the detailed metadata-only sections. The iteration-observability skill now points agents at the new summary first while keeping raw prompts/tool outputs out of handoffs.
- Continued the main-file extraction dogfood slice by moving transcript follow/latest scroll state and Jump-to-latest patching into `apps/web/src/transcript-follow.ts`, keeping `main.ts` as the DOM/event coordinator while preserving the existing scroll behavior.
- Continued the small main-file extraction pass by moving running queue types, queue update/add/remove helpers, count predicates, and queued-chip rendering into `apps/web/src/running-queue.ts`; `main.ts` now keeps only WebSocket/composer coordination for queued steers/follow-ups.
- Added `bun run project:notes --handoff`, a read-only line-numbered handoff helper that points agents at the relevant `PROJECT_LOG.md` anchors, latest verification line, recent status bullets, top priorities, and end-of-session checklist without broad log reads.
- Fixed queued follow-up consistency during active runs: follow-up chips that leave the backend/pi queue now remain as dashed `Sending…` pending-transcript chips until the matching user transcript row appears, then the queue DOM is patched away immediately without requiring a full app render. The queued-follow-up fake-agent harness now simulates queue removal before transcript confirmation while also asserting the composer clears immediately, pending UI remains during the gap, and edit/cancel/overflow flows still work. Follow-ups removed the redundant `Session menu hidden/open` notice banner when toggling the hamburger/sidebar; the control itself communicates the state, and made the queued-message section an inline non-overlay panel that defaults collapsed on mobile so it no longer covers chat content.
- Tuned iteration telemetry for the latest 5-session review: `bun run report:iteration --session-context` and `--session-history` now print a `Rerun opportunity` block with repeated validation counts, repeated failed focused-harness counts, and a conservative avoidable-rerun target. Focused failed `ui-harness:*` loops now get stronger guidance to inspect the latest artifact directory/logs, patch one cause, and rerun only that scenario; the iteration-observability skill mirrors that stop/inspect/patch/rerun discipline.
- Fixed sidebar recency ordering when a session is reopened after its last transcript activity: WebSocket/session opens now touch `lastOpenedAt`, the sidebar sorts by the newer of `lastOpenedAt` and `lastActivityAt`, and cards display the same newest recency value so recently accessed sessions jump above older active sessions.
- Fixed duplicate pasted/attached screenshot previews in user transcript rows: the prompt image data URL still renders as the primary inline preview, while the generated `Screenshot artifact: .bakery/artifacts/...` helper path no longer adds a second artifact-card preview for the same prompt attachment.
- Made tool output expansion explicit and deterministic: tool/system rows now use a dedicated chevron button for inline output, row/header selection no longer toggles or auto-expands output, expansion state is owned by the app across rerenders, completed tool groups stay closed unless explicitly opened, and mobile tap targets for tool expansion/actions are larger. Updated the inspector-preview harness to assert header clicks keep completed tool output hidden before using the explicit output toggle.
- Continued the frontend iteration-speed extraction by moving transcript grouping/render-plan helpers, completed-tool group shell rendering, group-position calculation, running-tool adjacency, element ordering, and default row expansion predicates from `apps/web/src/main.ts` into `apps/web/src/transcript-renderer.ts`, with focused unit coverage so `main.ts` keeps DOM coordination while transcript render rules are easier to test.
- Split the monolithic `apps/web/src/styles.css` into ordered CSS modules under `apps/web/src/styles/` (`base`, `sidebar`, `layout`, `composer`, `transcript`, `right-panel`, `metadata`, and `mobile`) while preserving the exact existing cascade; `styles.css` is now only the ordered import list so UI styling changes have smaller edit surfaces.
- Continued composer/UI extraction by moving prompt image attachment helpers, MIME/path normalization, FileReader wrappers, prompt-image rendering, and focused helper tests into `apps/web/src/prompt-images.ts`; `main.ts` now keeps the upload/send coordination while image rules are separately testable.
- Extracted pure-ish composer draft/send helpers into `apps/web/src/composer-actions.ts`: session-scoped prompt draft and attachment warning storage keys, one-shot attachment-warning state, bash prompt parsing, prompt text fallback, WebSocket send payload construction, and queued-message payload shaping now have focused Bun tests while WebSocket sends/DOM binding stay in `main.ts`.
- Completed the planned three-commit `main.ts` extraction burst: transcript DOM row binding/live patch helpers now live in `apps/web/src/transcript-dom.ts`, composer DOM/event wiring and image picker coordination in `apps/web/src/composer-controller.ts`, and agent/session event normalization in `apps/web/src/session-events.ts`. `main.ts` dropped to about 2.5k lines and remains the app-state/WebSocket/render coordinator.
- Continued the open-source-readiness cleanup chain by moving composer image intake/upload/remove coordination, pending-question panel behavior/rendering, sidebar rendering helpers, and WebSocket/reconnect setup into focused web modules with unit coverage; `main.ts` is now about 2.7k lines and more clearly acts as the app-state coordinator.
- Dogfooding bash command blocking showed the idle-only error appeared too far from the text input; composer-scoped send/attachment notices now render inline in the prompt shell instead of as top-of-main app notices.
- Added focused unit coverage for `apps/web/src/session-events.ts`, including agent event type parsing, slash-command results, bash lifecycle normalization, message/tool transcript conversion, queue updates, and metadata update merging.
- Dogfooding bash drafts on mobile showed the composer still looked like normal running input; prompt drafts beginning with `!` now switch the composer label/hint and textarea styling into the local-bash color family, while `!!` no-context drafts and transcript rows use a distinct amber no-context variant.
- Replaced the noisy brand-new-session transcript card (`Session` / `No messages yet.`) with a quiet centered empty-state hint across desktop and mobile, so empty sessions no longer look like selected transcript content before the first prompt. Updated the empty-session and mobile harness waits to assert the new hint.
- Turned that empty-session state into workflow-discovery quick starts for pi browser dogfooding: `/plan`, screenshot, `@file`, and `!bash` chips explain the main Bakery affordances, and the text chips prefill/focus the composer for keyboard flow. Follow-up mobile dogfooding made the quick starts render as compact single-column rows on narrow screens and suppresses stale Jump-to-latest affordances for empty transcripts.
- Consolidated desktop and mobile model/thinking controls into one compact header picker backed by a small `model-thinking-picker` helper module. The trigger shows a shorthand model label plus non-default thinking level, opens separate Model/Thinking selects, respects controller/viewer disabling, and is covered by focused helper tests plus `model-thinking` and `mobile-layout` harness assertions.
- Replaced the experimental iMessage-style transcript bubbles with a ChatGPT-style conversation layout: assistant messages are content-first/plain with always-visible bottom actions, user prompts are compact right-aligned dark chips with hover/focus actions, image attachments render as separate compact cards with full-screen lightbox expansion, and conversation selection no longer draws blue boxes while tool/system/question rows keep their functional card styling.
- Reworked completed/background tool activity into a quieter indented activity-log treatment: tool lifecycle events now carry start/end/duration metadata into transcript items, grouped completed tools show elapsed wall-clock time from the group's first tool start while the agent is running (with a one-second UI tick) instead of summing/reporting only completed tool durations, individual rows show elapsed duration when expanded/collapsed, long tool labels/commands truncate to a bounded width with full titles available via tooltip, completed image-read/image-result tools now group with adjacent background tools instead of surfacing as standalone cards, completed tool groups/rows dropped the card chrome in favor of a thin rail, expanded completed tools inside groups now keep the log header and render only their output as a contained pane, running/failed agent tool rows now use blue/red rails plus contained output panes instead of full colored cards, user-initiated bash rows use a right-aligned rail/output treatment with the rail on the right edge, no redundant badge label, a macOS Terminal-inspired color-scheme output pane shared with agent terminal output, without fake titlebar chrome, ANSI SGR colors are rendered safely in terminal panes, and the existing no-context variant preserved, answered-question summaries now use a matching accent-rail note style instead of blue cards, inline assistant tool-call intent chips are suppressed so the activity log is the source of truth, stale action menus are cleaned during live transcript patches so multiple ellipsis dropdowns cannot remain open, and desktop system cards no longer stretch across the full transcript width while mobile keeps full-width readability.
- Removed noisy success/info app notice banners for screenshot attach/upload, copy actions, tree navigation, session title save/clear, queued-message edit-back, control requests, and sidebar pinning; these paths now rely on the visible UI state change while failures/blockers still surface notices.
- Moved prompt screenshot thumbnails above the composer mode row so adding images no longer separates/pushes the `Prompt` label away from the textarea.
- `/plan` final summaries now use a shared action marker for Accept plan and Back to chat; the transcript hides both the current two-action marker and legacy four-action marker. The web UI replaces the normal composer with a `Plan ready` takeover panel containing only Accept plan and Back to chat: Accept submits the implementation prompt immediately, while Back to chat simply dismisses the takeover and restores the normal composer.
- Added a focused `mobile-long-transcript-controls` harness scenario for the real-phone dogfooding report that long transcripts made button taps feel about a second behind. The scenario grows a 96-tool/39-image mobile transcript, resets app perf counters, then times representative drawer, model/thinking, composer, completed-tool expansion, and action-menu taps; the first local baseline stayed under 160ms max action latency with no long tasks, render max 21ms, and 500 capped row-update samples.
- Removed the full-app render from mobile hamburger drawer open/close: mobile sidebar/backdrop toggles now patch only the drawer DOM/root classes and preserve the existing transcript DOM instead of rebuilding the whole app shell. The mobile long-transcript harness now asserts hamburger open/close produce zero full renders and zero transcript row updates; the latest focused run measured drawer open/close at 34ms/13ms with `hamburgerPerf` `{ renderDelta: 0, patchDelta: 0, rowUpdateDelta: 0 }`.
- Fixed the follow-up regression from that mobile sidebar patch: session-card click handlers now bind as part of sidebar-local event wiring, so a patched mobile drawer can open an existing session. The `mobile-layout` harness now creates a second real session and selects back to the original from the drawer.
- Started the composer/transcript design-language alignment pass: the prompt shell is now a neutral rounded input dock with a quieter inline mode/meta row, compact transcript-aligned action buttons, calmer image thumbnails, and matching desktop/mobile treatment while preserving existing prompt, screenshot, running, autocomplete, and context-usage behavior. Follow-ups renamed Abort to Stop, kept context usage in the top-right mode row with a quieter progress bar, removed the low-value hint copy, swapped prompt-mode controls to SVG icons, centered the composer column while leaving transcript layout unchanged, fixed bash-mode label switching so it updates while typing instead of on blur, removed the old “Pi is working/responding” composer activity field, optimistically swaps to running controls immediately after prompt send, and moved controls into a ChatGPT-like bottom row inside the composer dock with compact tooltipped SVG buttons.
- Started the matching top-bar design-language pass: the header is now a quieter translucent identity strip with title/workspace/details hierarchy, summary/title generation moved behind a metadata-focused Details popover, and mobile keeps the header visible for stability. Follow-up strengthened the Details popover design language by making workspace and summary into rounded section cards with clearer labels, preview text, and calmer empty-summary copy, removed popover/card transparency so transcript content no longer bleeds through the details surface, and made saved summaries show immediately inside Details instead of requiring a second expand click. Model/thinking moved out of the top bar into a brain-icon composer control with the Show thinking preference inside its menu; the explicit Auto-scroll checkbox was removed so follow-latest is inferred from whether the user is at the bottom of the transcript. Follow-up portaled the mobile model popover to a fixed root overlay, anchored it from the tapped button geometry, and added a small pointer toward the composer button, avoiding iOS/grid stacking issues where transcript content could cover the menu. The Jump-to-latest affordance is now an icon-only down-arrow button with a neutral translucent blurred circular surface while keeping unread update counts in aria-label/title text. Running tool output now patches terminal text in place instead of rebuilding the whole row on every partial update, reducing streaming tool-call jitter. The mobile top-bar hide/reveal feature was removed entirely after dogfooding showed transcript height changes during streaming could still cause instability. Jump-to-latest now measures the queued-message strip and floats above it so the two controls no longer overlap. The header status pill now suppresses the low-value idle state and only appears for active/non-ready states; harness readiness waits now read app state instead of depending on a visible idle chip.
- Removed the controller/viewer/client-count label from the top bar. Multi-client presence now appears only in the composer bottom bar as a compact eye-icon viewer count, and the indicator is hidden when there are no additional viewers.
- Compacted the persistent mobile top bar after dogfooding showed it was taking transcript space again: narrow screens now keep only hamburger, editable title, and an icon-only Details affordance in the header; workspace/summary remain available in the Details popover, passive running/status pills are hidden from the mobile header in favor of composer-side state, desktop keeps the text Details button, the header surface/border are more solid to separate it from the transcript, and `mobile-layout` asserts the mobile workspace line/status row are hidden plus the Details control is icon-only.
- Added the first local Bun distribution/onboarding slice: root `README.md` now documents install, `bun run doctor`, local run, LAN run, commands, and core `PI_WEB_*` variables; `bun run doctor` checks Bun/deps, workspace/data dirs, port availability, host/token safety, and prints local/LAN URLs; `docs/local-network.md` now points LAN users through the doctor and the single-command `bun run dev:lan` path.
- Added the first opt-in session isolation slice: the sidebar can create “New isolated session” entries backed by managed Git worktrees under `PI_WEB_WORKTREE_DIR`, with per-session branch/source/base metadata persisted and surfaced in session labels/details. Isolated sessions now also get a compact persistent header chip that opens Details, including mobile coverage that avoids adding a banner or extra status row. Dirty source repositories warn that the worktree starts from `HEAD` and does not copy uncommitted changes.
- Started the sessions/navigation IA split: `/sessions` is now a dedicated find-and-resume page with search across title/summary/prompt/workspace/branch/status, while the sidebar/drawer is reduced to a navigation shell plus workspace/new-session/settings controls. Existing session detail routes remain `/sessions/:id`, and mobile drawer coverage now asserts the drawer does not duplicate the session list. Follow-ups made the sessions page content use one scroll container so the hero/search area scroll away with the session list instead of feeling sticky on mobile while the app top bar stays visible, removed the redundant Chat route button from the sidebar/drawer, and fixed same-session route navigation from `/sessions` back to chat rendering.
- Added the first dedicated `/settings` page and moved low-frequency app controls there: API base, token, theme, and metadata model. The sidebar/drawer now keeps workspace and new-session actions first, then links to Settings below session creation so navigation stays session-first on mobile. Follow-up tightened mobile settings spacing, compacted typography/cards, and fixed input/button box sizing so fields no longer overflow their cards.
- Reduced mobile transcript image jitter/flash during active runs: running completed-tool elapsed ticks now patch only the group summary text instead of rebuilding the transcript DOM every second, and mobile transcript images use reserved compact-card geometry plus async decoding for markdown, prompt-attachment, and artifact previews while preserving tap-to-expand.
- Follow-up mobile image stability fix preserves existing transcript row/image DOM across transcript structure patches and full app renders, skips identical row HTML rebuilds, and adds `mobile-image-stream-stability` harness coverage that holds a loaded mobile transcript image through a streaming multi-tool run.
- Clarified mobile running composer actions: while an agent run is active, the mobile footer now labels the guidance send action as `Guide` and the queued next prompt as `Follow up`, while keeping Stop as the compact square icon; prompt-sending actions now stay disabled/gray until the composer has text or attached images.
- Fixed delayed composer idle/running mode swaps by routing status changes through an immediate composer DOM patch: the footer running class, mode label/classes, textarea placeholder, Send/Guide tooltip/ARIA text, and Follow up/Stop visibility now update without waiting for a delayed streaming transcript patch or a blur/click-away render. `streaming-responsiveness` now asserts the composer DOM is already in running mode when app state becomes running and back in Prompt mode when the run returns idle.
- Added the first live tool activity stack pass: adjacent SDK tool calls now form one open running stack while active or failed, the stack shows only the five most recent tool receipts with older receipts faded upward, running and failed tool outputs stay collapsed by default, failed tools remain in-place with red summary/rail treatment instead of breaking into an expanded error wall, and completed all-success groups settle back to the existing collapsed group behavior. The UI harness settings bootstrap also force-collapses an initially open mobile sidebar overlay so focused scenarios can reliably reach Settings controls.
- Brought the reduced sidebar/drawer closer to the transcript/composer design language with tokenized elevated surfaces, softer overlay depth/backdrop, card-like navigation/workspace sections, updated focus/hover states, pill-like session/status chips, and mobile drawer shadow/radius alignment while preserving the existing navigation/session behavior.
- Added regression coverage for live grouped tool rows so their summary line stays consistent as `Ran N tools` and does not regress to the ambiguous `Tool activity` label while elapsed time/status badges update.
- Refined grouped tool-call display language toward quiet receipts: live groups now lead with the current running tool name plus count/elapsed metadata, mobile/narrow layouts render live groups compact by default while desktop keeps recent receipts open, completed groups show only a minimal count/duration/primary-tool receipt, and failed completed tools no longer promote red summary/error treatment unless the user expands details.
- Stabilized live grouped-tool rendering for performance and consistency: the first running tool now renders through the same live group component, active group ids stay anchored to the first tool instead of changing as tools append, dirty tool groups patch/insert only their local `<details>` region and existing row elements instead of replacing the transcript structure, and the narrow tool-stream harness now tracks render/patch/row-update counts during tool streaming.
- Reworked grouped tool activity into a stable tool-run card on desktop and mobile: the top row shows elapsed/count metadata, the middle row shows the current/last tool, and a bottom-right Details button reveals individual collapsed tool-call rows only on demand. Tool output remains unrendered until both the run card and the individual tool row are expanded, durations use whole seconds with no sub-10s ms/decimal noise, completed groups use stable per-tool duration data when available, and Q&A ask_question tools remain outside tool-run grouping so decision panels stay visible.
- Added `docs/extensions-design.md`, a Bakery extension architecture draft that keeps pi SDK extensions as the agent/runtime layer while defining trusted local Bakery extension points for web slash commands, backend actions, typed browser UI slots, Web Component authoring, reusable UI primitives, `/plan` as the reference extension, testing, reload/dev-loop expectations, and a phased path from bundled extension registry to trusted local extension loading.
- Started the backend open-source-readiness split by extracting artifact/file-preview route handling from `apps/server/src/index.ts` into `apps/server/src/artifact-routes.ts`, keeping upload/raw-preview behavior and workspace containment checks unchanged while shrinking the server entry point.
- Continued the backend open-source-readiness split by moving session metadata generation/update routes, slash-command/file-search routes, session/tree/fork REST routes, and the WebSocket session hub into focused backend modules. `apps/server/src/index.ts` is now about 114 lines and mainly owns app bootstrap, auth/config/settings routes, and route registration while `apps/server/src/session-hub.ts` owns WebSocket lifecycle/controller handling.
- Completed a four-commit behavior-preserving `main.ts` deslop chain: transcript row actions, composer mode/send patching, session shell event binding, and session status/banner rendering now live in focused web modules. `apps/web/src/main.ts` is down to about 2.3k lines and remains the app-state/render coordinator.

## How to run

Default local dev loop:

```bash
bun run dev
```

This ensures the backend is running in the background, then starts Vite in the foreground. Restart only the backend without touching Vite/browser state:

```bash
bun run dev:server:restart
bun run dev:server:logs
bun run dev:server:down
```

The older two-terminal foreground loop still works when desired:

```bash
PI_WEB_WORKSPACE_ROOT="$PWD" bun run dev:server
bun run dev:web
```

Open `http://127.0.0.1:5173/`. The API URL should be `http://127.0.0.1:3141`. Leave token blank unless `PI_WEB_AUTH_TOKEN` is set. For first-run/local readiness checks, use `bun run doctor`.

For LAN/Tailscale access, use the LAN IP printed by `bun run doctor --lan` or configure your own DNS/hosts entry, run `PI_WEB_AUTH_TOKEN=... PI_WEB_WORKSPACE_ROOT="$PWD" bun run doctor --lan`, then run `PI_WEB_AUTH_TOKEN=... PI_WEB_WORKSPACE_ROOT="$PWD" bun run dev:lan`. Set `PI_WEB_VITE_ALLOWED_HOSTS` when using custom hostnames. See `docs/local-network.md`.

## Verification

```bash
bun install
bun run check
bun run test:web-perf
bun run ui:manual
curl http://127.0.0.1:3141/healthz
```

Latest: removed the overbuilt grouped tool-activity card architecture in a four-commit cleanup: transcript rendering now emits flat `pi-transcript-row` shells for running, completed, failed, image, and developer-bash tools; the app no longer tracks grouped activity expansion ids or runs a 250ms grouped receipt patch timer; running tool rows use a simple CSS-only spinner; and the tool-focused UI harnesses now assert row-level behavior instead of wrapper/card selectors. Validation used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-live-controller.ts apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript-renderer.test.ts scripts/ui-harness/scenarios/transcript.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-30T14-24-29-463Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-30T14-24-34-725Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-30T14-24-36-800Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-30T14-24-39-584Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-30T14-24-41-838Z/`), plus tool-specific `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-30T14-24-46-604Z/`) and `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-30T14-25-18-853Z/`) after one alignment fix. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: completed the requested four-commit behavior-preserving `main.ts` deslop chain: `apps/web/src/autocomplete-controller.ts`, `apps/web/src/plan-action-controller.ts`, `apps/web/src/running-queue-controller.ts`, and `apps/web/src/settings-panel.ts` now own file/slash autocomplete orchestration, plan accept/back-to-chat behavior, running queue controls/patching, and settings/session-detail rendering. `apps/web/src/main.ts` dropped to about 2.0k lines. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/autocomplete-controller.ts apps/web/src/plan-action-controller.ts apps/web/src/running-queue-controller.ts apps/web/src/settings-panel.ts`, `bun run check`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T23-43-42-722Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T23-43-47-624Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T23-43-49-695Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T23-43-52-471Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T23-43-54-850Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: completed the agreed behavior-preserving `main.ts` deslop chain across four commits: `apps/web/src/transcript-row-actions.ts`, `apps/web/src/composer-mode-controller.ts`, `apps/web/src/session-shell-events.ts`, and `apps/web/src/session-status-controller.ts` now own transcript action orchestration, composer mode/send patching, shell/sidebar/settings event binding, and status/banner rendering respectively. `apps/web/src/main.ts` dropped to about 2.3k lines. Validation used `bun run report:iteration --recommend ...` for each focused pair, `bun run check`, and selector-focused harnesses after each commit: `streaming-responsiveness`, `slash-commands`, `question-answer`, `inspector-preview`, and `transcript-scroll-stability` (latest artifact sets: `test-results/ui-harness/streaming-responsiveness-2026-04-29T23-03-16-469Z/`, `test-results/ui-harness/slash-commands-2026-04-29T23-03-21-691Z/`, `test-results/ui-harness/question-answer-2026-04-29T23-03-23-764Z/`, `test-results/ui-harness/inspector-preview-2026-04-29T23-03-26-523Z/`, and `test-results/ui-harness/transcript-scroll-stability-2026-04-29T23-03-28-868Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's focused-first escalation guidance after targeted checks passed.

Previous latest: completed the higher-risk backend open-source-readiness split by extracting WebSocket session lifecycle/controller handling from `apps/server/src/index.ts` into `apps/server/src/session-hub.ts`. The new registry owns hub creation, metadata broadcasting, session deletion disposal, `/api/sessions/:id/ws` registration, and shutdown cleanup while preserving existing prompt/bash/controller behavior; `apps/server/src/index.ts` dropped to about 114 lines. Added focused Bun coverage in `apps/server/src/session-hub.test.ts` for `/name` parsing and image data URL boundary parsing. Validation used `bun run report:iteration --recommend apps/server/src/index.ts apps/server/src/session-hub.ts apps/server/src/session-hub.test.ts`, `bun test apps/server/src/session-hub.test.ts`, `bun run check`, and selector-focused lifecycle/slash harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-29T19-07-53-311Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-29T19-08-10-619Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-04-29T19-08-18-099Z/`), and `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T19-08-23-526Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's focused-first guidance after targeted checks passed.

Previous latest: completed an ambitious transcript cleanup chain that moved transcript item state/dirty tracking/tool timing into `apps/web/src/transcript-controller.ts`, agent-event-to-transcript orchestration into `apps/web/src/transcript-event-controller.ts`, transcript-preserving live DOM patch helpers into `apps/web/src/transcript-live-controller.ts`, and empty-transcript/plan-composer transcript shell rendering into `apps/web/src/transcript-shell.ts`. `apps/web/src/main.ts` is down to about 2.45k lines and is more focused on app coordination. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript-controller.ts apps/web/src/transcript-event-controller.ts apps/web/src/transcript-live-controller.ts apps/web/src/transcript-shell.ts apps/web/src/transcript-controller.test.ts`, `bun run check`, `bun test apps/web/src/transcript-controller.test.ts apps/web/src/transcript.test.ts apps/web/src/transcript-renderer.test.ts`, and selector-focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T15-58-59-832Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T15-59-04-951Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T15-59-07-019Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T15-59-09-748Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T15-59-12-104Z/`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after targeted checks passed.

Previous latest: UI harness scenario-family split moved shared Playwright helpers into `scripts/ui-harness/scenarios/helpers.ts`, moved scenario bodies from `scripts/ui-harness/scenarios/index.ts` into family modules (`session`, `mobile`, `transcript`, `slash-commands`, `lifecycle`, `artifacts`, and `visual`), and left `index.ts` as the stable dispatcher/threshold surface so `bun scripts/ui-harness.ts --scenario <name>` and `bun run test:web-perf` keep the same CLI shape. Validation used `bun run report:iteration --recommend scripts/ui-harness.ts scripts/ui-harness`, `bun run check`, selector-focused harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T15-06-01-836Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T15-06-48-094Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T15-06-54-231Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T15-07-00-617Z/`), plus direct family smoke coverage for `image-artifact-paths` (`test-results/ui-harness/image-artifact-paths-2026-04-29T15-08-02-546Z/`) and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-29T15-08-06-307Z/`). `bun run test:web-perf` was escalated because the dispatcher split was broad; it stopped at `empty-session-layout` (`test-results/ui-harness/all-2026-04-29T15-07-23-621Z/`) on the existing footer-height assertion (`162px > 125px`), and the focused smoke suite above passed after artifact inspection.

Previous latest: codebase-efficiency spike added `docs/codebase-efficiency-audit.md`, capturing current organization/iteration hotspots, telemetry evidence, operating principles, and ranked follow-up slices for `apps/web/src/main.ts`, `scripts/ui-harness.ts`, `apps/server/src/index.ts`, validation reruns, and extension-shaped work. Validation used `bun run report:iteration --recommend docs/codebase-efficiency-audit.md PROJECT_LOG.md` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector marked this as docs/project-log only.

Previous latest: tool activity durations now survive a browser refresh in the same browser by caching live tool timing metadata per web session in localStorage keyed by SDK toolCallId, then applying that cache during snapshot hydration where pi's persisted toolResult messages do not carry startedAt/endedAt/durationMs. This preserves completed group duration receipts after refresh without changing backend/session files. Validation used `bun run report:iteration --recommend apps/web/src/main.ts`, `bun run check`, selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T06-07-06-459Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T06-07-11-424Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T06-07-13-491Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T06-07-16-314Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T06-07-18-684Z/`), plus focused `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T06-07-23-472Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: grouped tool activity receipts now avoid changing tool-name previews and show only stable metadata: duration, call count, and failed count when present. This keeps live activity cards predictable while detailed tool names/output remain available after expanding the group. The narrow tool-stream harness was updated to assert the parent activity stays expanded when an individual nested row is collapsed. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T06-02-41-634Z/`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T06-03-16-326Z/`), and selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T06-02-57-054Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T06-03-01-806Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T06-03-03-864Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T06-03-06-682Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T06-03-09-075Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T06-03-13-817Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: fixed expanded tool activity groups so collapsing an individual nested tool row no longer collapses the entire parent activity card on rerender. The app now stores expanded activity group ids and passes them back into transcript rendering, preserving the parent card's expanded DOM/ARIA state across row-level output toggles and other renders. Regression coverage asserts expanded tool activity state survives transcript rerendering. Validation used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T05-57-17-092Z/`), and selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-57-19-561Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-57-24-302Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-57-26-365Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T05-57-29-162Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T05-57-31-511Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: grouped tool activity durations now use one consistent wall-clock meaning: running cards tick from the first tool in the activity run, and completed cards freeze at first-tool start to final-tool end. Individual expanded tool rows still keep per-tool durations, while the overarching receipt differs mainly by whether its elapsed time is still incrementing. Regression coverage now asserts live groups no longer reset to the current tool's start and completed groups prefer wall-clock timestamps before duration-sum fallback. Validation used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, plus focused visual harnesses `bun scripts/ui-harness.ts --scenario tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T05-50-32-188Z/`) and `bun scripts/ui-harness.ts --scenario narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T05-50-34-821Z/`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for these files and the extra focused tool harnesses passed.

Previous latest: transcript jitter observability added reasoned browser perf events for render/patch samples, structure patches, running receipt timer patches, render fallbacks, queue-height changes, scroll corrections, and auto-scroll transitions; the UI harness now records event/reason counts plus recent events in `piWebPerf`. The first diagnostic run already points at likely future jitter work: `narrow-tool-stream` recorded 1 structure patch, 2 render fallbacks, 23 scroll corrections, and 6 auto-scroll transitions, with repeated `detached-during-patch` → `scroll-near-bottom` cycles during active tool streaming. Validation used `bun run report:iteration --recommend apps/web/src/utils.ts apps/web/src/main.ts apps/web/src/transcript-follow.ts scripts/ui-harness.ts`, `bun run check`, selector-requested `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-41-03-914Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-41-33-408Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-41-35-575Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T05-41-38-366Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T05-41-10-853Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T05-41-15-608Z/`), plus diagnostic `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T05-41-40-714Z/tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: tool activity elapsed-time investigation fixed the jarring live receipt jump by making active grouped-tool duration follow the current running tool's `startedAt` instead of the oldest completed tool in the group; the timer patch now reuses the full receipt label from the render model so full/structure renders and 1s interval patches stay consistent, including failed-count metadata. Regression coverage now asserts the old completed + newly running tool case stays at `1s` instead of jumping to the group age, and that full render/timer-patch receipt text matches. Validation used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/main.ts apps/web/src/session-events.ts apps/web/src/transcript-renderer.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-34-19-217Z/`), `bun scripts/ui-harness.ts --scenario slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-34-26-731Z/`), `bun scripts/ui-harness.ts --scenario question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-34-28-789Z/`), `bun scripts/ui-harness.ts --scenario inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T05-34-31-527Z/`), and `bun scripts/ui-harness.ts --scenario transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T05-34-33-886Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: tool-run detail alignment follow-up normalized expanded successful/failed tool receipt header grids so the disclosure chevron, title, overflow menu, and header height stay aligned; the `tool-grouping` harness now records horizontal and vertical toggle-offset alignment for failed rows. Validation used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T05-27-53-280Z/tool-grouping-expanded.png`), `bun scripts/ui-harness.ts --scenario narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T05-28-01-263Z/tool-stream.png`), and selector-requested `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T05-28-15-026Z/mobile-running-composer-controls.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T05-28-17-473Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T05-28-19-540Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T05-28-22-248Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: tool-run card unification used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-renderer.test.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/main.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused tool coverage `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T04-22-35-859Z/tool-stream.png`) and `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T04-22-41-410Z/tool-grouping-expanded.png`), plus selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T04-22-56-257Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T04-23-00-998Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T04-23-25-550Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T04-24-40-260Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T04-24-42-988Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T04-24-47-749Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: adaptive mobile running tool activity used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-renderer.test.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/main.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused tool coverage `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-59-10-504Z/tool-stream.png`), and selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-59-23-538Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-59-28-285Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-59-30-301Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-59-33-090Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-59-35-384Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-59-40-125Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: extension architecture design used `bun run report:iteration --recommend docs/extensions-design.md DESIGN.md` and `bun run check`. The validation selector chose static checks only for this docs/design-only slice; full `bun run test:web-perf` was intentionally skipped because no browser UI/protocol/session lifecycle code changed.

Previous latest: stable live grouped-tool patching used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/main.ts apps/web/src/transcript-dom.ts apps/web/src/transcript-renderer.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-23-08-661Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-23-13-422Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-23-15-496Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-23-18-228Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-23-20-516Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-23-25-246Z/mobile-running-composer-controls.png`), plus tool-specific `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-22-56-855Z/narrow-tool-stream.png`, with `toolStreamPerf` render/patch/row-update counts) and `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T03-23-27-722Z/tool-grouping-expanded.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: quiet adaptive grouped tool-call display used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript-renderer.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, selector-requested harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-11-52-879Z/mobile-running-composer-controls.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-11-55-435Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-11-57-601Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-12-01-525Z/`), plus tool-specific `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T03-11-40-291Z/tool-grouping-expanded.png`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-11-42-596Z/narrow-tool-stream.png`) coverage. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: delayed composer mode-switch fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-01-12-740Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-01-22-319Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-01-24-506Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-01-28-475Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-01-30-760Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-01-35-515Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: live tool-group label regression coverage used `bun run report:iteration --recommend apps/web/src/transcript-renderer.test.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this test-only follow-up.

Previous latest: sidebar visual-language pass used `bun run report:iteration --recommend apps/web/src/styles/sidebar.css apps/web/src/styles/mobile.css apps/web/src/main.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T02-44-01-934Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T02-44-06-892Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T02-44-09-125Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T02-44-13-042Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T02-44-15-518Z/`), plus extra sidebar-adjacent `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T02-44-40-107Z/`), `sessions-page` (`test-results/ui-harness/sessions-page-2026-04-29T02-44-42-692Z/`), and `themes` (`test-results/ui-harness/themes-2026-04-29T02-44-44-001Z/`) coverage. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: `/plan` canonical grill-me prompt refinement used `bun run report:iteration --recommend apps/server/src/workflow-skills.ts scripts/assert-workflow-skills.ts`, `bun run check`, and focused slash-command coverage `bun scripts/ui-harness.ts --scenario slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T02-37-51-948Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: live tool activity stack used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-renderer.test.ts apps/web/src/styles/transcript.css apps/web/src/main.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript-renderer.test.ts`, `bun run check`, focused tool/scroll harnesses `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T22-22-16-529Z/`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-28T22-22-05-293Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T22-22-07-880Z/`), and selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T22-22-21-616Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T22-22-26-348Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T22-22-28-691Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T22-22-32-593Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T22-22-35-053Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile initial-sidebar fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T20-33-15-983Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T20-33-20-806Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T20-33-23-048Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T20-33-27-026Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T20-33-29-575Z/`), and updated `mobile-layout` coverage seeded with desktop-open/pinned sidebar persistence (`test-results/ui-harness/mobile-layout-2026-04-28T20-33-07-582Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile image stream-stability follow-up used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, the new targeted `mobile-image-stream-stability` scenario (`test-results/ui-harness/mobile-image-stream-stability-2026-04-28T19-55-02-007Z/mobile-image-stream-stability.png`), and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T19-55-19-329Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T19-55-24-169Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T19-55-08-659Z/` after fixing focus preservation), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T19-55-26-402Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T19-55-28-919Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T19-55-33-772Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile settings spacing follow-up used `bun run report:iteration --recommend apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and extra visual confidence via `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T18-11-43-990Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this CSS-only follow-up.

Previous latest: settings-page IA slice used `bun run project:notes`, `/plan` interview, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/router.ts apps/web/src/router.test.ts apps/web/src/styles/layout.css scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T18-07-39-465Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T18-08-30-413Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T18-08-44-840Z/` after one transient first-run miss), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T18-08-51-158Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T18-08-53-706Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T18-09-11-598Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile transcript image jitter slice used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/mobile.css`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-39-14-545Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T17-39-19-793Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-39-21-948Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-39-26-073Z/`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-39-28-568Z/`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected checks passed.

Previous latest: sidebar Chat removal/same-session route follow-up used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-28-37-077Z/`), `slash-commands` after fixing the same-session chat route regression (`test-results/ui-harness/slash-commands-2026-04-28T17-28-42-278Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-28-44-427Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-28-48-519Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-28-51-031Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T17-28-55-764Z/mobile-layout.png`), plus extra `sessions-page` coverage (`test-results/ui-harness/sessions-page-2026-04-28T17-28-58-127Z/sessions-page.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected checks passed.

Previous latest: sessions page scroll follow-up used `bun run report:iteration --recommend apps/web/src/styles/layout.css`, `bun run check`, and focused `sessions-page` harness (`test-results/ui-harness/sessions-page-2026-04-28T17-23-48-709Z/sessions-page.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for the CSS-only change; the focused sessions-page harness was run additionally for visual coverage. Follow-up corrected the scroll container so the top app bar remains visible while only the sessions content scrolls.

Latest: composer send-availability follow-up keeps Send/Guide/Follow up disabled/gray until the composer has text or attached images. Validation reused `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-49-23-683Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T17-49-34-701Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-49-36-858Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-49-40-977Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-49-43-462Z/`), and `mobile-layout` with disabled/enabled composer assertions (`test-results/ui-harness/mobile-layout-2026-04-28T17-49-48-230Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: mobile running composer button-label slice used `bun run project:notes`, `/plan` interview, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T17-45-30-249Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T17-45-37-301Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T17-45-39-451Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T17-45-43-573Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T17-45-46-078Z/`), and `mobile-layout` with new running-control label assertions and screenshot (`test-results/ui-harness/mobile-layout-2026-04-28T17-45-50-834Z/mobile-running-composer-controls.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: sessions/navigation IA split used `bun run report:iteration --recommend apps/web/src/router.ts apps/web/src/router.test.ts apps/web/src/main.ts apps/web/src/session-sidebar.ts apps/web/src/styles/sidebar.css scripts/ui-harness.ts`, `bun install` after the isolated worktree lacked dependencies, `bun run check`, new focused `sessions-page` (`test-results/ui-harness/sessions-page-2026-04-28T16-54-23-629Z/sessions-page.png`), and selector-requested harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T16-54-29-140Z/`), `slash-commands` final rerun (`test-results/ui-harness/slash-commands-2026-04-28T16-56-30-047Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T16-56-36-183Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T16-56-40-321Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T16-56-42-827Z/`), and `mobile-layout` final rerun (`test-results/ui-harness/mobile-layout-2026-04-28T16-58-00-860Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: PR screenshot handoff follow-up removed the tracked PR PNG artifacts again to avoid committing transient screenshots to repo history, updated PR #1 back to local screenshot artifact paths, and clarified `AGENTS.md` to list `test-results/...` paths by default unless the operator chooses a GitHub attachment upload flow. Validation used `bun run check`; full `bun run test:web-perf` was intentionally skipped because this was docs/process-only.

Previous latest: PR handoff guidance now asks agents to create/update GitHub PRs with a human-readable title/body derived from the final handoff, including summary, validation, manual test notes, and screenshot artifact paths instead of relying on `gh pr create --fill`. Validation used `bun run check`; full `bun run test:web-perf` was intentionally skipped because this was docs/process-only.

Previous latest: current-dev-loop PR handoff guidance added an `AGENTS.md` end-of-session step for `bakery/session/*` branches with authenticated `gh`: push the isolated branch and create/update a GitHub PR for operator review/merge. Validation used `bun run check`; full `bun run test:web-perf` was intentionally skipped because this was docs/process-only.

Previous latest: isolated-session header cue used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/layout.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T14-52-35-324Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T14-52-40-809Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T14-52-42-924Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T14-52-47-032Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T14-52-49-531Z/`), and `mobile-layout` first as part of the full selected sequence (`test-results/ui-harness/mobile-layout-2026-04-28T14-52-54-269Z/`) then rerun after adding explicit isolated-header screenshot/assertions (`test-results/ui-harness/mobile-layout-2026-04-28T14-53-42-266Z/mobile-isolated-header.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks passed.

Previous latest: opt-in Git worktree session isolation used `bun run report:iteration --recommend packages/protocol/src/index.ts apps/server/src/config.ts apps/server/src/metadata-store.ts apps/server/src/workspaces.ts apps/server/src/index.ts apps/web/src/main.ts apps/web/src/session-metadata.ts apps/web/src/session-sidebar.ts`, `bun run check`, a temp-repo smoke of `createGitWorktreeSession`, and selector-requested focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-28T14-43-42-137Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-28T14-43-47-667Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-04-28T14-43-54-098Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T14-44-00-124Z/`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T14-44-04-121Z/`), and `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T14-44-11-344Z/`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused lifecycle/protocol checks passed.

Previous latest: local Bun distribution/onboarding slice used `bun run report:iteration --recommend package.json README.md scripts/doctor.ts docs/local-network.md apps/web/vite.config.ts docs/remote-artifacts.md`, `bun run check`, `bun run doctor`, expected-failure `env -u PI_WEB_AUTH_TOKEN -u PI_WEB_HOST PI_WEB_WORKSPACE_ROOT="$PWD" bun run doctor --lan`, and successful `env PI_WEB_AUTH_TOKEN=test PI_WEB_WORKSPACE_ROOT="$PWD" bun run doctor --lan`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for docs/package/script/Vite config changes with no browser UI/protocol/session lifecycle impact. Public docs/config now use generic LAN IP/custom-host examples instead of machine-local domain names.

Previous latest: pending question UI modernization used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T14-17-21-856Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T14-17-28-888Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T14-17-32-595Z/question-answer.png`, `test-results/ui-harness/question-answer-2026-04-28T14-17-32-595Z/question-answer-dark.png`, `test-results/ui-harness/question-answer-2026-04-28T14-17-32-595Z/question-answer-mobile-touch-hint.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T14-17-38-658Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T14-17-43-936Z/transcript-scroll-stability.png`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T14-17-50-189Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed. Follow-up real-session dogfooding reported the updated question UI looks good.

Previous latest: mobile top-bar compaction, contrast, and passive-status removal used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css apps/web/src/styles/layout.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T06-12-36-082Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T06-12-41-336Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T06-12-43-525Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T06-12-47-491Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T06-12-49-879Z/transcript-scroll-stability.png`), and `mobile-layout` with new mobile header assertions (`test-results/ui-harness/mobile-layout-2026-04-28T06-12-54-616Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T06-12-54-616Z/mobile-metadata-popover.png`, `test-results/ui-harness/mobile-layout-2026-04-28T06-12-54-616Z/mobile-queued-expanded.png`); the CSS-only contrast follow-up then used `bun run report:iteration --recommend apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and targeted visual `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T06-15-38-599Z/mobile-layout.png`). The passive-status removal used `bun run report:iteration --recommend apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, and focused `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T06-18-15-747Z/mobile-layout.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T06-18-18-546Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T06-18-20-739Z/question-answer.png`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T06-18-24-664Z/streaming-responsiveness.png`); the first `mobile-layout` rerun exposed hidden `.status.running` waits in the harness, so those waits now use app state instead. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: idle-status suppression used `bun run report:iteration --recommend apps/web/src/main.ts` followed by the final `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-55-32-459Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-55-37-715Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-55-39-864Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-55-43-818Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-55-46-311Z/transcript-scroll-stability.png`), and final `mobile-layout` smoke after the status color/harness updates (`test-results/ui-harness/mobile-layout-2026-04-28T05-56-42-399Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T05-56-42-399Z/mobile-queued-expanded.png`). The first `streaming-responsiveness` run failed because the harness still waited for `.status.idle`; harness waits now inspect app idle state instead. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: Jump-to-latest/queue overlap fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/transcript.css`, `bun run check`, targeted `queued-follow-up` (`test-results/ui-harness/queued-follow-up-2026-04-28T05-49-32-808Z/queued-follow-up.png`, `test-results/ui-harness/queued-follow-up-2026-04-28T05-49-32-808Z/queued-follow-up-final.png`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-49-38-962Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T05-49-38-962Z/mobile-queued-expanded.png`), and selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-49-49-562Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-49-54-813Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-49-56-954Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-50-00-938Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-49-41-699Z/transcript-scroll-stability.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: remove mobile header auto-hide used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-46-45-390Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-47-00-400Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-47-02-571Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-47-06-498Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-46-50-652Z/transcript-scroll-stability.png`), plus targeted mobile/tool smokes `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-46-36-565Z/mobile-layout.png`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T05-46-39-839Z/tool-stream.png`, `test-results/ui-harness/narrow-tool-stream-2026-04-28T05-46-39-839Z/narrow-tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: mobile header jitter follow-up used `bun run report:iteration --recommend apps/web/src/main.ts`, `bun run check`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-44-52-540Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-45-09-132Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-45-11-301Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-45-15-229Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-44-57-816Z/transcript-scroll-stability.png`), plus targeted mobile/tool smokes `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-44-43-679Z/mobile-layout.png`) and `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T05-44-47-003Z/tool-stream.png`, `test-results/ui-harness/narrow-tool-stream-2026-04-28T05-44-47-003Z/narrow-tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: running tool-call jitter follow-up used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/transcript.test.ts`, `bun run check`, focused harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-42-42-043Z/mobile-layout.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-42-45-141Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-42-47-290Z/question-answer-pending.png`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-42-51-248Z/streaming-responsiveness.png`), plus targeted `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-28T05-42-26-306Z/tool-stream.png`, `test-results/ui-harness/narrow-tool-stream-2026-04-28T05-42-26-306Z/narrow-tool-stream.png`). Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance after selected focused checks passed.

Previous latest: neutral Jump-to-latest style follow-up used `bun run report:iteration --recommend apps/web/src/styles/transcript.css` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow CSS color tuning.

Previous latest: icon-only Jump-to-latest follow-up used `bun run report:iteration --recommend apps/web/src/transcript-follow.ts apps/web/src/styles/transcript.css` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow affordance markup/CSS change.

Previous latest: mobile model menu anchor follow-up used `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-23-51-262Z/model-thinking-mobile.png`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-24-07-969Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because this was a focused mobile popover placement fix after the broader composer-side model/settings focused suite had passed.

Previous latest: mobile model menu portal fix used `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-22-08-698Z/model-thinking-mobile.png`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-22-21-442Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because this was a focused mobile popover layering/placement fix after the broader composer-side model/settings focused suite had passed.

Previous latest: mobile model menu stacking follow-up raised the open composer/model menu above transcript overlay z-indexes and used `bun run check` plus `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-19-37-723Z/model-thinking-mobile.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow CSS stacking fix after the composer-side model/settings focused suite had passed.

Previous latest: mobile model menu stacking fix used `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-17-23-932Z/mobile-layout.png`), and `bun scripts/ui-harness.ts --scenario model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-17-27-185Z/model-thinking-mobile.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow CSS stacking fix after the composer-side model/settings focused suite had passed.

Previous latest: composer-side model/settings first pass used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/model-thinking-picker.ts apps/web/src/model-thinking-picker.test.ts apps/web/src/styles/composer.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun test apps/web/src/model-thinking-picker.test.ts`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-14-13-269Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-14-23-980Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-14-26-122Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-14-30-052Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-14-32-759Z/transcript-scroll-stability.png`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-14-38-162Z/mobile-layout.png`), and `model-thinking` (`test-results/ui-harness/model-thinking-2026-04-28T05-14-40-893Z/model-thinking.png`, `test-results/ui-harness/model-thinking-2026-04-28T05-14-40-893Z/model-thinking-mobile.png`). Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance after focused checks passed.

Previous latest: Details summary reveal follow-up used `bun run check` and targeted visual smoke `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-09-33-485Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow details-popover markup/style follow-up after the previous selector chose static checks for this styling area.

Previous latest: Details popover opacity follow-up used `bun run check` and targeted visual smoke `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-07-38-571Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow CSS-only readability fix after the previous selector chose static checks for the details popover styling slice.

Previous latest: Details popover visual follow-up used `bun run report:iteration --recommend apps/web/src/session-metadata.ts apps/web/src/styles/metadata.css apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and targeted visual smoke `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-06-12-214Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow markup/style follow-up and the targeted mobile visual smoke passed.

Previous latest: top-bar identity redesign used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/layout.css apps/web/src/styles/mobile.css apps/web/src/styles/metadata.css apps/web/src/styles/composer.css`, `bun run check`, focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T05-00-51-637Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T05-00-59-612Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T05-01-01-761Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T05-01-05-695Z/inspector-preview.png`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T05-01-08-425Z/transcript-scroll-stability.png`), and targeted `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T05-02-48-292Z/mobile-layout.png`, `test-results/ui-harness/mobile-layout-2026-04-28T05-02-48-292Z/mobile-metadata-popover.png`). Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance after focused checks passed. An exploratory `session-metadata` rerun exposed a pre-existing/generic-placeholder harness expectation mismatch after `/name --clear` (`New session` vs workspace fallback) before reaching the updated Details-menu path; it was not included in the selected validation set.

Previous latest: ChatGPT-like composer bottom-row follow-up used `bun run check`, `bun test apps/web/src/composer-controller.test.ts`, focused `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T04-30-30-887Z/streaming-responsiveness.png`) for optimistic running-control swap, and focused `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-34-50-648Z/mobile-layout.png`) for compact in-dock controls. Full `bun run test:web-perf` was intentionally skipped because these were narrow composer markup/style follow-ups and focused checks passed.

Previous latest: centered composer-only follow-up used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css apps/web/src/styles/layout.css apps/web/src/styles/mobile.css apps/web/src/styles/transcript.css PROJECT_LOG.md`, `bun run check`, `bun test apps/web/src/composer-controller.test.ts`, focused visual smokes `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-17-02-764Z/mobile-layout.png`) and `theme-gallery` (`test-results/ui-harness/theme-gallery-2026-04-28T04-17-06-216Z/theme-gallery-dark.png`, `test-results/ui-harness/theme-gallery-2026-04-28T04-17-06-216Z/theme-gallery-light.png`), plus selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T04-17-39-985Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T04-17-45-577Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T04-17-48-091Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T04-17-51-823Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T04-17-54-671Z/transcript-scroll-stability.png`). Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance after focused checks passed; follow-up reverted transcript centering while keeping the new centered composer.

Previous latest: composer compact-controls/context follow-up used `bun run check` and focused visual smoke harnesses `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-08-43-014Z/mobile-layout.png`) and `theme-gallery` (`test-results/ui-harness/theme-gallery-2026-04-28T04-08-46-458Z/theme-gallery-dark.png`, `test-results/ui-harness/theme-gallery-2026-04-28T04-08-46-458Z/theme-gallery-light.png`). Full `bun run test:web-perf` was intentionally skipped because this was a narrow composer style/markup follow-up after the selector chose static checks for the CSS-only slice.

Previous latest: composer design-language pass used `bun run report:iteration --recommend apps/web/src/styles/composer.css apps/web/src/styles/layout.css apps/web/src/styles/mobile.css`, `bun run check`, and visual smoke harnesses `theme-gallery` (`test-results/ui-harness/theme-gallery-2026-04-28T04-03-10-683Z/theme-gallery-dark.png`, `test-results/ui-harness/theme-gallery-2026-04-28T04-03-10-683Z/theme-gallery-light.png`) and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-03-18-357Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this CSS-only slice; the two focused visual harnesses were run for desktop/light/dark and mobile screenshot review.

Previous latest: clean session-route slice used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/router.ts apps/web/src/router.test.ts scripts/ui-harness.ts`, `bun test apps/web/src/router.test.ts`, `bun run check`, and focused harnesses `session-routing` (`test-results/ui-harness/session-routing-2026-04-28T03-56-30-234Z/session-routing.png`), `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T03-55-51-217Z/streaming-responsiveness.png`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-55-56-586Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: Mobile sidebar session-selection fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-42-24-652Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance; the selector also listed streaming/slash/question/inspector/scroll due to `main.ts` churn, but this bug fix only moves session-card binding into sidebar-local wiring and extends the targeted mobile drawer scenario.

Previous latest: Mobile hamburger local-patch fix used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-28T03-37-52-080Z/mobile-long-transcript-controls.png`), `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-38-01-685Z/mobile-layout.png`), and `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T03-38-05-996Z/streaming-responsiveness.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed; slash/question/inspector/scroll were not rerun because this slice only changes mobile sidebar toggling plus extracted sidebar binding/rendering, and the targeted mobile/new benchmark plus streaming smoke covered the touched interaction path.

Previous latest: Mobile long-transcript control benchmark used `bun run report:iteration --recommend scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-28T03-31-28-716Z/mobile-long-transcript-controls.png`), and `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T03-31-40-300Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused harnesses passed; the selector also recommended slash/question/streaming for broader harness-script changes, but this slice only adds an isolated scenario and reran the new scenario plus mobile layout smoke.

Previous latest: Back to chat composer-takeover fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/composer.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario slash-commands`, and `bun scripts/ui-harness.ts --scenario mobile-layout`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: full composer takeover `/plan` actions used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/styles/composer.css apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, and focused harnesses `slash-commands`, `streaming-responsiveness`, `question-answer`, `inspector-preview`, `transcript-scroll-stability`, and `mobile-layout`. Follow-up dogfooding caught that the takeover stayed visible after submitting feedback while a run was still ending; fixed by showing takeover only when the latest transcript item is the marked plan summary, and by submitting running actions as queued follow-ups. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: `/plan` inline action visual/focus follow-up used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/web/src/transcript.ts apps/web/src/main.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario slash-commands`, and `bun scripts/ui-harness.ts --scenario mobile-layout`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after the focused slash/mobile checks passed.

Previous latest: inline `/plan` final-action buttons used `bun run report:iteration --recommend apps/server/src/workflow-skills.ts scripts/assert-workflow-skills.ts apps/web/src/transcript.ts apps/web/src/main.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css apps/server/src/fake-runner.ts scripts/ui-harness.ts apps/web/src/transcript.test.ts`, `bun run check`, focused harnesses `slash-commands`, `streaming-responsiveness`, `question-answer`, `inspector-preview`, `transcript-scroll-stability`, `narrow-tool-stream`, and optional `mobile-layout`. The first `slash-commands` run caught the fake-agent plan prompt's new “Cancel plan” marker accidentally triggering the fake cancel-question path; fixed and reran `slash-commands` after sharing the marker through `packages/protocol`. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused harnesses passed.

Previous latest: success/info app notice cleanup used `bun run report:iteration --recommend apps/web/src/main.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because this was a narrow notice-state cleanup and static checks passed.

Previous latest: prompt screenshot thumbnail placement used `bun run report:iteration --recommend apps/web/src/main.ts`, `bun run check`, and focused image attachment harness `bun scripts/ui-harness.ts --scenario image-attachments` (`test-results/ui-harness/image-attachments-2026-04-28T02-28-38-140Z/image-attachments.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because this was a narrow composer layout change and the focused image path passed.

Previous latest: screenshot attach/upload banner removal used `bun run report:iteration --recommend apps/web/src/main.ts` and `bun run check`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because this was a narrow success-notice copy/state change and static checks passed.

Previous latest: failed local bash duplicate-card fix used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/main.ts apps/web/src/transcript.test.ts` plus the follow-up selector `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/transcript.test.ts`, focused unit tests (`bun test apps/web/src/transcript.test.ts apps/web/src/session-events.test.ts`), `bun run check`, targeted `bash-commands` reruns after both live-path follow-ups (`test-results/ui-harness/bash-commands-2026-04-28T02-14-43-111Z/`, `test-results/ui-harness/bash-commands-2026-04-28T02-17-11-130Z/`), and the earlier focused harnesses: `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T02-11-43-525Z/streaming-responsiveness.png`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T02-11-49-292Z/slash-commands.png`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T02-11-51-821Z/question-answer.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T02-11-55-194Z/inspector-preview.png`), and `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T02-11-58-073Z/transcript-scroll-stability.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused checks/harnesses passed.

Previous latest: tool activity receipts/durations and no-card activity-log follow-up used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript-renderer.ts apps/web/src/session-events.ts apps/web/src/main.ts apps/web/src/styles/transcript.css apps/server/src/fake-runner.ts apps/web/src/session-events.test.ts apps/web/src/transcript-renderer.test.ts`, the CSS follow-up selector `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, focused unit tests (`bun test apps/web/src/session-events.test.ts apps/web/src/transcript-renderer.test.ts apps/web/src/transcript.test.ts`), `bun run check`, and focused harnesses: `streaming-responsiveness` (latest inline-tool-chip suppression rerun: `test-results/ui-harness/streaming-responsiveness-2026-04-28T02-02-25-538Z/streaming-responsiveness.png`), `slash-commands` (rerun after question/system width follow-up: `test-results/ui-harness/slash-commands-2026-04-28T01-24-13-967Z/`), `question-answer` (latest accent-rail rerun: `test-results/ui-harness/question-answer-2026-04-28T01-32-56-676Z/question-answer-pending.png`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T01-20-39-575Z/inspector-preview.png`), `transcript-scroll-stability`, `narrow-tool-stream` (latest no-card rerun: `test-results/ui-harness/narrow-tool-stream-2026-04-28T01-27-28-085Z/narrow-tool-stream.png`), `tool-grouping` (latest inline-tool-chip suppression rerun: `test-results/ui-harness/tool-grouping-2026-04-28T02-02-31-384Z/tool-grouping-expanded.png`), `narrow-tool-stream` (latest ANSI terminal rendering rerun: `test-results/ui-harness/narrow-tool-stream-2026-04-28T02-06-59-145Z/narrow-tool-stream.png`), `bash-commands` (latest ANSI terminal rendering rerun: `test-results/ui-harness/bash-commands-2026-04-28T02-06-52-885Z/`), and `tool-image-heavy-transcript` (latest no-card rerun: `test-results/ui-harness/tool-image-heavy-transcript-2026-04-28T01-27-35-956Z/tool-image-heavy-transcript.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance after focused transcript/tool scenarios passed.

Latest: sidebar backdrop hover blank-screen fix used `bun run project:notes`, `bun run report:iteration --recommend apps/web/src/styles/sidebar.css`, and `bun run check`. Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this narrow CSS-only fix.

Previous latest: inspector/sidebar removal used `bun run project:notes`, targeted code inspection, `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/transcript.ts apps/web/src/transcript-dom.ts apps/web/src/styles/base.css apps/web/src/styles/mobile.css apps/web/src/styles.css apps/server/src/pi-runner.ts apps/server/src/fake-runner.ts scripts/ui-harness.ts`, `bun run check`, focused harness scenarios `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T04-45-50-470Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T04-45-56-035Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T04-45-58-250Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T04-46-02-206Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T04-46-04-913Z/`), `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T04-46-10-340Z/mobile-layout.png`), and the updated transcript-fork coverage in `tree-fork-navigation` (`test-results/ui-harness/tree-fork-navigation-2026-04-28T04-46-29-967Z/transcript-fork-no-tree-ui.png`). Final static validation reran `bun run check`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance because focused checks covered the removed inspector/tree UI and transcript action paths.

Previous latest: ChatGPT-style transcript conversation layout used `bun run report:iteration --recommend apps/web/src/transcript.ts apps/web/src/transcript-dom.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css PROJECT_LOG.md`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T00-56-10-407Z/streaming-responsiveness.png`), `bun scripts/ui-harness.ts --scenario inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T01-04-05-900Z/inspector-preview.png`), `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T01-00-40-426Z/mobile-layout.png`), and `bun scripts/ui-harness.ts --scenario image-attachments` (`test-results/ui-harness/image-attachments-2026-04-28T00-59-40-382Z/`). Follow-up final static validation reran `bun run check`. Full `bun run test:web-perf` was intentionally skipped because focused checks/harnesses covered the changed transcript/action/image surfaces.

Previous latest: chat bubble overflow affordance tweak used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, `bun run check`, and `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T00-46-42-007Z/streaming-responsiveness.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this small CSS-only change.

Previous latest: chat bubble optical-centering tweak used `bun run report:iteration --recommend apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, `bun run check`, and `bun scripts/ui-harness.ts --scenario streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T00-44-52-464Z/streaming-responsiveness.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose static checks only for this small CSS-only change.

Previous latest: autocomplete keyboard regression fix used `bun run report:iteration --recommend apps/web/src/composer-controller.ts apps/web/src/main.ts apps/web/src/composer-controller.test.ts`, `bun test apps/web/src/composer-controller.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario streaming-responsiveness`, `bun scripts/ui-harness.ts --scenario slash-commands`, `bun scripts/ui-harness.ts --scenario question-answer`, `bun scripts/ui-harness.ts --scenario inspector-preview`, and `bun scripts/ui-harness.ts --scenario transcript-scroll-stability`. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance.

Previous latest: mobile quick-start fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T00-38-33-364Z/mobile-empty-quick-starts.png`), and `bun scripts/ui-harness.ts --scenario empty-session-layout` (`test-results/ui-harness/empty-session-layout-2026-04-28T00-38-49-645Z/empty-session-layout.png`). Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance.

Previous latest: empty-session quick-start discovery used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/transcript.css scripts/ui-harness.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario empty-session-layout` (`test-results/ui-harness/empty-session-layout-2026-04-28T00-35-32-473Z/empty-session-layout.png`), `bun scripts/ui-harness.ts --scenario slash-commands`, `bun scripts/ui-harness.ts --scenario mobile-layout`, and `bun scripts/ui-harness.ts --scenario streaming-responsiveness`. One parallel mobile/slash harness attempt failed from shared fixed ports and passed when rerun serially. Full `bun run test:web-perf` was intentionally skipped per the validation selector's escalation-only guidance.

Previous latest: chat bubble UX pass used `bun run report:iteration --recommend apps/web/src/styles/base.css apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css`, `bun run check`, an exploratory `bun scripts/ui-harness.ts --list` invocation that defaulted to/passed `streaming-responsiveness` with screenshots under `test-results/ui-harness/streaming-responsiveness-2026-04-28T00-17-31-923Z/`, and follow-up mobile bubble tuning validated with `bun scripts/ui-harness.ts --scenario mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T00-22-00-029Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the validation selector chose focused static checks only for CSS-token/style changes.

Previous latest: model/thinking picker regression coverage used `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking`, and `bun scripts/ui-harness.ts --scenario mobile-layout`; `model-thinking` now repeats the picker check at mobile width, verifies selected model/thinking values persist, asserts the popover stays onscreen, and captures `model-thinking-mobile.png`. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: mobile model/thinking popover viewport fix used `bun run check` and `bun scripts/ui-harness.ts --scenario mobile-layout`; the first mobile harness rerun caught right-edge overflow, then passed after centering the mobile popover and applying border-box sizing. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: mobile model/thinking picker fix used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/model-thinking-picker.ts apps/web/src/model-thinking-picker.test.ts apps/web/src/styles/composer.css scripts/ui-harness.ts PROJECT_LOG.md`; `bun test apps/web/src/model-thinking-picker.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario mobile-layout`, and `bun scripts/ui-harness.ts --scenario model-thinking` passed. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: consolidated model/thinking picker used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/mobile.css scripts/ui-harness.ts`; `bun test apps/web/src/model-thinking-picker.test.ts`, `bun run check`, `bun scripts/ui-harness.ts --scenario model-thinking`, and `bun scripts/ui-harness.ts --scenario mobile-layout` passed. Full `bun run test:web-perf` was intentionally skipped per focused-first escalation guidance.

Previous latest: bounded recent-history telemetry used `bun run report:iteration --recommend scripts/report-iteration.ts .pi/skills/iteration-observability/SKILL.md PROJECT_LOG.md`; `bun run check`, `bun run report:iteration`, `bun run project:notes`, and `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session` passed. Full `bun run test:web-perf` was intentionally skipped because the selector marked this script/skill-doc change as script-only with no browser harness coverage needed.

Previous latest: quiet empty-session UX used `bun run report:iteration --recommend apps/web/src/main.ts apps/web/src/styles/transcript.css scripts/ui-harness.ts`; `bun run check`, `empty-session-layout`, `streaming-responsiveness`, `slash-commands`, `question-answer`, `inspector-preview`, `transcript-scroll-stability`, and `mobile-layout` passed. Full `bun run test:web-perf` was intentionally skipped per escalation-only guidance.

Latest: mobile flat-tool receipt readability tweak used `bun run report:iteration --recommend apps/web/src/styles/mobile.css`, `bun run check`, and direct visual/regression harnesses `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-29T03-52-49-446Z/mobile-long-transcript-controls.png`) and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-52-51-630Z/mobile-layout.png`). Full `bun run test:web-perf` was intentionally skipped because the selector chose static checks only for this mobile CSS-only follow-up and focused mobile harnesses passed.

Previous latest: flat tool activity simplification used `bun run report:iteration --recommend apps/web/src/transcript-renderer.ts apps/web/src/transcript-dom.ts apps/web/src/main.ts apps/web/src/styles/transcript.css apps/web/src/styles/mobile.css scripts/ui-harness.ts`, `bun run check`, `bun test apps/web/src/transcript-renderer.test.ts`, selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-29T03-48-56-502Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-29T03-49-14-857Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-29T03-49-16-928Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-29T03-49-19-602Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-29T03-49-21-889Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-29T03-49-08-776Z/`), plus direct tool/mobile regression harnesses `narrow-tool-stream` (`test-results/ui-harness/narrow-tool-stream-2026-04-29T03-51-13-763Z/`), `tool-grouping` (`test-results/ui-harness/tool-grouping-2026-04-29T03-51-19-095Z/`), and `mobile-long-transcript-controls` (`test-results/ui-harness/mobile-long-transcript-controls-2026-04-29T03-51-21-322Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: direct take-control UI reconciliation used `bun run report:iteration --recommend apps/web/src/main.ts scripts/ui-harness.ts`, `bun run check`, direct lifecycle harnesses `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-28T20-08-36-975Z/`) and `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-28T20-08-46-863Z/`), plus selector-requested focused harnesses `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T20-08-53-658Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T20-08-58-480Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T20-09-00-729Z/`), `inspector-preview` (`test-results/ui-harness/inspector-preview-2026-04-28T20-09-04-759Z/`), `transcript-scroll-stability` (`test-results/ui-harness/transcript-scroll-stability-2026-04-28T20-09-07-347Z/`), and `mobile-layout` (`test-results/ui-harness/mobile-layout-2026-04-28T20-09-12-188Z/`). Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused checks passed.

Previous latest: direct take-control semantics used `bun run report:iteration --recommend apps/server/src/index.ts apps/server/src/config.ts apps/web/src/main.ts packages/protocol/src/index.ts scripts/ui-harness.ts`, `bun run check`, and focused harnesses `reconnect-controller` (`test-results/ui-harness/reconnect-controller-2026-04-28T19-14-45-953Z/`), `controller-handoff-edges` (`test-results/ui-harness/controller-handoff-edges-2026-04-28T19-14-50-453Z/`), `backend-restart` (`test-results/ui-harness/backend-restart-2026-04-28T19-14-55-547Z/`), `slash-commands` (`test-results/ui-harness/slash-commands-2026-04-28T19-15-01-783Z/`), `question-answer` (`test-results/ui-harness/question-answer-2026-04-28T19-15-18-582Z/`), and `streaming-responsiveness` (`test-results/ui-harness/streaming-responsiveness-2026-04-28T19-15-27-740Z/`). One parallel `question-answer` attempt failed due to harness port contention and passed when rerun serially. Full `bun run test:web-perf` was intentionally skipped per the selector's escalation-only guidance after focused lifecycle/protocol checks passed.

## Next priorities

1. Dogfood the completed UI harness family split when adding or debugging scenarios: keep `scripts/ui-harness/scenarios/index.ts` as a thin dispatcher, put new scenario bodies in the matching family module, and revisit the `empty-session-layout` footer-height assertion before relying on full `bun run test:web-perf` as a green baseline.
2. Continue open-source-readiness cleanup with a fresh hotspot check before the next refactor: backend route/WebSocket hub boundaries and the newest web-controller seams now exist, so prefer the extension-shaped `/plan` registry or a narrowly product-tied session lifecycle/composer image extraction instead of broad splitting unless a feature demands it.
3. Start Phase 1 of `docs/extensions-design.md`: refactor bundled workflow skills into an extension-shaped internal registry without dynamic loading, keeping `/plan` behavior unchanged while proving command metadata/handler shape and preserving slash-command coverage; use the audit guardrails to avoid growing backend/web monoliths.
4. Dogfood opt-in isolated worktree sessions in a real Git repo: create one from a clean repo and one from a dirty repo, confirm the branch/worktree path/details are understandable, agent edits stay out of the source checkout, and the warning about dirty source changes is clear.
5. Add explicit worktree status/change-summary display for isolated sessions: branch, base commit, dirty state, changed-file count, and a copyable manual review command before considering merge/apply actions.
6. Dogfood the new local install docs/doctor path on a clean-ish machine or fresh clone: confirm `bun install`, `bun run doctor`, local `PI_WEB_WORKSPACE_ROOT="$PWD" bun run dev`, and token-protected `bun run doctor --lan` copy produce the right next actions for a first pi-developer user.
7. Consider the next distribution slice after that dogfood: a one-command local launcher that runs the doctor, starts both services, and optionally opens the browser, or a browser first-run checklist that explains API URL/token/workspace choices after startup.
8. Dogfood the modernized pending question panel on a real phone/narrow browser next if not already covered by the latest real-session check: desktop real-session dogfooding reported the updated question UI looks good; only mobile touch ergonomics and viewer/disconnected copy need more real-device confidence.
9. Dogfood the new top-bar identity strip plus composer brain model/settings control on desktop and a real phone/narrow browser: confirm the title/workspace/details hierarchy gives enough orientation, Details is discoverable, and the composer-side model menu placement does not crowd Prompt/Context.
10. Explore the follow-up connection/control status treatment: decide how controller/viewer/take-control and red/yellow/green connection state should move toward composer-side status without conflicting with composer mode/context/model indicators.
11. Dogfood inferred auto-scroll behavior: confirm users understand that staying at the bottom follows latest output, scrolling up pauses follow-latest, and Jump to latest is enough without an explicit Auto-scroll checkbox.
12. Dogfood the adaptive mobile running tool activity on a real phone/narrow browser: confirm the summary-only default feels calmer during long tool runs, tapping the activity row to reveal receipts is discoverable enough, and desktop still has enough visible progress detail.
13. Dogfood the empty-session quick starts on desktop and a real phone/narrow browser: confirm the `/plan`, screenshot, `@file`, and `!bash` affordances teach the expected Bakery usage without feeling like a noisy onboarding tour, and decide whether the screenshot chip should open the picker or stay guidance-only.
14. Dogfood `!`/`!!` bash command support in real sessions: confirm failed commands now update the same local-bash card instead of rendering a duplicate SDK echo, the distinct expanded local-bash styling is readable in both themes, `!!` context exclusion is useful, idle-only blocking is acceptable, and decide whether the next slice should allow bash while an agent turn is running.
15. Dogfood the focused-first validation decision block plus the new `Rerun opportunity` block in future sessions: confirm agents stop after the first focused harness failure, inspect artifacts/logs before rerunning, skip full `bun run test:web-perf` for script/docs-only changes, and escalate only for protocol/session lifecycle, broad UI, or unexpected focused failures.
16. Dogfood `/skill:iteration-observability` in real pi sessions and tune `.pi/skills/iteration-observability/SKILL.md` if agents still rerun failed focused scenarios without artifact inspection, print too much, or produce generic process advice instead of evidence-backed repository recommendations.
17. Dogfood `bun run project:notes --handoff` at the end of future sessions; tune its anchors/line hints if agents still read broad `PROJECT_LOG.md` ranges or miss the right update targets.
18. Keep dogfooding the `Action summary` and `Rerun opportunity` sections in `bun run report:iteration --session-history --latest-sessions 10 --exclude-current-session` and `--session-context`: the latest bounded 10-session review estimated 7-15 avoidable focused-harness reruns while avoiding contamination from the active conversation; next tune thresholds/wording only if repeated sessions show noise instead of actionable recommendations.
19. Dogfood the CSS module split in the next real UI styling session; if the boundaries feel off, tune the module names/section placement before deeper style edits.
20. Continue the main-file extraction dogfood only with a fresh hotspot check: autocomplete, plan actions, running queue, settings, transcript state/event/live/shell, row actions, composer mode, shell events, and status seams now exist, so the next cleanup should target a remaining app-coordinator island such as session lifecycle/routing or composer image/drop handling rather than re-growing `main.ts`.
21. Consider the next observability slice: add explicit phase markers/intent labels or expose validation/edit telemetry in a user-facing session diagnostics panel if JSONL-derived reports prove valuable but too offline/manual.
22. Continue the tool-call/tool-output design language pass if dogfooding shows gaps: tune running/error tool prominence, align tool action placement/icons with the new bottom action row where appropriate, and decide whether duration metadata should be persisted into snapshot history beyond live web transcript items.
23. Dogfood the mobile sidebar drawer on a real phone/narrow browser: confirm the recency groups feel useful with real session history, Today/Yesterday expanded defaults are right, backdrop/toggle close behavior feels obvious, header density is still acceptable, and settings remain lower-priority than session switching.
24. Dogfood the desktop sidebar overlay in real work: confirm the header hamburger is discoverable enough without the old 48px rail, backdrop/Escape/toggle close behavior feels right, pinned docked mode remains understandable, and grouped sessions reduce noise without hiding needed continuations.
25. Dogfood the shared editable metadata suggestion flow on desktop and a real phone/narrow browser: confirm ✨ generation progress, provider errors, editable title/summary fields, per-field ✓/× controls, and the trigger-adjacent mobile popover feel obvious; tune saved-summary visibility on mobile if hiding the collapsed summary still feels like a gap after applying.
26. Dogfood the mobile layout on a real phone/narrow browser: confirm the hamburger drawer feels quick, session-first ordering is right, no side rails steal space, the compact `Ctx` pill is readable in the lower composer bar, and hiding Details/Preview on mobile does not block common work. Tune drawer close behavior/backdrop/header density or continue desktop/mobile parity gaps as needed.
27. Continue the full transcript performance spike focused on decoupling chat rendering from the rest of the app shell. Use the real `Fix Screenshot Attachment Flow` session plus `tool-image-heavy-transcript` and `mobile-long-transcript-controls` as benchmarks; next targets are inspector/session chrome/show-thinking updates so they patch local UI islands instead of causing transcript row hydration/traversal, and a dedicated transcript component/controller boundary before deeper virtualization.
28. Dogfood the long-transcript performance path in the real mobile session that felt sluggish. The focused fake-agent mobile control baseline is currently healthy, so if the real session still lags, capture whether the pain is larger transcript scale than 96 tools, initial snapshot hydration, browser/device class, image loading, or a control path not covered by the benchmark.
29. Dogfood the simplified screenshot flow in the original remote-session browser: drag, paste, and paperclip should all attach prompt image bytes and insert/render `.bakery/artifacts/...` transcript preview paths without any image mode choice. If this feels good, consider removing or shortening older artifact-mode docs/log references in follow-up cleanup.
30. Continue the architecture-review/iteration-speed pass with the next smallest extraction only after checking current hotspots: likely session lifecycle/controller helpers, composer image/drop handling, or a pure rendering/state island, using `bun run report:iteration --recommend <changed files>` before validation.
31. Dogfood `bun run project:notes` at the start of future sessions; tune its sections/caps if it omits needed handoff context or still prints too much.
32. Dogfood the new `## Validation decision` handoff block in future sessions; tune `scripts/report-iteration.ts` guidance or `scripts/assert-workflow-skills.ts` assertions if agents still skip the report, omit full-suite run/skip rationale, or if recommendations are noisy.
33. Continue dogfooding `/plan` in real model sessions and tune launcher prompt/tool guidance if the model drifts from the canonical `grill-me` behavior: relentless but practical one-question-at-a-time interviewing, codebase exploration before asking discoverable questions, recommended answers for every question, and decision-tree branch resolution before final handoff.
34. Continue keyboard-first polish from dogfooding if new edge cases appear: reconnect timing beyond snapshot reload, Q&A viewer take-control flow from the question panel, transcript fork action accessibility, and additional shortcut coverage.
35. Continue evolving the typed bundled workflow-skill registry into a fuller default-installed skills layer: editable/content-like instructions, clearer resource provenance in UI/API surfaces, and reusable launch paths for additional bakery-owned workflows.
36. Tune answered-question transcript summaries from real-use feedback: visual density, whether to show the recommendation/chosen option metadata, and interaction with tool-card grouping/collapse.
37. Continue the theme/color pass from real-use feedback using `theme-gallery` first: inspect the gallery screenshots for remaining light/dark inconsistencies, then tune selected-row emphasis, tool/activity grouping, tree drawer, and lower-frequency overlays with contrast checks rather than trying to mimic GitHub directly.
38. Consider adding a lightweight contrast/token audit for core foreground/background pairs now that the visual gallery exists.
39. Tune session identity/sidebar details: adjust status labels, snippets, auto-collapse behavior, pin-mode copy, and summary preview density if they feel noisy in ongoing use.
40. Continue attention hierarchy from new real-use feedback: current-action strip, user-input-needed states, selected-row emphasis, and completed activity grouping now have a first pass but may need tuning during real dogfooding.
41. Fix any real-session lifecycle/UI state issues that surface: duplicate snapshots, stale controller state, scroll edge cases, reconnect copy, retry behavior, or remaining selection-preservation edge cases during live streaming.
42. Tighten web-perf thresholds over time once more baseline runs are available, and consider reporting percentile timings in addition to max timings.
43. Add sanitized real-event playback to complement the deterministic fake runner if real SDK/provider event patterns diverge from fake-agent coverage.
44. Expand branch/tree support beyond basic navigation/current-path clarity: add keyboard navigation, summarize-before-navigation flow, label/bookmark editing, filter/search modes, and clearer current-path rendering refinements from dogfooding.
45. Explore `@mariozechner/pi-web-ui` adapter once the remote agent state shape is clearer.

## Session handoff convention

At the end of each work session:

1. Run `bun run check`.
2. Update `PROJECT_LOG.md` current status / next priorities if anything changed.
3. Tell the human operator how to test the changes manually, including whether the current browser page can pick them up automatically, requires a page refresh, or requires a backend/dev-server restart.
4. Commit with a concise Conventional Commit message.
5. In the next AI session, start by reading `DESIGN.md` and `PROJECT_LOG.md`.

## UI validation convention

- For any UI feature touching typing, autocomplete, streaming, scrolling, or keyboard navigation: avoid full-app renders in the interaction path; patch local DOM islands and add focused harness coverage for responsiveness.
- UI-affecting changes should run `bun run report:iteration --recommend <changed files>` first, then `bun run check` plus the selected focused harness scenarios; run full `bun run test:web-perf` only when the selector/lifecycle breadth/focused failures justify escalation.
- Use `bun run ui:manual` for headed fake-agent exploratory validation in a temp workspace.
- Include harness scenario names and artifact paths in handoffs.
- When harness screenshots are generated, include the most relevant PNG file paths as workspace-relative paths in the handoff, not just artifact directories, so the web transcript renders thumbnail previews directly in the dev loop.
- If a new UI feature is not covered by the harness, extend `scripts/ui-harness.ts` with a scenario before relying on human-only manual testing where practical.